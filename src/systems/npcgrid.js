(function(){"use strict";const CBZ=window.CBZ;if(!CBZ||!CBZ.makeGrid)return;const CELL=10,grid=CBZ.makeGrid(CELL);let ready=!1;function posOf(n){return n.group.position}function rebuild(){grid.rebuild(CBZ.npcs,posOf),ready=!0}CBZ.queryNpcsNear=function(x,z,radius,out){out=out||[],out.length=0,ready||rebuild();const span=Math.ceil(radius/CELL),gx=grid.cellIndex(x),gz=grid.cellIndex(z),r2=radius*radius;for(let cx=gx-span;cx<=gx+span;cx++)for(let cz=gz-span;cz<=gz+span;cz++){const bucket=grid.bucket(cx,cz);if(bucket)for(let i=0;i<bucket.length;i++){const n=bucket[i],p=n.group.position,dx=p.x-x,dz=p.z-z;dx*dx+dz*dz<=r2&&out.push(n)}}return out},CBZ.onUpdate(21.7,function(){CBZ.game.mode==="escape"&&rebuild()})})();
