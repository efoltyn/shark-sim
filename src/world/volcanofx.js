(function(){"use strict";const CBZ=window.CBZ;if(!CBZ||!window.THREE)return;const THREE=window.THREE;CBZ.CONFIG=CBZ.CONFIG||{},CBZ.CONFIG.VOLCANO_V2==null&&(CBZ.CONFIG.VOLCANO_V2=!0),CBZ.CONFIG.VOLCANO_PYRO==null&&(CBZ.CONFIG.VOLCANO_PYRO=!0),CBZ.CONFIG.VOLCANO_LAHAR==null&&(CBZ.CONFIG.VOLCANO_LAHAR=!0),CBZ.CONFIG.VOLCANO_V3==null&&(CBZ.CONFIG.VOLCANO_V3=!0),CBZ.CONFIG.VOLCANO_ASH_LOAD==null&&(CBZ.CONFIG.VOLCANO_ASH_LOAD=CBZ.CONFIG.VOLCANO_V3!==!1);const V={},census={lava:0,pyro:0,lahar:0,ash:0,lights:0,tris:0,branches:0,vent:0,fountain:0},LIVE={lava:[],pyro:[],lahar:[],ash:[],vent:[],column:[],fountain:[]};function h01(x,z,salt){return CBZ.hash01?CBZ.hash01(x,z,salt|0):.5}function clamp(v,a,b){return v<a?a:v>b?b:v}function q(lo,hi){return CBZ.qScale?CBZ.qScale(lo,hi):hi}function qi(lo,hi){return Math.max(1,Math.round(q(lo,hi)))}const _c1=new THREE.Color,_c2=new THREE.Color,_c3=new THREE.Color;function ramp3(t,a,b,c,out){return t=clamp(t,0,1),t<.5?(_c1.setHex(a),_c2.setHex(b),out.copy(_c1).lerp(_c2,t*2)):(_c1.setHex(b),_c2.setHex(c),out.copy(_c1).lerp(_c2,(t-.5)*2))}function flatGround(){return 0}let _ashTex=null;function ashTex(){if(_ashTex)return _ashTex;const S=64,cv=document.createElement("canvas");cv.width=cv.height=S;const g=cv.getContext("2d");let seed=1632740311;const rnd=function(){return seed=seed*1664525+1013904223>>>0,seed/4294967296},lobe=function(lx,ly,r){const rg=g.createRadialGradient(lx,ly,r*.45,lx,ly,r);rg.addColorStop(0,"rgba(255,255,255,1)"),rg.addColorStop(1,"rgba(255,255,255,0)"),g.fillStyle=rg,g.beginPath(),g.arc(lx,ly,r,0,6.2832),g.fill()};lobe(S*.5,S*.5,S*.34);for(let i=0;i<14;i++){const a=rnd()*6.2832,d=S*(.12+rnd()*.24);lobe(S*.5+Math.cos(a)*d,S*.5+Math.sin(a)*d,S*(.09+rnd()*.13))}return _ashTex=new THREE.CanvasTexture(cv),_ashTex}let _glowTex=null;function glowTex(){if(_glowTex)return _glowTex;const S=64,cv=document.createElement("canvas");cv.width=cv.height=S;const g=cv.getContext("2d"),rg=g.createRadialGradient(S/2,S/2,0,S/2,S/2,S/2);return rg.addColorStop(0,"rgba(255,255,255,1)"),rg.addColorStop(.35,"rgba(255,222,170,0.62)"),rg.addColorStop(1,"rgba(255,180,110,0)"),g.fillStyle=rg,g.fillRect(0,0,S,S),_glowTex=new THREE.CanvasTexture(cv),_glowTex}function fallLine(o){o=o||{};const groundAt=o.groundAt||flatGround,step=o.step>0?+o.step:3,count=Math.max(2,Math.round(o.count||24)),turn=o.channel?.8:o.turn!=null?+o.turn:.45,wander=o.wander!=null?+o.wander:.16,salt=o.salt|0,probe=step*1.15;let x=+o.x||0,z=+o.z||0,b=o.bearing;if(b==null){let bestH=1e9;for(let k=0;k<12;k++){const a=k/12*Math.PI*2,hh=groundAt(x+Math.cos(a)*probe,z+Math.sin(a)*probe);hh<bestH&&(bestH=hh,b=a)}b==null&&(b=0)}const pts=[{x,z,y:groundAt(x,z)}];for(let i=1;i<count;i++){let best=b,bestH=1e9;for(let k=-3;k<=3;k++){const a=b+k/3*.91,hh=groundAt(x+Math.cos(a)*probe,z+Math.sin(a)*probe)+Math.abs(k)*1e-4;hh<bestH&&(bestH=hh,best=a)}let d=best-b;for(;d>Math.PI;)d-=Math.PI*2;for(;d<-Math.PI;)d+=Math.PI*2;b+=d*turn+(h01(x,z,salt+i)-.5)*wander,x+=Math.cos(b)*step,z+=Math.sin(b)*step,pts.push({x,z,y:groundAt(x,z)})}return{pts,seg:step,total:step*(count-1)}}V.fallLine=fallLine;function smoothPath(path,passes,groundAt){const P=path.pts;if(!P||P.length<3)return path;for(let k=0;k<(passes||1);k++){let px=P[0].x,pz=P[0].z;for(let i=1;i<P.length-1;i++){const cx=P[i].x,cz=P[i].z;P[i].x=cx*.5+(px+P[i+1].x)*.25,P[i].z=cz*.5+(pz+P[i+1].z)*.25,px=cx,pz=cz}}for(let i=0;i<P.length;i++)P[i].y=groundAt(P[i].x,P[i].z);return path}V.smoothPath=smoothPath;function pathAt(path,s,out){const P=path.pts,seg=path.seg;if(s<=0)return out.set(P[0].x,P[0].y,P[0].z),out;const fi=s/seg,i=Math.floor(fi);if(i>=P.length-1){const p=P[P.length-1];return out.set(p.x,p.y,p.z),out}const t=fi-i,a=P[i],b2=P[i+1];return out.set(a.x+(b2.x-a.x)*t,a.y+(b2.y-a.y)*t,a.z+(b2.z-a.z)*t),out}V.pathAt=pathAt;function pathCoord(path,x,z,liveLen){const P=path.pts,seg=path.seg,nMax=Math.min(P.length-1,Math.max(1,Math.ceil((liveLen??1e9)/seg)));let bestPerp=1e9,bestS=-1;for(let i=0;i<nMax;i++){const a=P[i],b2=P[i+1],dx=b2.x-a.x,dz=b2.z-a.z,L2=dx*dx+dz*dz||1;let t=((x-a.x)*dx+(z-a.z)*dz)/L2;t=clamp(t,0,1);const px=a.x+dx*t,pz=a.z+dz*t,perp=Math.hypot(x-px,z-pz);perp<bestPerp&&(bestPerp=perp,bestS=(i+t)*seg)}return{s:bestS,perp:bestPerp}}V.pathCoord=pathCoord;const LAVA_COLS=9,LAVA_U=[-1,-.87,-.55,-.3,0,.3,.55,.87,1],LAVA_COLS_N=5,LAVA_U_N=[-1,-.55,0,.55,1],MELT_INSET=.82,CRUST_A=920070,CRUST_B=1577744,CRUST_C=4071443,_LID_D=new THREE.Color(460035),_LID_W=new THREE.Color(2099973),MELT_A=3147521,MELT_B=6950916,MELT_C=14174223,LACE_W=20,LACE_L=24;let _laceTex=null;function laceTex(){if(_laceTex)return _laceTex;const S=512,cv=document.createElement("canvas");cv.width=cv.height=S;const g=cv.getContext("2d"),img=g.createImageData(S,S),D=img.data,top=new THREE.Color(MELT_C),stops=[new THREE.Color(_LID_D),new THREE.Color(_LID_W),new THREE.Color(MELT_B),top],KY=6.2831853/LACE_L,KX=6.2831853/LACE_W,LUT=new Uint8Array(256*3);for(let q2=0;q2<256;q2++){const t=q2/255,seg3=t<.22?0:t<.55?1:2,k3=seg3===0?t/.22:seg3===1?(t-.22)/.33:(t-.55)/.45;_c3.copy(stops[seg3]).lerp(stops[seg3+1],clamp(k3,0,1)),LUT[q2*3]=Math.round(255*clamp(_c3.r/top.r,0,1)),LUT[q2*3+1]=Math.round(255*clamp(_c3.g/top.g,0,1)),LUT[q2*3+2]=Math.round(255*clamp(_c3.b/top.b,0,1))}for(let py=0;py<S;py++){const sm=py/S*LACE_L;for(let pxi=0;pxi<S;pxi++){const vm=(pxi/S-.5)*LACE_W,a=sm*KY,b=vm*KX,w=Math.sin(a*1+b*.9)+.62*Math.sin(a*2-b*1.6+2.1)+.35*Math.sin(a*3+b*2.9+.6),f=.5*Math.sin(a*3+b*2.5+1.9*w)+.3*Math.sin(a*5-b*1.8+1.7+1.3*w)+.14*Math.sin(a*2+b*4.3+4.1+.8*w)+.1*Math.sin(a*7-b*5.4+.7),q2=clamp(1-Math.abs(f)*1.6,0,1)*255|0,o=(py*S+pxi)*4,q3=q2*3;D[o]=LUT[q3],D[o+1]=LUT[q3+1],D[o+2]=LUT[q3+2],D[o+3]=255}}g.putImageData(img,0,0);const tx=new THREE.CanvasTexture(cv);tx.wrapS=tx.wrapT=THREE.RepeatWrapping,tx.minFilter=THREE.LinearMipmapLinearFilter,tx.magFilter=THREE.LinearFilter,tx.generateMipmaps=!0;const rn=CBZ.renderer;return rn&&rn.capabilities&&rn.capabilities.getMaxAnisotropy&&(tx.anisotropy=Math.min(4,rn.capabilities.getMaxAnisotropy())),_laceTex=tx,_laceTex}V.lavaFlow=function(o){o=o||{};const parent=o.parent||CBZ.scene,groundAt=o.groundAt||flatGround,width=o.width>0?+o.width:5.5,halfW=width*.5,narrow=width<4.2,COLS=narrow?LAVA_COLS_N:LAVA_COLS,UU=narrow?LAVA_U_N:LAVA_U,len=o.len>0?+o.len:40,speed=o.speed>0?+o.speed:5,salt=o.salt!=null?o.salt|0:4711,seg=clamp(width*.38,2.3,2.8),path=smoothPath(fallLine({x:o.x,z:o.z,groundAt,bearing:o.bearing,step:seg,count:Math.ceil(len/seg)+1,salt,turn:.3,wander:.06}),2,groundAt),N=path.pts.length,branchN=o.branches>0?Math.min(3,o.branches|0):0,branchPts=[];for(let bi=0;bi<branchN;bi++){const bt=.3+.3*bi+.16*h01(o.x+bi*13,o.z-bi*7,salt+77);branchPts.push({at:Math.min(len*.86,len*bt),done:!1})}const children=[],wPh1=h01(o.x,o.z,salt+51)*6.28,wPh2=h01(o.x,o.z,salt+63)*6.28,mPh=h01(o.x,o.z,salt+71)*6.28,wProf=new Float32Array(N),hot0=new Float32Array(N),meander=new Float32Array(N);for(let i=0;i<N;i++){const p=path.pts[i],u=i/(N-1),sm=i*seg,lobe=1+.34*Math.sin(sm*.26+wPh1)+.17*Math.sin(sm*.62+wPh2);wProf[i]=(.5+.72*Math.pow(u,.7))*lobe*(.9+.2*h01(p.x,p.z,salt+11)),hot0[i]=clamp(1.06-.6*u+(h01(p.x,p.z,salt+37)-.5)*.16,.1,1),meander[i]=.13*Math.sin(i*.44+mPh)+.05*Math.sin(i*1.03+mPh*2)}const cGY=new Float32Array(N*COLS),mGY=new Float32Array(N*COLS),grain=new Float32Array(N*COLS),nX=new Float32Array(N),nZ=new Float32Array(N);for(let i=0;i<N;i++){const p=path.pts[i],a=path.pts[Math.max(0,i-1)],b2=path.pts[Math.min(N-1,i+1)];let nx=-(b2.z-a.z),nz=b2.x-a.x;const nl=Math.hypot(nx,nz)||1;nX[i]=nx/nl,nZ[i]=nz/nl;const hw=halfW*wProf[i];for(let c=0;c<COLS;c++){const u=UU[c],au=Math.abs(u),k=i*COLS+c,x=p.x+nX[i]*u*hw,z=p.z+nZ[i]*u*hw,g=groundAt(x,z);cGY[k]=g+(Math.max(g,p.y)-g)*(1-au*au);const mu=u*MELT_INSET+meander[i]*(1-au),amu=Math.abs(mu),mx=p.x+nX[i]*mu*hw,mz=p.z+nZ[i]*mu*hw,mg=groundAt(mx,mz);mGY[k]=mg+(Math.max(mg,p.y)-mg)*(1-amu*amu*amu),grain[k]=h01(x,z,salt+5)}}const cPos=new Float32Array(N*COLS*3),cCol=new Float32Array(N*COLS*3),cIdx=new Uint16Array(Math.max(1,(N-1)*(COLS-1)*6));let ii=0;for(let i=0;i<N-1;i++)for(let c=0;c<COLS-1;c++){const a=i*COLS+c,b2=a+1,d=a+COLS,e=d+1;cIdx[ii++]=a,cIdx[ii++]=d,cIdx[ii++]=b2,cIdx[ii++]=b2,cIdx[ii++]=d,cIdx[ii++]=e}const crustGeo=new THREE.BufferGeometry;crustGeo.setAttribute("position",new THREE.BufferAttribute(cPos,3)),crustGeo.setAttribute("color",new THREE.BufferAttribute(cCol,3)),crustGeo.setIndex(new THREE.BufferAttribute(cIdx,1));const crustMat=new THREE.MeshPhongMaterial({vertexColors:!0,flatShading:!0,shininess:0,specular:0,side:THREE.DoubleSide,polygonOffset:!0,polygonOffsetFactor:-3,polygonOffsetUnits:-3}),crust=new THREE.Mesh(crustGeo,crustMat);crust.frustumCulled=!1,crust.renderOrder=2,parent.add(crust);const kPos=new Float32Array(N*COLS*3),kCol=new Float32Array(N*COLS*3),kUv=new Float32Array(N*COLS*2),kIdx=new Uint16Array(Math.max(1,(N-1)*(COLS-1)*6));ii=0;for(let i=0;i<N-1;i++)for(let c=0;c<COLS-1;c++){const a=i*COLS+c,b2=a+1,d=a+COLS,e=d+1;kIdx[ii++]=a,kIdx[ii++]=d,kIdx[ii++]=b2,kIdx[ii++]=b2,kIdx[ii++]=d,kIdx[ii++]=e}const chGeo=new THREE.BufferGeometry;chGeo.setAttribute("position",new THREE.BufferAttribute(kPos,3)),chGeo.setAttribute("color",new THREE.BufferAttribute(kCol,3)),chGeo.setAttribute("uv",new THREE.BufferAttribute(kUv,2)),chGeo.setIndex(new THREE.BufferAttribute(kIdx,1));const chMat=new THREE.MeshBasicMaterial({vertexColors:!0,side:THREE.DoubleSide,fog:!0,map:laceTex(),polygonOffset:!0,polygonOffsetFactor:-6,polygonOffsetUnits:-6}),chan=new THREE.Mesh(chGeo,chMat);chan.frustumCulled=!1,chan.renderOrder=3,parent.add(chan);const hazeN=o.haze===!1?0:qi(2,Math.max(2,Math.min(5,Math.round(N*.22)))),hazeMat=new THREE.SpriteMaterial({map:glowTex(),color:16746038,transparent:!0,opacity:.06,blending:THREE.AdditiveBlending,depthWrite:!1,fog:!0}),haze=[];for(let i=0;i<hazeN;i++){const s=new THREE.Sprite(hazeMat);s.scale.set(width*.6,width*.6,1),s.renderOrder=8,s.visible=!1,parent.add(s),haze.push({sp:s,u:(i+.5)/hazeN,ph:Math.random()*6.28})}let light=null;o.light!==!1&&CBZ.CONFIG.VOLCANO_V2!==!1&&(light=new THREE.PointLight(16738846,0,Math.max(24,width*6),2),light.castShadow=!1,parent.add(light),census.lights++);let adv=Math.min(seg*1.05,len),age=0,dead=!1,lidT=0,lastSt=-1,quenchT=-1,phT=0,settled=!1;const QUENCH_SECS=8,_v=new THREE.Vector3,skinV=speed*2.4,laceOff=h01(o.x,o.z,salt+301)*LACE_L*7;function ring(i,cx,cy0,cz,nx,nz,hw,thick,hot,s,ph,bedC,bedM){const u01=N>1?clamp(s/Math.max(.001,(N-1)*seg),0,1):0;for(let c=0;c<COLS;c++){const u=UU[c],au=Math.abs(u),k=i*COLS+c,off=k*3,bump=Math.max(0,1-Math.pow((au-.87)*7,2)),crown=Math.max(0,1-Math.pow(au,3.6))+.5*bump,edgeK=.5+.5*(1-Math.pow(au,5)),gr=(.84+.3*grain[k])*edgeK,grM=.8+.2*grain[k];cPos[off]=cx+nx*u*hw,cPos[off+2]=cz+nz*u*hw,cPos[off+1]=(bedC?bedC[k]:cy0)+.05+thick*crown,ramp3(clamp((1-au)*(.3+.7*hot),0,1),CRUST_A,CRUST_B,CRUST_C,_c3),cCol[off]=_c3.r*gr,cCol[off+1]=_c3.g*gr,cCol[off+2]=_c3.b*gr;const mu=u*MELT_INSET+meander[i]*(1-au),amu=Math.abs(mu);kPos[off]=cx+nx*mu*hw,kPos[off+2]=cz+nz*mu*hw,kPos[off+1]=(bedM?bedM[k]:cy0)+.05+thick*(.45+.55*(1-Math.pow(amu,3.2)))+.04;const across=1-Math.pow(amu,3.4+2.1*u01),open=.98-.22*u01,flood=clamp((1-s/6)*1.3,0,1)*(.5+.5*hot);let env=across*open*Math.pow(hot,1.6);env=Math.max(env,flood*across),ramp3(clamp(env,0,1),MELT_A,MELT_B,MELT_C,_c3),_c3.multiplyScalar(Math.min(1,env*4.5)),kCol[off]=_c3.r*grM,kCol[off+1]=_c3.g*grM,kCol[off+2]=_c3.b*grM;const uo=k*2;kUv[uo]=.5+mu*hw/LACE_W,kUv[uo+1]=(s-ph)/LACE_L}}function stationThick(i){const bornAt=i*seg/Math.max(.001,speed),localAge=clamp(age-bornAt,0,120),qk=quenchT>=0?Math.min(1,quenchT/QUENCH_SECS):0;return{t:.18+.5*(localAge/(localAge+6))+.1*wProf[i],cool:clamp(localAge/90+qk,0,1),qk}}function writeStations(from,to){const ph=phT*skinV+laceOff;for(let i=from;i<=to&&i<N;i++){const p=path.pts[i],st=stationThick(i);ring(i,p.x,p.y,p.z,nX[i],nZ[i],halfW*wProf[i],st.t,hot0[i]*(1-.58*st.cool)*(1-.9*st.qk),i*seg,ph,cGY,mGY)}}const _noseC=new Float32Array(COLS),_noseM=new Float32Array(COLS);function writeNose(i){if(i<1)return;pathAt(path,adv,_v);const i0=i-1,a=path.pts[i0],b2=path.pts[Math.min(N-1,i)];let nx=-(b2.z-a.z),nz=b2.x-a.x;const nl=Math.hypot(nx,nz)||1;nx/=nl,nz/=nl;const frac=clamp((adv-i0*seg)/seg,0,1),hw=halfW*wProf[i]*(.34+.66*frac);for(let c=0;c<COLS;c++){const u=UU[c],au=Math.abs(u),k=i*COLS+c;_noseC[c]=cGY[k],_noseM[c]=mGY[k];const g=groundAt(_v.x+nx*u*hw,_v.z+nz*u*hw);cGY[k]=g+(Math.max(g,_v.y)-g)*(1-au*au);const mun=u*MELT_INSET+meander[i]*(1-au),amun=Math.abs(mun),mg=groundAt(_v.x+nx*mun*hw,_v.z+nz*mun*hw);mGY[k]=mg+(Math.max(mg,_v.y)-mg)*(1-amun*amun*amun)}const st=stationThick(i);ring(i,_v.x,_v.y,_v.z,nx,nz,hw,st.t*(.55+.45*frac),hot0[i]*(1-.58*st.cool)*(1-.9*st.qk),adv,phT*skinV+laceOff,cGY,mGY);for(let c=0;c<COLS;c++){const k=i*COLS+c;cGY[k]=_noseC[c],mGY[k]=_noseM[c]}}writeStations(0,1),crustGeo.computeVertexNormals();const handle={kind:"lava",group:crust,path,mesh:crust,channel:chan,get length(){return adv},get tip(){return pathAt(path,adv,_v),{x:_v.x,y:_v.y,z:_v.z}},get mid(){return pathAt(path,adv*.5,_v),{x:_v.x,y:_v.y,z:_v.z}},update(dt){if(dead||settled)return handle;age+=dt;const qk=quenchT>=0?Math.min(1,(quenchT+=dt)/QUENCH_SECS):0;phT+=dt*(1-qk),quenchT<0&&(adv=Math.min(len,adv+speed*dt)),quenchT>=0&&(branchPts.length=0);for(let b=0;b<branchPts.length;b++){const bp=branchPts[b];if(bp.done||adv<bp.at||(bp.done=!0,LIVE.lava.length>=26))continue;pathAt(path,bp.at,_v);const i0=clamp(Math.floor(bp.at/seg),0,N-2),A=path.pts[i0],B2=path.pts[i0+1],side=h01(_v.x,_v.z,salt+88)<.5?-1:1,a2=Math.atan2(B2.z-A.z,B2.x-A.x)+side*(.5+.55*h01(_v.x,_v.z,salt+99));children.push(V.lavaFlow({x:_v.x,z:_v.z,groundAt,parent,bearing:a2,width:width*.62,len:Math.max(11,(len-bp.at)*.85),speed:speed*.9,salt:salt*3+101+b*29,light:!1,haze:!1,branches:0})),census.branches++}for(let b=0;b<children.length;b++)children[b].update(dt);const st=Math.min(N-1,Math.floor(adv/seg)+1);lidT+=dt,(st!==lastSt||lidT>=.033||qk>=1)&&(lidT=0,lastSt=st,writeStations(0,Math.max(0,st-1))),writeNose(st),crustGeo.attributes.position.needsUpdate=!0,crustGeo.attributes.color.needsUpdate=!0,chGeo.attributes.position.needsUpdate=!0,chGeo.attributes.color.needsUpdate=!0,chGeo.attributes.uv.needsUpdate=!0,crustGeo.setDrawRange(0,Math.max(0,st)*(COLS-1)*6),chGeo.setDrawRange(0,Math.max(0,st)*(COLS-1)*6);const fl=.96+(.06*Math.sin(age*5.3)+Math.random()*.04-.04)*(1-qk);chMat.color.setScalar(clamp(fl,.75,1.04));for(let i=0;i<haze.length;i++){const H=haze[i],s=adv*H.u;if(s<.5||qk>=1){H.sp.visible=!1;continue}pathAt(path,s,_v),H.sp.visible=!0,H.sp.position.set(_v.x+Math.sin(age*.9+H.ph)*width*.3,_v.y+1.4+.7*Math.sin(age*1.6+H.ph),_v.z+Math.cos(age*.8+H.ph)*width*.3);const sc=width*(.42+.13*Math.sin(age*1.1+H.ph))*(1-qk);H.sp.scale.set(Math.max(.01,sc),Math.max(.01,sc),1)}return light&&(pathAt(path,adv*.82,_v),light.position.set(_v.x,_v.y+2.2,_v.z),light.intensity=(1.5+.35*Math.sin(age*4.1))*(1-qk)),qk>=1&&(settled=!0,chMat.color.setScalar(1)),handle},quench(){if(quenchT<0){quenchT=0;for(let b=0;b<children.length;b++)children[b].quench()}return handle},get quenched(){return quenchT>=0},hitTest(x,z){if(quenchT>=0)return!1;const c=pathCoord(path,x,z,adv);if(c.s>=0&&c.s<=adv){const i=clamp(Math.round(c.s/seg),0,N-1);if(c.perp<halfW*wProf[i]*.95)return!0}for(let b=0;b<children.length;b++)if(children[b].hitTest(x,z))return!0;return!1},progress(){return adv/len},dispose(){if(dead)return;dead=!0;for(let b=0;b<children.length;b++)children[b].dispose();parent.remove(crust),parent.remove(chan),crustGeo.dispose(),crustMat.dispose(),chGeo.dispose(),chMat.dispose();for(let i=0;i<haze.length;i++)parent.remove(haze[i].sp);hazeMat.dispose(),light&&(parent.remove(light),census.lights--);const k=LIVE.lava.indexOf(handle);k>=0&&LIVE.lava.splice(k,1)}};return census.lava++,LIVE.lava.push(handle),handle};let _gradeRead=!1,_TONE=!0,_GRADE=!0,_EXP=1.16;const GRADE_GAIN=[1.025,1.005,.982],GRADE_LIFT=[.0015,.0032,.0072];function toScreen(lin,ch){let c=lin;return _TONE&&(c=c*_EXP/.6,c=(c*(c+.0245786)-90537e-9)/(c*(.983729*c+.432951)+.238081),c=clamp(c,0,1),_GRADE&&(c=Math.pow(Math.max(c/.18,0),1.075)*.18,c=c*GRADE_GAIN[ch]+GRADE_LIFT[ch]),c=clamp(c,0,1)),c<=.0031308?c*12.92:1.055*Math.pow(c,1/2.4)-.055}const _screenCache={};function screenHex(r,g,b){_gradeRead||(_gradeRead=!0,_TONE=CBZ.toneMappingOn!==!1,_GRADE=CBZ.CONFIG.RENDER_GRADE_V1!==!1,_EXP=(+CBZ.renderExposureBase||1.16)*1.05);const key=r<<16|g<<8|b;if(_screenCache[key]!=null)return _screenCache[key];const want=[r/255,g/255,b/255];let out=0;for(let ch=0;ch<3;ch++){let lo=0,hi=1;for(let i=0;i<24;i++){const mid=(lo+hi)*.5;toScreen(mid,ch)<want[ch]?lo=mid:hi=mid}out=out<<8|clamp(Math.round((lo+hi)*.5*255),0,255)}return _screenCache[key]=out,out}V.screenHex=screenHex;const _sunXZ={x:.6,z:-.8};function sunBearing(){const s=CBZ.sun;if(s&&s.position){let dx=s.position.x,dz=s.position.z;const tg=CBZ.sunTarget;tg&&tg.position&&(dx-=tg.position.x,dz-=tg.position.z);const L=Math.sqrt(dx*dx+dz*dz);L>1e-4&&(_sunXZ.x=dx/L,_sunXZ.z=dz/L)}return _sunXZ}V.ashColumn=function(o){o=o||{};const parent=o.parent||CBZ.scene,x=+o.x||0,z=+o.z||0,baseY=o.y!=null?+o.y:0,height=o.height>0?+o.height:55,r0=o.r>0?+o.r:7,mats=[],useFog=!o.fogless,CH_BASE=screenHex(30,25,21),CH_BODY=screenHex(48,45,43),CH_CROWN=screenHex(68,65,61),C_NIGHT=new THREE.Color(screenHex(12,12,15)),C_SUN=new THREE.Color(screenHex(172,164,149)),C_VENT=new THREE.Color(screenHex(214,96,38)),VENT_REACH=18+.95*r0,N=qi(80,156),grp=new THREE.Group;grp.frustumCulled=!1,parent.add(grp);const RISE=5,END=1.4;let seed=((x*73856093|0)^(z*19349663|0)^1638253527)>>>0;const rnd=function(){return seed=seed*1664525+1013904223>>>0,seed/4294967296},puffs=[];for(let i=0;i<N;i++){const mat=new THREE.SpriteMaterial({map:puffTex(i%3),color:CH_BODY,transparent:!0,opacity:0,depthWrite:!1,depthTest:!0,fog:useFog,blending:THREE.NormalBlending,rotation:(rnd()-.5)*.5});mats.push(mat);const m=new THREE.Sprite(mat);m.renderOrder=7,m.visible=!1,grp.add(m);const role=i%10<3?0:i%10<8?1:2,rr=role===0?.44*Math.sqrt(rnd()):role===1?.26+.56*Math.sqrt(rnd()):.66+.46*rnd();puffs.push({m,role,life:-(i/N)*RISE*END,ang:rnd()*6.2832,rr,ph:rnd()*6.2832,sz:(role===0?1.06:role===1?.98:.82)*(.78+.44*rnd()),aspect:.74+rnd()*.54,spin:(rnd()-.5)*.05,wind:.76+rnd()*.38,maxOp:role===0?.56:role===1?.46:.28})}let t=0,dead=!1;const handle={kind:"column",group:grp,puffCount:N,update(dt,wx,wz,opts){if(dead)return handle;t+=dt;const bx=wx||0,bz=wz||0,night=opts&&opts.night!=null?clamp(+opts.night,0,1):clamp(1-(.5+2.2*(CBZ.sunHeight!=null?CBZ.sunHeight:.5)),0,1),day=1-night,sd=sunBearing();for(let i=0;i<puffs.length;i++){const P=puffs[i];if(P.life+=dt,P.life<0){P.m.visible=!1;continue}let u=P.life/RISE;u>=END&&(P.life=0,u=0,P.ang=rnd()*6.2832,P.ph=rnd()*6.2832,P.wind=.76+rnd()*.38);const climb=Math.min(1,u),hh=1-Math.pow(1-climb,1.3),head=clamp((u-1)/(END-1),0,1),axisR=(r0+.2*hh*height)*(1+.85*head),lean=height*.27*hh*hh*P.wind,drift=P.ang+hh*.72+t*(.018+P.spin*.08),radial=axisR*P.rr*(1+head*(P.role===2?.34:.12)),capLift=head*height*(.03+.05*Math.sin(P.ph)),py=baseY+hh*height+capLift;P.m.position.set(x+Math.cos(drift)*radial+bx*lean,py,z+Math.sin(drift)*radial+bz*lean);const sc=axisR*(.52+.44*P.sz)*(1+.16*head);P.m.scale.set(sc*P.aspect,sc*(1.12-(P.aspect-.74)*.3),1);const born=clamp(u/.016,0,1),dying=u<1.2?1:clamp((END-u)/(END-1.2),0,1);P.m.material.opacity=P.maxOp*born*dying,P.m.material.rotation+=P.spin*dt;const col=P.m.material.color;if(ramp3(hh,CH_BASE,CH_BODY,CH_CROWN,col),night>0&&col.lerp(C_NIGHT,night*.88),day>.02){const sunSide=(Math.cos(drift)*sd.x+Math.sin(drift)*sd.z)*P.rr;sunSide>0&&col.lerp(C_SUN,sunSide*day*(.1+.46*hh))}const dy=py-baseY;if(dy<VENT_REACH){const gk=1-dy/VENT_REACH;col.lerp(C_VENT,gk*gk*(.2+.68*night))}P.m.visible=P.m.material.opacity>.012&&sc>.05}return handle},dispose(){if(dead)return;dead=!0;for(let i=0;i<puffs.length;i++)grp.remove(puffs[i].m);parent.remove(grp);for(let i=0;i<mats.length;i++)mats[i].dispose();const k=LIVE.column.indexOf(handle);k>=0&&LIVE.column.splice(k,1)}};return LIVE.column.push(handle),handle},V.fountain=function(o){o=o||{};const parent=o.parent||CBZ.scene,x=+o.x||0,z=+o.z||0,baseY=o.y!=null?+o.y:0,groundAt=typeof o.groundAt=="function"?o.groundAt:flatGround,mag=clamp(o.mag!=null?+o.mag:.5,0,1),mouth=o.r>0?+o.r:1.6+2.4*mag,G=CBZ.TUNE&&CBZ.TUNE.gravity>0?CBZ.TUNE.gravity:20,V0=18+20*mag,CONE=(15+12*mag)*Math.PI/180,N=qi(56,132),F_HOT=new THREE.Color(screenHex(255,246,214)),F_MID=new THREE.Color(screenHex(255,140,38)),F_OLD=new THREE.Color(screenHex(152,30,8)),grp=new THREE.Group;grp.frustumCulled=!1,parent.add(grp);let seed=((x*73856093|0)^(z*19349663|0)^490698673)>>>0;const rnd=function(){return seed=seed*1664525+1013904223>>>0,seed/4294967296},mats=[],clots=[];function launch(P){const a=rnd()*6.2832,rr=Math.sqrt(rnd())*mouth;P.x=x+Math.cos(a)*rr,P.y=baseY,P.z=z+Math.sin(a)*rr;const tilt=CONE*(.2+.8*rnd()),az=a+(rnd()-.5)*1.5,sp=V0*(.48+.64*rnd()),st=Math.sin(tilt);P.vx=Math.cos(az)*st*sp,P.vy=Math.cos(tilt)*sp,P.vz=Math.sin(az)*st*sp,P.age=0,P.gy=null,P.sz=.5+1.05*(.35*rnd()+.65*clamp((sp/V0-.48)/.64,0,1)),P.tf=Math.max(.35,2*P.vy/G)}for(let i=0;i<N;i++){const mat=new THREE.SpriteMaterial({map:glowTex(),color:16777215,transparent:!0,opacity:0,depthWrite:!1,depthTest:!0,fog:!1,blending:THREE.AdditiveBlending});mats.push(mat);const m=new THREE.Sprite(mat);m.renderOrder=8,m.visible=!1,grp.add(m);const P={m,mat,x:0,y:0,z:0,vx:0,vy:0,vz:0,age:0,tf:1,sz:1,gy:null,wait:i/N*1.2};launch(P),clots.push(P)}let dead=!1,active=1;const handle={kind:"fountain",group:grp,clotCount:N,setActive(k){return active=clamp(k==null?1:+k,0,1),handle},update(dt){if(dead||!(dt>0))return handle;dt>.2&&(dt=.2);const live=Math.max(1,Math.round(N*(.25+.75*active)));for(let i=0;i<clots.length;i++){const P=clots[i];if(i>=live){P.m.visible=!1;continue}if(P.wait>0){P.wait-=dt,P.m.visible=!1;continue}if(P.age+=dt,P.vy-=G*dt,P.x+=P.vx*dt,P.y+=P.vy*dt,P.z+=P.vz*dt,P.vy<0&&P.y<baseY){if(P.gy==null)try{P.gy=+groundAt(P.x,P.z)||0}catch{P.gy=baseY-30}if(P.y<=P.gy){launch(P),P.m.visible=!1;continue}}const k=clamp(P.age/P.tf,0,1),col=P.mat.color;k<.42?col.copy(F_HOT).lerp(F_MID,k/.42):col.copy(F_MID).lerp(F_OLD,(k-.42)/.58),P.mat.opacity=.82*(1-.6*k)*clamp(P.age/.06,0,1);const s=P.sz*(1+.3*k),stretch=1+.55*clamp(Math.abs(P.vy)/V0,0,1);P.m.position.set(P.x,P.y,P.z),P.m.scale.set(s/Math.sqrt(stretch),s*stretch,1),P.m.visible=!0}return handle},dispose(){if(dead)return;dead=!0;for(let i=0;i<clots.length;i++)grp.remove(clots[i].m);parent.remove(grp);for(let i=0;i<mats.length;i++)mats[i].dispose();const k=LIVE.fountain.indexOf(handle);k>=0&&LIVE.fountain.splice(k,1)}};return census.fountain++,LIVE.fountain.push(handle),handle},V.ventGlow=function(o){o=o||{};const parent=o.parent||CBZ.scene,groundAt=o.groundAt||flatGround,x=+o.x||0,z=+o.z||0,R=o.r>0?+o.r:7,salt=o.salt!=null?o.salt|0:4444,SEC=20,RINGS=[.34,.62,1],VC=1+SEC*RINGS.length,pos=new Float32Array(VC*3),col=new Float32Array(VC*3),CORE=16774344,RING_C=[16766583,15761181,6690826];_c1.setHex(CORE),pos[0]=x,pos[1]=groundAt(x,z)+.24,pos[2]=z,col[0]=_c1.r,col[1]=_c1.g,col[2]=_c1.b;for(let r=0;r<RINGS.length;r++){_c1.setHex(RING_C[r]);for(let s=0;s<SEC;s++){const a=s/SEC*Math.PI*2,wob=.68+.64*h01(x+Math.cos(a)*9,z+Math.sin(a)*9,salt+r*17),rr=R*RINGS[r]*(r===RINGS.length-1?wob:.86+.28*(wob-.68)),px=x+Math.cos(a)*rr,pz=z+Math.sin(a)*rr,off=(1+r*SEC+s)*3;pos[off]=px,pos[off+1]=groundAt(px,pz)+.2-r*.04,pos[off+2]=pz;const g2=.9+.2*h01(px,pz,salt+5);col[off]=_c1.r*g2,col[off+1]=_c1.g*g2,col[off+2]=_c1.b*g2}}const idx=new Uint16Array((SEC+(RINGS.length-1)*SEC*2)*3);let ii=0;for(let s=0;s<SEC;s++)idx[ii++]=0,idx[ii++]=1+s,idx[ii++]=1+(s+1)%SEC;for(let r=0;r<RINGS.length-1;r++)for(let s=0;s<SEC;s++){const a=1+r*SEC+s,b=1+r*SEC+(s+1)%SEC;idx[ii++]=a,idx[ii++]=a+SEC,idx[ii++]=b,idx[ii++]=b,idx[ii++]=a+SEC,idx[ii++]=b+SEC}const geo=new THREE.BufferGeometry;geo.setAttribute("position",new THREE.BufferAttribute(pos,3)),geo.setAttribute("color",new THREE.BufferAttribute(col,3)),geo.setIndex(new THREE.BufferAttribute(idx,1));const mat=new THREE.MeshBasicMaterial({vertexColors:!0,side:THREE.DoubleSide,fog:!0,polygonOffset:!0,polygonOffsetFactor:-6,polygonOffsetUnits:-6}),mesh=new THREE.Mesh(geo,mat);mesh.frustumCulled=!1,mesh.renderOrder=3,parent.add(mesh);let light=null;o.light!==!1&&(light=new THREE.PointLight(16747050,1.8,Math.max(30,R*6),2),light.castShadow=!1,light.position.set(x,groundAt(x,z)+3,z),parent.add(light),census.lights++);let t=0,dead=!1;const handle={kind:"vent",mesh,update(dt){if(dead)return handle;t+=dt;const fl=.94+.08*Math.sin(t*6.2)+Math.random()*.04;return mat.color.setScalar(clamp(fl,.8,1.12)),light&&(light.intensity=1.6+.5*Math.sin(t*5.1)+Math.random()*.2),handle},hitTest(px,pz){return Math.hypot(px-x,pz-z)<R*.5},dispose(){if(dead)return;dead=!0,parent.remove(mesh),geo.dispose(),mat.dispose(),light&&(parent.remove(light),census.lights--);const k=LIVE.vent.indexOf(handle);k>=0&&LIVE.vent.splice(k,1)}};return census.vent++,LIVE.vent.push(handle),handle};let _PYRO_ASH=null;function pyroAsh(){return _PYRO_ASH||(_PYRO_ASH=[screenHex(30,26,23),screenHex(36,32,29),screenHex(62,56,50),screenHex(72,65,58),screenHex(104,96,86),screenHex(122,113,101)],_PYRO_ASH)}const PYRO_BODY=6,_puffTex=[];function puffTex(k){if(_puffTex[k])return _puffTex[k];const S=128,cv=document.createElement("canvas");cv.width=cv.height=S;const g=cv.getContext("2d");let seed=795749149+k*2654435769>>>0;const rnd=function(){return seed=seed*1664525+1013904223>>>0,seed/4294967296},lobe=function(lx,ly,r,a0){const rg=g.createRadialGradient(lx-r*.34,ly-r*.38,r*.05,lx,ly,r);rg.addColorStop(0,"rgba(255,255,255,"+a0+")"),rg.addColorStop(.42,"rgba(196,196,196,"+(a0*.92).toFixed(3)+")"),rg.addColorStop(.78,"rgba(112,112,112,"+(a0*.62).toFixed(3)+")"),rg.addColorStop(1,"rgba(80,80,80,0)"),g.fillStyle=rg,g.beginPath(),g.arc(lx,ly,r,0,6.2832),g.fill()};lobe(S*.5,S*.54,S*.36,.95);const n=8+k%3;for(let i=0;i<n;i++){const a=i/n*6.2832+rnd()*.9,d2=S*(.17+rnd()*.12);lobe(S*.5+Math.cos(a)*d2,S*.54+Math.sin(a)*d2,S*(.15+rnd()*.12),.72+rnd()*.23)}for(let i=0;i<11;i++){const a=rnd()*6.2832,d2=S*(.27+rnd()*.13);lobe(S*.5+Math.cos(a)*d2,S*.54+Math.sin(a)*d2,S*(.06+rnd()*.08),.6+rnd()*.25)}const img=g.getImageData(0,0,S,S),d=img.data;for(let y=0;y<S;y++){const ramp=1-.5*Math.pow(y/(S-1),1.4);for(let x=0;x<S;x++){const o=(y*S+x)*4;d[o]*=ramp,d[o+1]*=ramp,d[o+2]*=ramp}}return g.putImageData(img,0,0),_puffTex[k]=new THREE.CanvasTexture(cv),_puffTex[k]}let _pyroMats=null;function pyroMats(){if(_pyroMats)return _pyroMats;_pyroMats=[];for(let i=0;i<PYRO_BODY;i++)_pyroMats.push(new THREE.SpriteMaterial({map:puffTex(i%3),color:pyroAsh()[i],transparent:!0,opacity:1,depthWrite:!1,fog:!0,blending:THREE.NormalBlending,rotation:0}));for(let i=0;i<2;i++)_pyroMats.push(new THREE.SpriteMaterial({map:puffTex(i),color:i?10369030:7151362,transparent:!0,opacity:.5,depthWrite:!1,fog:!0,blending:THREE.AdditiveBlending,rotation:i*2.1}));return _pyroMats}V.pyroclastic=function(o){o=o||{};const parent=o.parent||CBZ.scene,groundAt=o.groundAt||flatGround,width=o.width>0?+o.width:26,halfW=width*.5,height=o.height>0?+o.height:20,speed=o.speed>0?+o.speed:44,len=o.len>0?+o.len:150,tail=o.tail>0?+o.tail:62,salt=o.salt!=null?o.salt|0:8123,path=fallLine({x:o.x,z:o.z,groundAt,bearing:o.bearing,step:6,count:Math.ceil(len/6)+1,salt,turn:.4,wander:.1}),mats=pyroMats(),N=qi(58,136),grp=new THREE.Group;grp.frustumCulled=!1,parent.add(grp);const blobs=[];for(let i=0;i<N;i++){const lag=Math.pow(Math.random(),1.7)*tail,basal=i%8===0&&lag<tail*.3,hf=basal?.04+Math.random()*.11:Math.random(),tier=Math.min(2,Math.floor(Math.pow(hf,.85)*3)),m=new THREE.Sprite(basal?mats[PYRO_BODY+i%2]:mats[tier*2+(i&1)]);m.renderOrder=7,grp.add(m),blobs.push({m,lag,basal,hf,lat:(Math.random()*2-1)*1.1,ph:Math.random()*6.28,sz:(basal?.17:.15)+Math.pow(Math.random(),1.4)*(basal?.17:.42)})}let front=0,t=0,dead=!1;const _v=new THREE.Vector3,handle={kind:"pyro",group:grp,path,get frontS(){return front},get done(){return front>path.total+tail*.4},frontPos(){return pathAt(path,Math.min(front,path.total),_v),{x:_v.x,y:_v.y,z:_v.z}},update(dt){if(dead)return handle;t+=dt,front+=speed*dt;for(let i=0;i<mats.length;i++)mats[i].rotation=.32*Math.sin(t*(.35+i*.09)+i*1.7);for(let i=0;i<blobs.length;i++){const B=blobs[i],s=front-B.lag;if(s<-4||s>path.total+8){B.m.visible=!1;continue}B.m.visible=!0,pathAt(path,clamp(s,0,path.total),_v);const fi=clamp(Math.floor(s/path.seg),0,path.pts.length-2),a=path.pts[fi],b2=path.pts[fi+1];let nx=-(b2.z-a.z),nz=b2.x-a.x;const nl=Math.hypot(nx,nz)||1;nx/=nl,nz/=nl;const ageK=clamp(B.lag/Math.max(1,tail),0,1),spread=halfW*(.55+.75*ageK),churn=1+.3*Math.sin(t*1.9+B.ph),gy=groundAt(_v.x,_v.z),sc=width*B.sz*(.62+.7*ageK)*(.92+.13*Math.sin(t*1.6+B.ph));B.m.position.set(_v.x+nx*B.lat*spread+Math.sin(t*1.1+B.ph)*2.4,gy+height*B.hf*(.22+.95*ageK)*churn+sc*.42,_v.z+nz*B.lat*spread+Math.cos(t*1.3+B.ph)*2.4),B.m.scale.set(sc,sc*(B.basal?.72:.92),1)}return handle},contains(x,z){const c=pathCoord(path,x,z,Math.min(front+6,path.total));if(c.s>front+1||c.s<front-tail*.9)return 0;const ageK=clamp((front-c.s)/Math.max(1,tail),0,1);return c.perp>=halfW*(.55+.55*ageK)?0:ageK<.42?1:2},threatAt(x,z){const c=pathCoord(path,x,z,path.total),lane=c.perp<halfW*1.35?1-c.perp/(halfW*1.35):0;if(!lane)return 0;const ahead=c.s-front;return ahead<-tail?0:clamp(lane*(ahead<0?1:1-ahead/90),0,1)},dispose(){if(dead)return;dead=!0;for(let i=0;i<blobs.length;i++)grp.remove(blobs[i].m);parent.remove(grp);const k=LIVE.pyro.indexOf(handle);k>=0&&LIVE.pyro.splice(k,1)}};return census.pyro++,LIVE.pyro.push(handle),handle};const LAHAR_COLS=7,LAHAR_U=[-1,-.68,-.32,0,.32,.68,1];V.lahar=function(o){o=o||{};const parent=o.parent||CBZ.scene,groundAt=o.groundAt||flatGround,width=o.width>0?+o.width:12,halfW=width*.5,len=o.len>0?+o.len:110,speed=o.speed>0?+o.speed:11,salt=o.salt!=null?o.salt|0:2609,seg=Math.max(3.5,width*.7),path=fallLine({x:o.x,z:o.z,groundAt,bearing:o.bearing,step:seg,count:Math.ceil(len/seg)+1,salt,channel:!0,wander:.1}),N=path.pts.length,wProf=new Float32Array(N);for(let i=0;i<N;i++){const p=path.pts[i];wProf[i]=.62+.55*(i/(N-1))+.3*h01(p.x,p.z,salt+3)}const pos=new Float32Array(N*LAHAR_COLS*3),col=new Float32Array(N*LAHAR_COLS*3),idx=new Uint16Array(Math.max(1,(N-1)*(LAHAR_COLS-1)*6));let ii=0;for(let i=0;i<N-1;i++)for(let c=0;c<LAHAR_COLS-1;c++){const a=i*LAHAR_COLS+c,b2=a+1,d=a+LAHAR_COLS,e=d+1;idx[ii++]=a,idx[ii++]=d,idx[ii++]=b2,idx[ii++]=b2,idx[ii++]=d,idx[ii++]=e}const geo=new THREE.BufferGeometry;geo.setAttribute("position",new THREE.BufferAttribute(pos,3)),geo.setAttribute("color",new THREE.BufferAttribute(col,3)),geo.setIndex(new THREE.BufferAttribute(idx,1));const mat=new THREE.MeshPhongMaterial({vertexColors:!0,flatShading:!0,shininess:0,specular:0,side:THREE.DoubleSide,emissive:1316378,emissiveIntensity:1,polygonOffset:!0,polygonOffsetFactor:-3,polygonOffsetUnits:-3}),mesh=new THREE.Mesh(geo,mat);mesh.frustumCulled=!1,mesh.receiveShadow=!0,mesh.renderOrder=2,parent.add(mesh);const rideN=o.debris===!1?0:qi(3,11),bGeo=new THREE.DodecahedronGeometry(1,0),lGeo=new THREE.CylinderGeometry(.34,.4,4.4,6),bMat=new THREE.MeshPhongMaterial({color:5787719,flatShading:!0,shininess:0,specular:0}),lMat=new THREE.MeshPhongMaterial({color:4864552,flatShading:!0,shininess:0,specular:0}),riders=[];for(let i=0;i<rideN;i++){const log=i%3===2,m=new THREE.Mesh(log?lGeo:bGeo,log?lMat:bMat),sc=log?1:.7+Math.random()*1.5;m.scale.setScalar(sc),m.visible=!1,parent.add(m),riders.push({m,s:-Math.random()*len*.5,lat:(Math.random()*2-1)*.7,ph:Math.random()*6.28,spin:(Math.random()-.5)*1.6,log,v:speed*(.75+Math.random()*.4)})}let adv=seg,t=0,dead=!1,hardT=-1;const _v=new THREE.Vector3;function writeStations(to){const setK=hardT>=0?clamp(hardT/4,0,1):0;for(let i=0;i<=to&&i<N;i++){const p=path.pts[i],hw=halfW*wProf[i],a=path.pts[Math.max(0,i-1)],b2=path.pts[Math.min(N-1,i+1)];let nx=-(b2.z-a.z),nz=b2.x-a.x;const nl=Math.hypot(nx,nz)||1;nx/=nl,nz/=nl;const heave=setK>=1?0:Math.sin(t*2.1-i*.8)*.32*(1-setK);for(let c=0;c<LAHAR_COLS;c++){const u=LAHAR_U[c],off=(i*LAHAR_COLS+c)*3,au=Math.abs(u),x=p.x+nx*u*hw,z=p.z+nz*u*hw;pos[off]=x,pos[off+2]=z;const lumps=(h01(x,z,salt+9)-.5)*.9;pos[off+1]=p.y+.08+(.75+.7*(1-au*au))+heave*(1-au)+lumps;const grain=.85+.3*h01(x,z,salt+17);ramp3(clamp(.25+.5*(1-au)+setK*.8,0,1),4537653,6642767,9866625,_c3),col[off]=_c3.r*grain,col[off+1]=_c3.g*grain,col[off+2]=_c3.b*grain}}}writeStations(1),geo.computeVertexNormals();const handle={kind:"lahar",mesh,path,get length(){return adv},get hardened(){return hardT>=4},update(dt){if(dead)return handle;t+=dt,hardT>=0?hardT+=dt:adv=Math.min(len,adv+speed*dt);const st=Math.min(N-1,Math.floor(adv/seg)+1);writeStations(st),geo.attributes.position.needsUpdate=!0,geo.attributes.color.needsUpdate=!0,geo.setDrawRange(0,Math.max(0,st)*(LAHAR_COLS-1)*6);for(let i=0;i<riders.length;i++){const R=riders[i];if(hardT<0&&(R.s+=R.v*dt,R.s>adv-seg&&(R.s=Math.max(0,R.s-adv*.75))),R.s<.5||R.s>adv){R.m.visible=!1;continue}pathAt(path,R.s,_v);const fi=clamp(Math.floor(R.s/seg),0,N-2),a=path.pts[fi],b2=path.pts[fi+1];let nx=-(b2.z-a.z),nz=b2.x-a.x;const nl=Math.hypot(nx,nz)||1;nx/=nl,nz/=nl,R.m.visible=!0,R.m.position.set(_v.x+nx*R.lat*halfW*.72,_v.y+.75+(hardT>=0?0:.22*Math.sin(t*2.6+R.ph)),_v.z+nz*R.lat*halfW*.72),hardT<0&&(R.m.rotation.x+=R.spin*dt,R.m.rotation.z+=R.spin*.7*dt)}return handle},hitTest(x,z){if(hardT>=0)return null;const c=pathCoord(path,x,z,adv);if(c.s<0||c.s>adv)return null;const i=clamp(Math.round(c.s/seg),0,N-1),hw=halfW*wProf[i];if(c.perp>hw)return null;const a=path.pts[Math.max(0,i-1)],b2=path.pts[Math.min(N-1,i+1)];let dx=b2.x-a.x,dz=b2.z-a.z;const dl=Math.hypot(dx,dz)||1;return{depth:(1-c.perp/hw)*1.5,dirx:dx/dl,dirz:dz/dl,s:c.s}},harden(){return hardT<0&&(hardT=0),handle},dispose(){if(dead)return;dead=!0,parent.remove(mesh),geo.dispose(),mat.dispose();for(let i=0;i<riders.length;i++)parent.remove(riders[i].m);bGeo.dispose(),lGeo.dispose(),bMat.dispose(),lMat.dispose();const k=LIVE.lahar.indexOf(handle);k>=0&&LIVE.lahar.splice(k,1)}};return census.lahar++,LIVE.lahar.push(handle),handle},V.ashLoad=function(o){o=o||{};const parent=o.parent||CBZ.scene,groundAt=o.groundAt||flatGround,cx=+o.cx||0,cz=+o.cz||0,R=o.r>0?+o.r:120,NC=Math.max(6,Math.min(96,Math.round(o.cells||qi(40,76)))),cell=R*2/NC,salt=o.salt!=null?o.salt|0:6151,FULL=o.full>0?+o.full:.05,DRIFT_ON=.9,DRIFT_SPAN=1.4;function vnoise(x,z,wl,s2){const gx=Math.floor(x/wl),gz=Math.floor(z/wl);let fx=x/wl-gx,fz=z/wl-gz;fx=fx*fx*(3-2*fx),fz=fz*fz*(3-2*fz);const a=h01(gx,gz,s2),b=h01(gx+1,gz,s2),c=h01(gx,gz+1,s2),d2=h01(gx+1,gz+1,s2);return a+(b-a)*fx+(c-a)*fz+(a-b-c+d2)*fx*fz}const cells=[],grid=new Int32Array(NC*NC).fill(-1);for(let i=0;i<NC;i++)for(let j=0;j<NC;j++){const x=cx-R+(i+.5)*cell,z=cz-R+(j+.5)*cell;if(Math.hypot(x-cx,z-cz)>R*1.02)continue;grid[i*NC+j]=cells.length;const jx=x+(h01(x,z,salt+61)-.5)*cell*.9,jz=z+(h01(x,z,salt+67)-.5)*cell*.9,ang=h01(x,z,salt+71)*Math.PI*.5,jit=.58+.92*h01(x,z,salt+89),n1=vnoise(jx,jz,cell*4.8,salt+103),n2=vnoise(jx,jz,cell*1.9,salt+131),C={x:jx,z:jz,y:0,w:cell,d:cell,depth:0,roof:!1,ang,jit,probed:!1,cy:null,peakD:0,shed:1,gain:.42+1.15*(.62*n1+.38*n2)};cells.push(C)}const groundCells=cells.length,MAXC=groundCells+Math.max(0,o.roofs||64),pos=new Float32Array(MAXC*4*3),col=new Float32Array(MAXC*4*3),idx=MAXC*4>65535?new Uint32Array(MAXC*6):new Uint16Array(MAXC*6);for(let i=0;i<MAXC;i++){const v=i*4,k=i*6;idx[k]=v,idx[k+1]=v+2,idx[k+2]=v+1,idx[k+3]=v,idx[k+4]=v+3,idx[k+5]=v+2}const nrm=new Float32Array(MAXC*4*3);for(let i=1;i<nrm.length;i+=3)nrm[i]=1;const uvs=new Float32Array(MAXC*4*2);for(let i=0;i<MAXC;i++){const u=i*8;uvs[u]=0,uvs[u+1]=0,uvs[u+2]=1,uvs[u+3]=0,uvs[u+4]=1,uvs[u+5]=1,uvs[u+6]=0,uvs[u+7]=1}const geo=new THREE.BufferGeometry;geo.setAttribute("position",new THREE.BufferAttribute(pos,3)),geo.setAttribute("color",new THREE.BufferAttribute(col,3)),geo.setAttribute("normal",new THREE.BufferAttribute(nrm,3)),geo.setAttribute("uv",new THREE.BufferAttribute(uvs,2)),geo.setIndex(new THREE.BufferAttribute(idx,1));const mat=new THREE.MeshLambertMaterial({vertexColors:!0,side:THREE.DoubleSide,map:ashTex(),alphaTest:.45,transparent:!1,emissive:592654,emissiveIntensity:1,polygonOffset:!0,polygonOffsetFactor:-4,polygonOffsetUnits:-4}),mesh=new THREE.Mesh(geo,mat);mesh.frustumCulled=!1,mesh.receiveShadow=!1,mesh.renderOrder=1,parent.add(mesh);let dirty=!0,wT=0,peak=0,dead=!1,windAng=0,lastWedge=null,probeCursor=0;function probeCell(n){const C=cells[n];if(C.roof||C.probed)return;C.probed=!0,C.y=groundAt(C.x,C.z);const hw0=C.w*.78*C.jit,ca=Math.cos(C.ang),sa=Math.sin(C.ang),ox=[-hw0,hw0,hw0,-hw0],oz=[-hw0,-hw0,hw0,hw0];C.cy=new Float32Array(4);for(let k=0;k<4;k++)C.cy[k]=groundAt(C.x+ox[k]*ca-oz[k]*sa,C.z+ox[k]*sa+oz[k]*ca);let lo=C.cy[0],hi=C.cy[0];for(let k=1;k<4;k++){const v=C.cy[k];v<lo&&(lo=v),v>hi&&(hi=v)}const slope=(hi-lo)/Math.max(.5,hw0*2);C.shed=clamp(1-(slope-.18)*1.3,.3,1)}function writeCell(n){const C=cells[n];if(!C.roof&&!C.probed){const v0=n*4*3;for(let k=0;k<4;k++)pos[v0+k*3]=C.x,pos[v0+k*3+1]=C.y,pos[v0+k*3+2]=C.z;return}const raw=C.depth/FULL*(C.roof?1:(C.gain||1)*(C.shed||1)),cov=C.roof?clamp(raw,0,1):clamp((raw-DRIFT_ON)/DRIFT_SPAN,0,1);if(cov<.02){const v0=n*4*3;for(let k=0;k<4;k++)pos[v0+k*3]=C.x,pos[v0+k*3+1]=C.y,pos[v0+k*3+2]=C.z;return}const jit=C.roof?1:C.jit||1,grow=C.roof?.28+.72*cov:.62+.42*cov,wornK=!C.roof&&C.peakD>.012?clamp(1-C.depth/C.peakD,0,1):0;let angW=C.roof?0:C.ang||0;if(wornK>.01){let dA=windAng-angW;dA-=Math.PI*Math.round(dA/Math.PI),angW+=dA*wornK}const hw=C.w*1.05*grow*jit*(1+.75*wornK),hd=C.d*1.05*grow*jit*(1-.5*wornK),lift=(C.roof?.03:.02)+Math.min(.3,C.depth)*.15,v=n*4*3,ca=C.roof?1:Math.cos(angW),sa=C.roof?0:Math.sin(angW),qx=[-hw,hw,hw,-hw],qz=[-hd,-hd,hd,hd];for(let k=0;k<4;k++)pos[v+k*3]=C.x+qx[k]*ca-qz[k]*sa,pos[v+k*3+1]=(C.cy?C.y+(C.cy[k]-C.y)*cov:C.y)+lift,pos[v+k*3+2]=C.z+qx[k]*sa+qz[k]*ca;const deep=clamp(C.depth/.35,0,1),grain=.86+.2*h01(C.x,C.z,salt);C.roof?(_c3.setHex(1645341).lerp(_c1.setHex(3356218),Math.min(1,cov*1.15)),_c3.lerp(_c1.setHex(2171685),deep*.7)):(_c3.setHex(3356218).lerp(_c1.setHex(5791078),cov),_c3.lerp(_c1.setHex(4014409),deep*.5));for(let k=0;k<4;k++)col[v+k*3]=_c3.r*grain,col[v+k*3+1]=_c3.g*grain,col[v+k*3+2]=_c3.b*grain}for(let n=0;n<cells.length;n++)writeCell(n);geo.setDrawRange(0,cells.length*6);const handle={kind:"ash",mesh,cell,get wedge(){return lastWedge},get peakDepth(){return peak},get cellCount(){return cells.length},update(dt,spec){if(dead)return handle;spec=spec||{},wT+=dt;const rate=+spec.rate||0;if(wT<.125&&!(rate>0&&peak===0))return handle;const step=wT;if(wT=0,probeCursor<groundCells){const lim=Math.min(groundCells,probeCursor+700);for(;probeCursor<lim;probeCursor++)probeCell(probeCursor);dirty=!0}if(rate>0){const wx=spec.windX!=null?+spec.windX:1,wz=spec.windZ!=null?+spec.windZ:0,wl=Math.hypot(wx,wz)||1,ux=wx/wl,uz=wz/wl;windAng=Math.atan2(uz,ux);const sx=spec.srcX!=null?+spec.srcX:cx,sz=spec.srcZ!=null?+spec.srcZ:cz,spread=spec.spread!=null?clamp(+spec.spread,0,1):.18,lobeP=spec.lobe>0?+spec.lobe:2.2,reach=spec.reach>0?+spec.reach:R*.55;lastWedge=lastWedge||{},lastWedge.srcX=sx,lastWedge.srcZ=sz,lastWedge.ux=ux,lastWedge.uz=uz,lastWedge.reach=reach,lastWedge.lobe=lobeP,lastWedge.spread=spread;for(let n=0;n<cells.length;n++){const C=cells[n],dx=C.x-sx,dz=C.z-sz,d=Math.hypot(dx,dz)||.001,dot=(dx*ux+dz*uz)/d,lobe=dot>0?Math.pow(dot,lobeP):0,fall=1/(1+d/reach*(d/reach)),k=(spread+(1-spread)*lobe)*fall;k>.001&&(C.depth+=rate*k*step,C.depth>C.peakD&&(C.peakD=C.depth),C.depth>peak&&(peak=C.depth))}dirty=!0}else{if(spec.windX!=null||spec.windZ!=null){const wxE=+spec.windX||0,wzE=+spec.windZ||0;Math.hypot(wxE,wzE)>.001&&(windAng=Math.atan2(wzE,wxE))}const wear=1+4*(spec.rain>0?Math.min(1,+spec.rain):0);let m=0,any=!1;for(let n=0;n<cells.length;n++){const C=cells[n];if(C.depth<=0)continue;const expo=C.roof?1.4:2-(C.shed||1);C.depth-=(.0016+C.depth*.028)*wear*expo*step,C.depth<4e-4?C.depth=0:C.depth>m&&(m=C.depth),any=!0}peak=m,any&&(dirty=!0)}if(dirty){dirty=!1;for(let n=0;n<cells.length;n++)writeCell(n);geo.attributes.position.needsUpdate=!0,geo.attributes.color.needsUpdate=!0,geo.setDrawRange(0,cells.length*6)}return handle},depthAt(x,z){const i=Math.floor((x-(cx-R))/cell),j=Math.floor((z-(cz-R))/cell);if(i<0||j<0||i>=NC||j>=NC)return 0;const n=grid[i*NC+j];if(n<0||n>=groundCells)return 0;const C=cells[n];return C.probed?C.depth:0},addRoof(r){if(!r||cells.length>=MAXC)return-1;const id=cells.length;return cells.push({x:+r.x||0,z:+r.z||0,y:+r.y||0,w:Math.max(1,+r.w||4),d:Math.max(1,+r.d||4),depth:0,roof:!0,ref:r.ref||null}),writeCell(id),dirty=!0,id},roofDepth(id){const C=cells[id];return C?C.depth:0},roofRef(id){const C=cells[id];return C?C.ref:null},clearCell(id){const C=cells[id];C&&(C.depth=0,dirty=!0)},dispose(){if(dead)return;dead=!0,parent.remove(mesh),geo.dispose(),mat.dispose();const k=LIVE.ash.indexOf(handle);k>=0&&LIVE.ash.splice(k,1)}};return census.ash++,LIVE.ash.push(handle),handle},CBZ.volcanoAudit=function(){let lavaTransparent=0,lavaMeshes=0,lavaTris=0,lavaScars=0;const lavaTips=[],lavaMids=[],lavaScarTips=[],lavaScarMids=[];for(let i=0;i<LIVE.lava.length;i++){const f=LIVE.lava[i];if(f.quenched){lavaScars++;try{lavaScarTips.push(f.tip),lavaScarMids.push(f.mid)}catch{}}else try{lavaTips.push(f.tip),lavaMids.push(f.mid)}catch{}const ms=[f.mesh,f.channel];for(let k=0;k<ms.length;k++){const m=ms[k];if(!m||!m.material)continue;lavaMeshes++,(m.material.transparent||m.material.blending===THREE.AdditiveBlending)&&lavaTransparent++;const g=m.geometry;g&&g.drawRange&&(lavaTris+=Math.max(0,g.drawRange.count)/3)}}let pyroBlobs=0;for(let i=0;i<LIVE.pyro.length;i++){const g=LIVE.pyro[i].group;g&&(pyroBlobs+=g.children.length)}let columnPuffs=0;for(let i=0;i<LIVE.column.length;i++){const c=LIVE.column[i];columnPuffs+=c.puffCount||(c.group?c.group.children.length:0)}let fountainClots=0;for(let i=0;i<LIVE.fountain.length;i++)fountainClots+=LIVE.fountain[i].clotCount||0;let ashPeak=0,ashCells=0;for(let i=0;i<LIVE.ash.length;i++)ashPeak=Math.max(ashPeak,LIVE.ash[i].peakDepth),ashCells+=LIVE.ash[i].cellCount;return{v2:CBZ.CONFIG.VOLCANO_V2!==!1,lavaFlows:LIVE.lava.length-lavaScars,lavaScars,lavaMeshes,lavaTransparent,lavaOpaque:lavaTransparent===0,lavaTris:Math.round(lavaTris),lavaTips,lavaMids,lavaScarTips,lavaScarMids,lavaBranches:census.branches,ventGlows:LIVE.vent.length,ashColumns:LIVE.column.length,columnPuffs,fountains:LIVE.fountain.length,fountainClots,builtFountain:census.fountain,pyroLive:LIVE.pyro.length,pyroBlobs,laharLive:LIVE.lahar.length,ashFields:LIVE.ash.length,ashCells,ashPeakDepth:+ashPeak.toFixed(3),lights:census.lights,builtLava:census.lava,builtPyro:census.pyro,builtLahar:census.lahar,builtAsh:census.ash,builtVent:census.vent}},CBZ.volcanoFx=V})();
