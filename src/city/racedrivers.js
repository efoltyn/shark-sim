(function(){"use strict";const CBZ=window.CBZ;if(!CBZ)return;const g=CBZ.game;CBZ.CONFIG=CBZ.CONFIG||{},CBZ.CONFIG.RACE_REAL_DRIVERS==null&&(CBZ.CONFIG.RACE_REAL_DRIVERS=!0);const BODY={coupe:{accel:42,top:44,turn:3,grip:9.4,brake:38},muscle:{accel:40,top:41,turn:2.45,grip:6.6,brake:30},sedan:{accel:32,top:35,turn:2.6,grip:7.4,brake:32},suv:{accel:26,top:31,turn:2.1,grip:5.6,brake:27},pickup:{accel:27,top:32,turn:2,grip:5.2,brake:26},van:{accel:23,top:29,turn:1.85,grip:4.8,brake:24},hatch:{accel:29,top:31,turn:2.85,grip:7.2,brake:31}};function perf(car){const bk=car._bk||(CBZ.cityVehicleBodyKind&&car.model?CBZ.cityVehicleBodyKind(car.model):"coupe"),b=BODY[bk]||BODY.sedan,rarity=car.model?Math.max(0,Math.min(1,car.model.rarity||0)):.35;let accel=b.accel*(.9+rarity*.22),top=b.top*(.88+rarity*.28),grip=b.grip,brake=b.brake,turn=b.turn;const d=1-Math.max(0,Math.min(100,car.engineHp==null?100:car.engineHp))/100;return accel*=1-d*.55,top*=1-d*.42,grip*=1-d*.5,turn*=1-d*.28,{accel,top,grip,brake,turn,dmg:d}}function angDiff(a){for(;a>Math.PI;)a-=Math.PI*2;for(;a<-Math.PI;)a+=Math.PI*2;return a}const clamp=(v,a,b)=>Math.max(a,Math.min(b,v)),COURSES=Object.create(null),COURSE_ADOPT=Object.create(null),COURSE_REQUIRED=["speedway-weekend","apex-night","street-race"];function courseNumber(c,key,fallback){if(!c)return fallback;const v=c[key];if(typeof v=="function"&&(key==="lineLen"||key==="trackLen"))try{const n2=Number(v());return Number.isFinite(n2)?n2:fallback}catch{return fallback}const n=Number(v);return Number.isFinite(n)?n:fallback}function normalizeCourse(id,spec){if(!spec)return null;const c=Object.assign({},spec);return c.id=id||c.id||"course",c.mode=c.mode||(c.path?"path":"line"),c.mode==="line"&&typeof c.line!="function"||c.mode==="path"&&(!Array.isArray(c.path)||c.path.length<2)?null:c}function registerCourse(id,spec){if(!id)return null;const c=normalizeCourse(id,spec);return c?(COURSES[id]=c,c):null}function course(id){return id&&typeof id=="object"?normalizeCourse(id.id,id):id?(!COURSES[id]&&id==="speedway"&&CBZ.speedwayCourse&&registerCourse(id,CBZ.speedwayCourse),COURSES[id]||null):null}function pathLength(path){let n=0;for(let i=1;i<(path||[]).length;i++)n+=Math.hypot(path[i].x-path[i-1].x,path[i].z-path[i-1].z);return Math.max(1,n)}function pathCourse(id,checkpoints,path,opts){opts=opts||{};const c=normalizeCourse(id,{mode:"path",path:(path||[]).map(function(p){return{x:p.x,z:p.z,cp:p.cp==null?null:p.cp}}),checkpoints:(checkpoints||[]).map(function(p){return{x:p.x,z:p.z}}),cpTotal:(checkpoints||[]).length,lineLen:Math.max(120,pathLength(path))});return c?(opts.register!==!1&&id&&(COURSES[id]=c),c):null}function adoptCourse(id){id&&(COURSE_ADOPT[id]=!0,(CBZ._raceCourseConsumers||(CBZ._raceCourseConsumers=Object.create(null)))[id]=!0)}function raceToolAudit(){const declared=CBZ._raceCourseConsumers||{},missing=COURSE_REQUIRED.filter(function(id){return!COURSE_ADOPT[id]&&!declared[id]});return{courses:Object.keys(COURSES).length,adopted:COURSE_REQUIRED.length-missing.length,required:COURSE_REQUIRED.length,legacy:missing.length,missing}}const D={list:[]};function modelForStyle(style,color){const CARS=CBZ.cityEcon&&CBZ.cityEcon.CARS||[];let m=null;for(const c of CARS)if(c.detailStyle===style){m=c;break}return m||(m=CARS.length?CARS[CARS.length-1]:{name:"Race Car",value:6e4,body:"coupe",rarity:.9,color:12597547}),Object.assign({},m,color!=null?{color}:null)}function spawn(opts){if(!CBZ.cityMakeCar||!CBZ.city||!CBZ.city.arena)return null;const C=course(opts.course);C&&(opts=Object.assign({},opts,{mode:C.mode,line:C.line||null,lineLen:courseNumber(C,"lineLen",700),trackHalf:courseNumber(C,"trackHalf",7),path:C.path||null,cpTotal:courseNumber(C,"cpTotal",0)}));const model=opts.model||modelForStyle(opts.style||"muscle",opts.color);let car=null;try{car=CBZ.cityMakeCar(opts.x,opts.z,opts.heading||0,!1,model,.3)}catch{return null}if(!car)return null;if(car.ai=!1,car.baseV=0,car.v=0,car.vx=0,car.vz=0,car._raceCar=!0,opts.livery&&CBZ.cityApplyRaceLivery)try{CBZ.cityApplyRaceLivery(car.group,opts.livery)}catch{}const skill=opts.skill!=null?opts.skill:.8,m={car,tag:opts.tag||"race",name:opts.name||model.name||"Rival",number:opts.number!=null?opts.number:0,skill,aggr:opts.aggr!=null?opts.aggr:.5,consistency:opts.consistency!=null?opts.consistency:.5+skill*.45,mode:opts.mode||"line",line:opts.line||null,lineLen:opts.lineLen||700,trackHalf:opts.trackHalf||7,t:0,laps:0,lane:opts.lane0||0,targetLane:opts.lane0||0,lane0:opts.lane0||0,biasT:null,path:opts.path||null,wpi:0,cpPassed:0,cpTotal:opts.cpTotal||0,finished:!1,dnf:!1,state:"grid",stuckT:0,recoverT:0,mistakeCD:2+Math.random()*5,mistakeT:0,mistakeSteer:0,playerProgress:opts.playerProgress||null,_lapFloor:0};return m.line&&(m.t=opts.t0!=null?(opts.t0%1+1)%1:coarseParam(m.line,opts.x,opts.z)),D.list.push(m),m}function coarseParam(line,x,z){let best=0,bd=1/0;for(let i=0;i<96;i++){const t=i/96,f=line(t),d=(x-f.x)*(x-f.x)+(z-f.z)*(z-f.z);d<bd&&(bd=d,best=t)}return best}function paramNear(line,x,z,tPrev){let best=tPrev,bd=1/0;for(let k=-4;k<=12;k++){const t=(tPrev+k*.004+1)%1,f=line(t),d=(x-f.x)*(x-f.x)+(z-f.z)*(z-f.z);d<bd&&(bd=d,best=t)}return best}function curvatureAt(line,t,lineLen){const f0=line((t+1)%1),f1=line((t+.01+1)%1),dh=Math.abs(angDiff(f1.heading-f0.heading)),ds=Math.max(.5,.01*lineLen);return dh/ds}function despawn(m){const i=D.list.indexOf(m);i>=0&&D.list.splice(i,1),m.car&&CBZ.cityScrapCar?CBZ.cityScrapCar(m.car):m.car&&(m.car.dead=!0)}function despawnAll(tag){for(let i=D.list.length-1;i>=0;i--)(!tag||D.list[i].tag===tag)&&despawn(D.list[i])}function setState(state,tag){for(const m of D.list)(!tag||m.tag===tag)&&(m.state=state)}function progressOf(m){if(m.mode==="path"){if(!m.path||!m.path.length)return 0;let frac=0;const wp=m.path[Math.min(m.wpi,m.path.length-1)];if(wp&&m.car){const d=Math.hypot(wp.x-m.car.pos.x,wp.z-m.car.pos.z);frac=clamp(1-d/40,0,.95)}return(m.wpi+frac)/m.path.length}return m.laps+m.t}function scanTraffic(m){const car=m.car,fx=Math.sin(car.heading),fz=Math.cos(car.heading),rx=Math.cos(car.heading),rz=-Math.sin(car.heading);let ahead=null,aheadDot=18,behind=null,behindDot=-10;function consider(oc,oLane){if(!oc||oc===car||oc.dead)return;const dx=oc.pos.x-car.pos.x,dz=oc.pos.z-car.pos.z;if(dx*dx+dz*dz>20*20)return;const dot=dx*fx+dz*fz,lat=dx*rx+dz*rz;dot>1&&dot<aheadDot&&Math.abs(lat)<5?(aheadDot=dot,ahead={c:oc,lane:oLane,d:dot,lat,v:Math.abs(oc.v||0)}):dot<-1&&dot>behindDot&&Math.abs(lat)<4&&(behindDot=dot,behind={c:oc,lane:oLane,d:-dot,lat,v:Math.abs(oc.v||0)})}for(const o of D.list)o.tag===m.tag&&consider(o.car,o.lane);const P=CBZ.player;return P&&P.driving&&P._vehicle&&consider(P._vehicle,null),{ahead,behind}}function racecraft(m,insideLane,traffic){let want=insideLane;const car=m.car,ahead=traffic.ahead,behind=traffic.behind;if(ahead&&Math.abs(car.v)>ahead.v+1)want=ahead.lat>=0?Math.max(-(m.trackHalf-2.2),insideLane-2.6):Math.min(m.trackHalf-2.2,insideLane+2.6);else if(behind&&behind.d>4&&m.aggr>.45&&behind.v>Math.abs(car.v)-.5){const oL=behind.lane!=null?behind.lane:clamp(m.lane+behind.lat,-2.6,2.6);want=clamp(oL,-2.6,2.6)*(.5+m.aggr*.5)}return want}const DRAFT_RANGE=32,DRAFT_LAT=3.1,DRAFT_MIN=2.2;CBZ.CONFIG.RACE_SLIPSTREAM==null&&(CBZ.CONFIG.RACE_SLIPSTREAM=!0);let _draftPairs=0,_draftPeak=0;function draftSweep(){const on=CBZ.CONFIG.RACE_SLIPSTREAM!==!1,cars=[];for(const m of D.list)m.car&&!m.car.dead&&m.state!=="grid"&&cars.push(m.car);const P=CBZ.player;P&&P.driving&&P._vehicle&&!P._vehicle.dead&&cars.push(P._vehicle);for(const c of cars)c._draft=0;if(!(!on||cars.length<2))for(let i=0;i<cars.length;i++){const c=cars[i];if(Math.abs(c.v||0)<8)continue;const fx=Math.sin(c.heading),fz=Math.cos(c.heading),rx=Math.cos(c.heading),rz=-Math.sin(c.heading);let best=0;for(let j=0;j<cars.length;j++){if(j===i)continue;const o=cars[j],dx=o.pos.x-c.pos.x,dz=o.pos.z-c.pos.z,dot=dx*fx+dz*fz;if(dot<DRAFT_MIN||dot>DRAFT_RANGE)continue;const lat=Math.abs(dx*rx+dz*rz);if(lat>DRAFT_LAT||Math.sin(o.heading)*fx+Math.cos(o.heading)*fz<.6)continue;const near=1-(dot-DRAFT_MIN)/(DRAFT_RANGE-DRAFT_MIN),line=1-lat/DRAFT_LAT,f=near*near*(.45+line*.55);f>best&&(best=f)}c._draft=best,best>.02&&(_draftPairs++,best>_draftPeak&&(_draftPeak=best))}}CBZ.raceDraftAudit=function(){return{on:CBZ.CONFIG.RACE_SLIPSTREAM!==!1,towsSeen:_draftPairs,peak:+_draftPeak.toFixed(3),range:DRAFT_RANGE,lat:DRAFT_LAT,topGain:CBZ.DRAFT_TOP_GAIN==null?.1:CBZ.DRAFT_TOP_GAIN}},CBZ.onUpdate(37.3,function(dt){if(!(g.mode!=="city"||!D.list.length)){dt>.12&&(dt=.12),draftSweep();for(let i=0;i<D.list.length;i++){const m=D.list[i],car=m.car;if(!car||car.dead||car._exploded){m.dnf=!0;continue}const P=perf(car);if(car._draft>0&&(P.top*=1+car._draft*(CBZ.DRAFT_TOP_GAIN==null?.1:CBZ.DRAFT_TOP_GAIN),P.accel*=1+car._draft*.18),m.state==="grid"){car.v=0,car.vx=0,car.vz=0,car.group.position.set(car.pos.x,0,car.pos.z),car.group.rotation.y=car.heading;continue}if(car.wreckT>0){car.wreckT-=dt,car.v*=Math.pow(.05,dt),car.spin=(car.spin||0)*Math.pow(.25,dt),car.heading+=(car.spin||0)*dt,car.pos.x+=Math.sin(car.heading)*car.v*dt,car.pos.z+=Math.cos(car.heading)*car.v*dt,CBZ.cityCollideVehicle&&CBZ.cityCollideVehicle(car),car.vx=Math.sin(car.heading)*car.v,car.vz=Math.cos(car.heading)*car.v,car.group.position.set(car.pos.x,0,car.pos.z),car.group.rotation.y=car.heading,car.wreckT<=0&&(m.state="race",m.stuckT=0);continue}let tgtX,tgtZ,cornerV=P.top;const v=car.v,vmag=Math.abs(v),traffic=scanTraffic(m);if(m.mode==="line"&&m.line){const tPrev=m.t;m.t=paramNear(m.line,car.pos.x,car.pos.z,m.t),tPrev>.9&&m.t<.1?m.laps++:tPrev<.1&&m.t>.9&&m.laps--;const look=clamp(6+vmag*.5,8,26)/m.lineLen,ft=m.line((m.t+look)%1),curv=curvatureAt(m.line,m.t+look*.6,m.lineLen),aLat=P.grip*(1.7+m.skill*.75);cornerV=Math.sqrt(aLat/Math.max(.002,curv));let insideLane=-(.8+m.skill*1.2)*clamp(curv*55,0,1);if(m.biasT==null&&(m.biasT=5),m.biasT>0){m.biasT-=dt;const mix=clamp(m.biasT/5,0,1);insideLane=insideLane*(1-mix)+(m.lane0||0)*mix}m.targetLane=racecraft(m,insideLane,traffic),m.lane+=(m.targetLane-m.lane)*Math.min(1,dt*1.8),m.lane=clamp(m.lane,-(m.trackHalf-1.6),m.trackHalf-1.6),tgtX=ft.x+ft.nx*m.lane,tgtZ=ft.z+ft.nz*m.lane;const f0=m.line(m.t),off=(car.pos.x-f0.x)*f0.nx+(car.pos.z-f0.z)*f0.nz;Math.abs(off)>m.trackHalf+1.5&&(cornerV=Math.min(cornerV,13),tgtX=f0.x,tgtZ=f0.z)}else if(m.mode==="path"&&m.path&&m.path.length){m.wpi>=m.path.length&&(m.finished=!0);const wp=m.path[Math.min(m.wpi,m.path.length-1)];tgtX=wp.x,tgtZ=wp.z;const d=Math.hypot(tgtX-car.pos.x,tgtZ-car.pos.z);d<7&&m.wpi<m.path.length&&(wp.cp!=null&&(m.cpPassed=Math.max(m.cpPassed,wp.cp+1)),m.wpi++,m.cpTotal&&m.cpPassed>=m.cpTotal&&(m.finished=!0));const nx2=m.path[Math.min(m.wpi+1,m.path.length-1)],a1=Math.atan2(tgtX-car.pos.x,tgtZ-car.pos.z),a2=Math.atan2(nx2.x-tgtX,nx2.z-tgtZ),bend=Math.abs(angDiff(a2-a1)),nearV=Math.max(7,P.top*(1-bend*.45)),cruise=P.top*(.72+m.skill*.16);cornerV=d<22?nearV+(cruise-nearV)*clamp((d-6)/16,0,1):cruise}else continue;let pace=.86+m.skill*.14;if(m.playerProgress){const gap=progressOf(m)-m.playerProgress();pace*=clamp(1-gap*.28,.93,1.06)}let targetV=Math.min(P.top*pace,cornerV);if(m.mistakeCD-=dt,m.mistakeCD<=0&&(m.mistakeCD=3+Math.random()*7,Math.random()<(1-m.consistency)*(.3+m.aggr*.25)&&(m.mistakeT=.5+Math.random()*.9,m.mistakeSteer=(Math.random()<.5?-1:1)*(.2+Math.random()*.3))),m.mistakeT>0&&(m.mistakeT-=dt,targetV*=1.12),traffic.ahead&&Math.abs(traffic.ahead.lat)<2.1){const closeD=3.5+vmag*.4;if(traffic.ahead.d<closeD&&(targetV=Math.min(targetV,traffic.ahead.v+Math.max(-2,(traffic.ahead.d-closeD*.5)*.8)),m.mode==="path")){const rx2=Math.cos(car.heading),rz2=-Math.sin(car.heading),side=traffic.ahead.lat>=0?-1:1;tgtX+=rx2*side*3.2,tgtZ+=rz2*side*3.2,targetV=Math.max(targetV,Math.min(cornerV,traffic.ahead.v+3))}}const des=Math.atan2(tgtX-car.pos.x,tgtZ-car.pos.z);let err=angDiff(des-car.heading);m.mistakeT>0&&(err+=m.mistakeSteer);const speedNorm=clamp(vmag/Math.max(1,P.top),0,1),maxYaw=P.turn*(1-speedNorm*.42),yaw=clamp(err*3,-maxYaw,maxYaw);vmag>.3&&(car.heading+=yaw*dt*(v<0?-1:1)),Math.abs(err)>.5&&vmag>12&&(targetV=Math.min(targetV,vmag*(1-Math.min(.5,(Math.abs(err)-.5)*.5)))),v<targetV-.5?car.v+=P.accel*dt*(1-Math.min(.7,Math.max(0,v)/P.top)):v>targetV+.8?car.v-=P.brake*dt:car.v=Math.max(0,car.v-1.1*dt),car.v=clamp(car.v,-9,P.top),m.state==="recover"?(m.recoverT-=dt,car.v=-6,car.heading-=clamp(err,-1,1)*1.4*dt,m.recoverT<=0&&(m.state="race",m.stuckT=0)):(vmag<1.4&&targetV>4?m.stuckT+=dt:m.stuckT=0,m.stuckT>1.6&&(m.state="recover",m.recoverT=1.1+Math.random()*.6)),car.vx=Math.sin(car.heading)*car.v,car.vz=Math.cos(car.heading)*car.v,car.pos.x+=car.vx*dt,car.pos.z+=car.vz*dt,CBZ.cityCollideVehicle?CBZ.cityCollideVehicle(car)>.05&&vmag>10&&(car.v*=.45,car.engineHp==null&&(car.engineHp=100),car.engineHp=Math.max(0,car.engineHp-Math.max(0,(vmag-6)*.7)),car.engineHp<=45&&(car._smoking=!0)):CBZ.collide&&CBZ.collide(car.pos,1),m.mode==="path"&&CBZ.city.arena&&CBZ.city.arena.clampToCity&&CBZ.city.arena.clampToCity(car.pos,1),car.group.position.set(car.pos.x,0,car.pos.z),car.group.rotation.y=car.heading}}});function createRace(opts){const C=course(opts.course),kit={laps:opts.laps||1,trackLen:opts.trackLen||courseNumber(C,"lineLen",700),course:C||null,entrants:opts.entrants.map(function(e){return{id:e.id,name:e.name,number:e.number,color:e.color,isPlayer:!!e.isPlayer,driver:e.driver||null,progress:e.progress,speed:e.speed||function(){return 20},total:0,pos:opts.entrants.length,lapFloor:e.lapFloor0!=null?e.lapFloor0:0,lapStart:0,lapTimes:[],best:0,lastLap:0,finished:!1,finishT:0}}),time:0,order:[],update:function(dt){kit.time+=dt;for(const e of kit.entrants){if(e.finished)continue;e.total=e.progress();const fl=Math.floor(e.total+1e-6);if(fl>e.lapFloor){e.lapFloor=fl;const lt=kit.time-e.lapStart;e.lapStart=kit.time,fl>0&&lt>3&&(e.lastLap=lt,e.lapTimes.push(lt),(!e.best||lt<e.best)&&(e.best=lt))}e.total>=kit.laps-1e-6&&!e.finished&&(e.finished=!0,e.finishT=kit.time)}kit.order=kit.entrants.slice().sort(function(a,b){return a.finished&&b.finished?a.finishT-b.finishT:a.finished!==b.finished?a.finished?-1:1:b.total-a.total});for(let i=0;i<kit.order.length;i++)kit.order[i].pos=i+1},gapSeconds:function(ahead,behind){const dp=Math.max(0,ahead.total-behind.total),sp=Math.max(6,behind.speed());return dp*kit.trackLen/sp},playerRow:function(){for(const e of kit.entrants)if(e.isPlayer)return e;return null},playerContext:function(){const p=kit.playerRow();if(!p)return null;const idx=kit.order.indexOf(p),ahead=idx>0?kit.order[idx-1]:null,behind=idx<kit.order.length-1?kit.order[idx+1]:null;return{row:p,ahead,behind,gapA:ahead?kit.gapSeconds(ahead,p):0,gapB:behind?kit.gapSeconds(p,behind):0}}};for(const e of kit.entrants)e.lapStart=0;return CBZ.raceKit._last=kit,kit}CBZ.raceLive=function(){if(D.list.length)return!0;try{const S=CBZ.speedwayRaceState&&CBZ.speedwayRaceState();if(S&&S.active)return!0;const st=CBZ.cityStreetRacing&&CBZ.cityStreetRacing.state&&CBZ.cityStreetRacing.state();if(st&&st.active)return!0}catch{}return!1},CBZ.raceDrivers={spawn,despawn,despawnAll,release:function(m){const i=D.list.indexOf(m);return i>=0&&D.list.splice(i,1),m&&m.car||null},setState,progressOf,modelForStyle,list:function(tag){return tag?D.list.filter(m=>m.tag===tag):D.list.slice()},enabled:function(){return CBZ.CONFIG.RACE_REAL_DRIVERS!==!1}},CBZ.raceKit={create:createRace,registerCourse,course,pathCourse,adopt:adoptCourse,courseLength:function(c){return courseNumber(course(c),"lineLen",0)}},CBZ.raceToolAudit=raceToolAudit,CBZ.speedwayCourse&&(registerCourse("speedway",CBZ.speedwayCourse),adoptCourse("speedway-weekend"))})();
