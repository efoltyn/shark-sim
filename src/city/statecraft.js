(function(){"use strict";const CBZ=window.CBZ;if(!CBZ)return;const g=CBZ.game;if(!CBZ.CONFIG)return;CBZ.CONFIG.GOV_OFFICE==null&&(CBZ.CONFIG.GOV_OFFICE=!0),CBZ.CONFIG.GOV_MILITARY==null&&(CBZ.CONFIG.GOV_MILITARY=!0);let _seed=774113329;function rng(){return _seed=_seed*1103515245+12345&2147483647,_seed/2147483647}function clamp(v,a,b){return v<a?a:v>b?b:v}function clamp01(v){return v<0?0:v>1?1:v}function money(n){return"$"+Math.round(n).toLocaleString()}const SALARY={city:420,state:900,federal:900,country:1800},SKIM_FRAC=.06,SKIM_CAP=25e3,SKIM_COOLDOWN=2,SKIM_CORRUPTION=9,DISCOVERY_DIVISOR=220,DISCOVERY_SCANDAL=8,POLICE_COST=6e3,POLICE_BODIES=6,POLICE_MUL=1.15,TAX_STEP=.02,TAX_MIN=0,TAX_MAX=.3,CURFEW_COST=2500,CURFEW_DAYS=2,CURFEW_LO=23,CURFEW_HI=5,CURFEW_DRIP=5,CURFEW_NPC_MAX=3,CURFEW_NPC_GAP=18,AMNESTY_COOLDOWN=3,AMNESTY_RESPECT=12,EMERGENCY_STEP=20,PARDON_COOLDOWN=3,SURGE_COST=4e3,SURGE_BODIES=8,SURGE_DAYS=2,GUARD_COST=3500,MARTIAL_COST=25e3,MARTIAL_DAYS=3,MARTIAL_BODIES=6,MARTIAL_LEGIT=.45,MARTIAL_UNREST_T=.5,MARTIAL_READINESS=.08,REFUSAL_READINESS=.05,READINESS_FLOOR=.1,TYRANNY_MAX=100,TYRANNY_DECAY=2.5,TYRANNY_DRAG_FLOOR=20,TYRANNY_DRAG_DIV=12;function fresh(){return{seatId:null,sinceDay:0,tyranny:0,refusals:0,skimmed:0,curfewUntil:0,curfewNight:null,curfewFlagged:0,curfewWarned:null,lastPardonDay:-999,lastSkimDay:-999,lastAmnestyDay:-999,forceAdded:0,origPoliceForce:null,policeBumped:!1,readinessHeld:0,soldiersOut:0,deployments:[],paidDay:-1}}function reset(){const S=g.govWorld;if(S)try{restorePolice(S)}catch{}g.govWorld=fresh()}function st(){return g.govWorld||(g.govWorld=fresh()),g.govWorld}function politics(){const w=CBZ.cityWorldEnsure?CBZ.cityWorldEnsure():null;return w&&w.politics?w.politics:g.cityPolitics||null}function day(){return CBZ.worldDay?CBZ.worldDay():0}function playerSid(){return CBZ.officials&&CBZ.officials.PLAYER_SID||"player"}function allRecs(){return!CBZ.polity||!CBZ.polity.list?[]:[].concat(CBZ.polity.list("city"),CBZ.polity.list("state"),CBZ.polity.list("federal"),CBZ.polity.list("country"))}function titleOf(rec){if(CBZ.officials&&CBZ.officials.titleFor)try{const t=CBZ.officials.titleFor(rec);if(t)return t}catch{}return"Official"}function holds(){if(!CBZ.CONFIG.GOV_OFFICE)return null;const sid=playerSid(),recs=allRecs();for(let i=0;i<recs.length;i++){const rec=recs[i];if(!rec||!rec.office||rec.office.holder!==sid)continue;const S=st();return{id:rec.id,rec,title:titleOf(rec),kind:rec.kind,sinceDay:S.sinceDay||0}}return null}function seatCountry(h){return!h||!CBZ.polity||!CBZ.polity.countryOf?null:CBZ.polity.countryOf(h.id)||null}function inJurisdiction(h,x,z){if(!h||!CBZ.polity||!CBZ.polity.of)return!1;const rec=h.rec;if(rec.kind==="country"||rec.kind==="state"){const loc=CBZ.polity.of(x,z);if(!loc)return!1;const up=rec.kind==="country"?CBZ.polity.countryOf(loc.id):CBZ.polity.stateOf(loc.id);return!!(up&&up.id===rec.id)}const r=rec.rect;return r?Math.abs(x-r.cx)<=r.hx&&Math.abs(z-r.cz)<=r.hz:!1}function news(text){if(CBZ.phoneNotify)try{CBZ.phoneNotify({app:"news",from:"City Desk",text,priority:1});return}catch{}CBZ.cityFeed&&CBZ.cityFeed(text,"#ffd76a")}function orders(from,text,prio){if(CBZ.phoneNotify)try{CBZ.phoneNotify({app:"system",from,text,priority:prio??1});return}catch{}CBZ.city&&CBZ.city.note&&CBZ.city.note(text,3)}function shock(id,n){if(CBZ.approvalShock&&isFinite(n))try{CBZ.approvalShock(id,n)}catch{}}function addTyranny(n,why){if(!isFinite(n)||n<=0)return;const S=st();S.tyranny=clamp((S.tyranny||0)+n,0,TYRANNY_MAX),S.lastForce=why||null}function tyranny(){return st().tyranny||0}function approvalOf(){const h=holds();return h?h.rec.approval||0:null}function treasuryOf(){const h=holds();return h?h.rec.treasury||0:null}function legitimacy(){const h=holds();if(!h)return 0;const band=clamp01(((h.rec.approval||0)-20)/60);return clamp01(band*(1-tyranny()/100))}function payFromTreasury(rec,amount){const have=rec.treasury||0;return have<amount?!1:(rec.treasury=have-amount,!0)}function bumpPolice(S,bodies,mul){if(CBZ.cityPoliceForceAdd&&bodies)try{CBZ.cityPoliceForceAdd(bodies),S.forceAdded=(S.forceAdded||0)+bodies}catch{}mul&&CBZ.CITY&&!S.policeBumped&&(S.origPoliceForce=CBZ.CITY.policeForce!=null?CBZ.CITY.policeForce:40,CBZ.CITY.policeForce=Math.round(S.origPoliceForce*mul),S.policeBumped=!0)}function restorePolice(S){if(S){if(S.forceAdded&&CBZ.cityPoliceForceAdd)try{CBZ.cityPoliceForceAdd(-S.forceAdded)}catch{}S.forceAdded=0,S.policeBumped&&CBZ.CITY&&S.origPoliceForce!=null&&(CBZ.CITY.policeForce=S.origPoliceForce),S.policeBumped=!1,S.origPoliceForce=null}}const DECREES={police:{name:"Fund the force",cost:POLICE_COST,note:"Six more bodies on the street, paid out of the treasury.",moves:["police.js forcePool via cityPoliceForceAdd","CBZ.CITY.policeForce","rec.treasury","approvalShock"],live:function(){return!!st().policeBumped},gate:function(h){return(h.rec.treasury||0)<POLICE_COST?{ok:!1,why:"The treasury is short "+money(POLICE_COST-(h.rec.treasury||0))+"."}:st().policeBumped?{ok:!1,why:"The force is already funded up."}:{ok:!0}},run:function(h){const S=st();payFromTreasury(h.rec,POLICE_COST),bumpPolice(S,POLICE_BODIES,POLICE_MUL);const delta=(CBZ.approvalState&&CBZ.approvalState.murders7d?CBZ.approvalState.murders7d(h.id):0)>=5?4:-4;return shock(h.id,delta),addTyranny(2,"funded the police"),news(h.title+" "+(delta>0?"answers a bloody week with badges":"puts six more badges on quiet streets")+" \u2014 "+money(POLICE_COST)+" out of the "+h.rec.name+" treasury."),{ok:!0,why:""}}},taxUp:{name:"Raise the tax rate",cost:0,note:"+2 points. Fills the treasury; the services line in every voter's head gets shorter.",moves:["rec.taxRate -> approval.js services term","rec.taxRate -> sim/econstate.js treasury flow"],live:function(){return!1},gate:function(h){return(h.rec.taxRate!=null?h.rec.taxRate:.1)>=TAX_MAX-1e-9?{ok:!1,why:"The rate is already at the ceiling."}:{ok:!0}},run:function(h){const before=h.rec.taxRate!=null?h.rec.taxRate:.1;return h.rec.taxRate=clamp(before+TAX_STEP,TAX_MIN,TAX_MAX),shock(h.id,-5),news(h.rec.name+" raises the rate to "+Math.round(h.rec.taxRate*100)+"%."),{ok:!0,why:""}}},taxDown:{name:"Cut the tax rate",cost:0,note:"-2 points. Popular, and the treasury stops filling.",moves:["rec.taxRate -> approval.js services term","rec.taxRate -> sim/econstate.js treasury flow"],live:function(){return!1},gate:function(h){return(h.rec.taxRate!=null?h.rec.taxRate:.1)<=TAX_MIN+1e-9?{ok:!1,why:"There is nothing left to cut."}:{ok:!0}},run:function(h){const before=h.rec.taxRate!=null?h.rec.taxRate:.1;return h.rec.taxRate=clamp(before-TAX_STEP,TAX_MIN,TAX_MAX),shock(h.id,5),news(h.rec.name+" cuts the rate to "+Math.round(h.rec.taxRate*100)+"%."),{ok:!0,why:""}}},curfew:{name:"Declare a curfew",cost:CURFEW_COST,note:"23:00 to 05:00 for two days. It binds you too.",moves:["g.heat on the player outdoors at night","ped.npcHeat/npcWanted via cityNpcOffense","rec.treasury","approvalShock","tyranny"],live:function(){return day()<(st().curfewUntil||0)},gate:function(h){return day()<(st().curfewUntil||0)?{ok:!1,why:"A curfew is already in force."}:(h.rec.treasury||0)<CURFEW_COST?{ok:!1,why:"Overtime for a curfew costs "+money(CURFEW_COST)+"."}:{ok:!0}},run:function(h){const S=st();return payFromTreasury(h.rec,CURFEW_COST),S.curfewUntil=day()+CURFEW_DAYS,S.curfewNight=null,S.curfewFlagged=0,S.curfewWarned=null,shock(h.id,-7),addTyranny(8,"declared a curfew"),CBZ.city&&CBZ.city.big&&CBZ.city.big("CURFEW \xB7 "+String(h.rec.name).toUpperCase()),news(h.title+" signs a curfew over "+h.rec.name+": nobody on the street between 23:00 and 05:00 for "+CURFEW_DAYS+" days."),orders("Your office","The curfew you signed does not exempt you. Be indoors by 23:00.",1),{ok:!0,why:""}}},amnesty:{name:"Declare an amnesty",cost:0,note:"Outstanding charges dropped across the jurisdiction. The street remembers it; the ballot does too.",moves:["ped.npcHeat/npcWanted/bounty on every ped in the rect","g.respect via CBZ.city.addRespect","approvalShock"],live:function(){return!1},gate:function(h){const S=st(),since=day()-(S.lastAmnestyDay||-999);return since<AMNESTY_COOLDOWN?{ok:!1,why:"Another amnesty this soon is worth nothing \xB7 "+(AMNESTY_COOLDOWN-since)+" day(s)."}:{ok:!0}},run:function(h){const S=st();S.lastAmnestyDay=day();const peds=CBZ.cityPeds||[];let n=0;for(let i=0;i<peds.length;i++){const p=peds[i];!p||p.dead||p.isPlayer||((p.npcWanted|0)>0||(p.npcHeat||0)>0||(p.bounty||0)>0)&&inJurisdiction(h,p.pos.x,p.pos.z)&&(p.npcHeat=0,p.npcWanted=0,p.bounty=0,n++)}return shock(h.id,-6),CBZ.city&&CBZ.city.addRespect&&CBZ.city.addRespect(AMNESTY_RESPECT),news(h.title+" signs an amnesty over "+h.rec.name+" \u2014 "+n+" outstanding case(s) dropped. The law looks the other way this time."),{ok:!0,why:n?"":"nobody was being hunted"}}},emergency:{name:"Assume emergency powers",cost:0,note:"+20 points. At 100 the country is a dictatorship and you are its dictator.",moves:["g.cityPolitics.emergencyPowers -> regimes.js emergencyRule/dictatorship ladder","approvalShock","tyranny"],live:function(){const p=politics();return!!(p&&(p.emergencyPowers||0)>0)},gate:function(h){if(h.kind==="city")return{ok:!1,why:"Emergency powers are a national instrument. A mayor cannot reach them."};const p=politics();return p?(p.emergencyPowers||0)>=100?{ok:!1,why:"There is nothing left to assume. You already have it all."}:{ok:!0}:{ok:!1,why:"No political record to write to."}},run:function(h){const p=politics(),before=p.emergencyPowers||0;return p.emergencyPowers=clamp(before+EMERGENCY_STEP,0,100),shock(h.id,-9),addTyranny(15,"assumed emergency powers"),news("Emergency powers in "+h.rec.name+" stand at "+Math.round(p.emergencyPowers)+"%."),p.emergencyPowers>=100?orders("Your office","There is no higher authority left to ask. Whatever happens next is yours.",2):orders("Your office","Emergency powers at "+Math.round(p.emergencyPowers)+"%. At 100 the republic stops being one.",1),{ok:!0,why:""}}},skim:{name:"Skim the treasury",cost:0,note:"Six percent, into your own pocket. Auditors are slow, not blind.",moves:["rec.treasury -> g.cash","g.cityPolitics.corruption -> scandal -> approval.js events term","tyranny"],live:function(){const p=politics();return!!(p&&(p.corruption||0)>0)},gate:function(h){const S=st(),since=day()-(S.lastSkimDay||-999);return since<SKIM_COOLDOWN?{ok:!1,why:"The books were touched too recently \xB7 "+(SKIM_COOLDOWN-since)+" day(s)."}:Math.min(SKIM_CAP,Math.round((h.rec.treasury||0)*SKIM_FRAC))<100?{ok:!1,why:"There is nothing in the "+h.rec.name+" treasury worth taking."}:{ok:!0}},run:function(h){const S=st(),take=Math.min(SKIM_CAP,Math.round((h.rec.treasury||0)*SKIM_FRAC));payFromTreasury(h.rec,take),CBZ.city&&CBZ.city.addCash&&CBZ.city.addCash(take),S.lastSkimDay=day(),S.skimmed=(S.skimmed||0)+take;const p=politics();return p&&(p.corruption=clamp((p.corruption||0)+SKIM_CORRUPTION,0,100)),addTyranny(4,"skimmed the treasury"),orders("Your office",money(take)+" moved. It will not stay invisible forever.",0),{ok:!0,why:""}}}},DECREE_ORDER=["taxDown","taxUp","police","amnesty","curfew","skim","emergency"];function decrees(){const h=holds(),out=[];for(let i=0;i<DECREE_ORDER.length;i++){const key=DECREE_ORDER[i],d=DECREES[key];let ok=!!h,why=h?"":"You hold no office.";if(h){const gt=d.gate(h);ok=gt.ok,why=gt.why||""}out.push({key,name:d.name,cost:d.cost,note:d.note,live:!!d.live(),ok,why})}return out}function decree(key){if(!CBZ.CONFIG.GOV_OFFICE)return{ok:!1,why:"Office powers are disabled."};const d=DECREES[key];if(!d)return{ok:!1,why:"No such decree."};const h=holds();if(!h)return{ok:!1,why:"You hold no office."};const gt=d.gate(h);if(!gt.ok)return{ok:!1,why:gt.why};let r;try{r=d.run(h)}catch{return{ok:!1,why:"The order did not go through."}}return CBZ.cityHudDirty&&CBZ.cityHudDirty(),r||{ok:!0,why:""}}function pardon(opts){if(opts=opts||{},!CBZ.CONFIG.GOV_OFFICE)return{ok:!1,why:"Office powers are disabled."};const h=holds();if(!h)return{ok:!1,why:"You hold no office."};const S=st(),stars=CBZ.cityStars?CBZ.cityStars():g.wanted|0;if(stars<=0)return{ok:!1,why:"There is nothing on you to pardon."};const P=CBZ.player,at=opts.at||P&&P.pos||null;if(!at)return{ok:!1,why:"No fixed position to judge jurisdiction by."};if(!inJurisdiction(h,at.x,at.z))return{ok:!1,why:"This is not "+h.rec.name+". Your writ does not run here."};if((g.cityCopKills|0)>0)return{ok:!1,why:"You killed police. No signature on earth covers that."};if(stars>=5)return{ok:!1,why:"A five-star manhunt is federal. The pen does not reach it."};if(g.cityCrimeLabel==="Military Incursion")return{ok:!1,why:"That happened inside the wire. It is the army's file, not yours."};const since=day()-(S.lastPardonDay||-999);if(since<PARDON_COOLDOWN)return{ok:!1,why:"You signed one "+since+" day(s) ago. Wait "+(PARDON_COOLDOWN-since)+"."};S.lastPardonDay=day();let erased=stars;stars<=2?CBZ.cityWantedReset?CBZ.cityWantedReset():CBZ.cityReduceWanted&&CBZ.cityReduceWanted(stars):(erased=2,CBZ.cityReduceWanted&&CBZ.cityReduceWanted(2));const cost=3+4*erased;shock(h.id,-cost),addTyranny(5+4*erased,"pardoned themselves");const selfName=CBZ.officials&&CBZ.officials.identityOf?CBZ.officials.identityOf(playerSid()).name:null;return news((selfName&&selfName!=="Someone"?selfName:h.title)+" signs a pardon covering "+erased+" count(s) in "+h.rec.name+". The law looks the other way this time."),CBZ.cityHudDirty&&CBZ.cityHudDirty(),{ok:!0,why:"",erased,approvalCost:cost}}function generalName(){if(!CBZ.polity||!CBZ.polity.list)return"Fort Brandt Command";const fed=CBZ.polity.list("federal")||[];for(let i=0;i<fed.length;i++){const r=fed[i];if(!r||!r.office||!r.office.holder)continue;const nm=CBZ.officials&&CBZ.officials.identityOf?CBZ.officials.identityOf(r.office.holder).name:null;if(nm&&nm!=="Someone")return"Gen. "+nm}return"Fort Brandt Command"}function militaryOf(h){const c=seatCountry(h);if(!c||!CBZ.polwar||!CBZ.polwar.militaryOf)return null;try{return CBZ.polwar.militaryOf(c.id)}catch{return null}}function martialReason(h){const c=seatCountry(h);if(!c)return null;if(CBZ.polwar&&CBZ.polwar.activeWarFor)try{if(CBZ.polwar.activeWarFor(c.id))return"a shooting war"}catch{}if(CBZ.civilwar&&CBZ.civilwar.fractureOf)try{if(CBZ.civilwar.fractureOf(c.id))return"an armed secession"}catch{}if(CBZ.civilwar&&CBZ.civilwar.unrest)try{if(CBZ.civilwar.unrest(c.id)>MARTIAL_UNREST_T)return"unrest past breaking point"}catch{}return null}function garrison(){const out=[],T=CBZ.cityMilitaryPersonnel;if(!Array.isArray(T))return out;for(let i=0;i<T.length;i++){const p=T[i];if(!(!p||p.dead||p._govDeployed||!p.pos)&&p.organization==="military"){if(CBZ.factions&&CBZ.factions.of){const ids=CBZ.factions.of(p);if(ids&&ids.length&&ids.indexOf("army")<0)continue}out.push(p)}}return out}function refuse(h,why){const S=st();S.refusals=(S.refusals||0)+1;const mil=militaryOf(h);mil&&isFinite(mil.readiness)&&(mil.readiness=Math.max(READINESS_FLOOR,mil.readiness-REFUSAL_READINESS)),shock(h.id,-3),addTyranny(5,"tried to order the army out");const gen=generalName(),line=S.refusals>=3?"That is the third time. The staff have stopped pretending it is a scheduling problem.":S.refusals===2?"Again, no. Ask me when the country still recognises your signature.":"The garrison will not deploy on that order. "+why;return orders(gen,line,2),{ok:!1,why,refused:!0,refusals:S.refusals}}function deployGuard(h,at){if(!CBZ.protection||!CBZ.protection.create)return{ok:!1,why:"No protection system loaded."};if((h.rec.treasury||0)<GUARD_COST)return{ok:!1,why:"The detail costs "+money(GUARD_COST)+" and the treasury is short."};if(legitimacy()<.15)return refuse(h,"You are not legitimate enough to be worth guarding.");let det=CBZ.protection.get?CBZ.protection.get("off_"+h.id):null;if(det||(det=CBZ.protection.create({id:"off_"+h.id,principal:{kind:"sid",ref:playerSid()},gearTier:h.kind==="country"?2:1,formation:"escort",fundingSource:"treasury",legalStatus:"state",memberCount:0})),!det)return{ok:!1,why:"The detail could not be raised."};const cap=CBZ.protection.HIRE_CAP||8;if((det.memberCount||0)>=cap)return{ok:!1,why:"The detail is already at full strength."};payFromTreasury(h.rec,GUARD_COST),det.principal.ref=playerSid(),det.memberCount=Math.min(cap,(det.memberCount||0)+1);const threshold=CBZ.militia&&CBZ.militia.MILITIA_HEADCOUNT||6;det.memberCount>=threshold&&addTyranny(6,"built a private army out of the public purse");const A=CBZ.city&&CBZ.city.arena,P=CBZ.player;if(A&&CBZ.protection.spawnMembers){const px=at&&at.x!=null?at.x:P?P.pos.x:0,pz=at&&at.z!=null?at.z:P?P.pos.z:0;try{CBZ.protection.spawnMembers(det,A,px,pz,rng)}catch{}}return shock(h.id,-1),orders("Your office","One more body on your detail \xB7 "+det.memberCount+" now, "+money(GUARD_COST)+" out of the treasury.",0),{ok:!0,why:"",detail:det.id,memberCount:det.memberCount}}function deploySurge(h,at){const S=st();if((h.rec.treasury||0)<SURGE_COST)return{ok:!1,why:"A surge costs "+money(SURGE_COST)+" and the treasury is short."};for(let i=0;i<S.deployments.length;i++)if(S.deployments[i].kind==="surge")return{ok:!1,why:"A surge is already running."};if(legitimacy()<.2)return refuse(h,"The commissioner will not move officers on your word.");const P=CBZ.player,pt={x:at&&at.x!=null?at.x:P?P.pos.x:0,z:at&&at.z!=null?at.z:P?P.pos.z:0};return inJurisdiction(h,pt.x,pt.z)?(payFromTreasury(h.rec,SURGE_COST),bumpPolice(S,SURGE_BODIES,0),S.deployments.push({kind:"surge",at:pt,day:day(),until:day()+SURGE_DAYS,bodies:SURGE_BODIES}),shock(h.id,-2),addTyranny(4,"surged police into a neighbourhood"),news(h.title+" surges "+SURGE_BODIES+" officers into "+h.rec.name+" for "+SURGE_DAYS+" days."),{ok:!0,why:""}):{ok:!1,why:"That point is outside "+h.rec.name+"."}}function deployMartial(h,at){const S=st();if(h.kind==="city")return{ok:!1,why:"A mayor does not command soldiers. Win a state or the country first."};const reason=martialReason(h);if(!reason)return{ok:!1,why:"No war, no fracture, no unrest. The garrison has no reason to leave the wire."};if(!CBZ._militaryBase)return{ok:!1,why:"Fort Brandt is not on the map."};const troops=garrison();if(troops.length<MARTIAL_BODIES)return{ok:!1,why:"Fort Brandt cannot field "+MARTIAL_BODIES+" right now."};if((h.rec.treasury||0)<MARTIAL_COST)return{ok:!1,why:"Moving the garrison costs "+money(MARTIAL_COST)+"."};for(let i=0;i<S.deployments.length;i++)if(S.deployments[i].kind==="martial")return{ok:!1,why:"The garrison is already deployed."};if(legitimacy()<MARTIAL_LEGIT)return refuse(h,"Approval "+Math.round(h.rec.approval||0)+", and the file on how you govern is thick.");const P=CBZ.player,pt={x:at&&at.x!=null?at.x:P?P.pos.x:0,z:at&&at.z!=null?at.z:P?P.pos.z:0};if(!inJurisdiction(h,pt.x,pt.z))return{ok:!1,why:"That point is outside "+h.rec.name+"."};payFromTreasury(h.rec,MARTIAL_COST);const bodies=[];for(let i=0;i<MARTIAL_BODIES&&i<troops.length;i++){const p=troops[i],ang=i/MARTIAL_BODIES*Math.PI*2;p._govHome={x:p.pos.x,z:p.pos.z,guard:p.guard||null},p._govDeployed=!0,p.pos.x=pt.x+Math.cos(ang)*4.5,p.pos.z=pt.z+Math.sin(ang)*4.5,p.guard={x:pt.x,z:pt.z},p.path=null,p.state="idle",p.speed=0,p.target&&p.target.set&&p.target.set(p.pos.x,0,p.pos.z),bodies.push(p)}const mil=militaryOf(h);let heldR=0,heldS=0;return mil&&(isFinite(mil.readiness)&&(heldR=Math.min(MARTIAL_READINESS,Math.max(0,mil.readiness-READINESS_FLOOR)),mil.readiness-=heldR),isFinite(mil.soldiers)&&(heldS=Math.min(bodies.length,Math.max(0,mil.soldiers)),mil.soldiers-=heldS)),S.readinessHeld=(S.readinessHeld||0)+heldR,S.soldiersOut=(S.soldiersOut||0)+heldS,S.deployments.push({kind:"martial",at:pt,day:day(),until:day()+MARTIAL_DAYS,bodies,n:bodies.length}),shock(h.id,-11),addTyranny(18,"put soldiers on the street"),CBZ.city&&CBZ.city.big&&CBZ.city.big("TROOPS DEPLOYED \xB7 "+String(h.rec.name).toUpperCase()),news(h.title+" orders the Fort Brandt garrison into "+h.rec.name+" over "+reason+". "+bodies.length+" soldiers on the street for "+MARTIAL_DAYS+" days."),orders(generalName(),"Column moving. "+bodies.length+" under arms, "+MARTIAL_DAYS+" days, and then they come home whatever you say.",2),{ok:!0,why:"",bodies:bodies.length,reason}}function deploy(kind,at){if(!CBZ.CONFIG.GOV_MILITARY)return{ok:!1,why:"Deployments are disabled."};const h=holds();return h?kind==="guard"?deployGuard(h,at):kind==="surge"?deploySurge(h,at):kind==="martial"?deployMartial(h,at):{ok:!1,why:"No such order."}:{ok:!1,why:"You hold no office."}}function deployments(){const S=st(),out=[];for(let i=0;i<S.deployments.length;i++){const d=S.deployments[i];out.push({kind:d.kind,at:{x:d.at.x,z:d.at.z},day:d.day,until:d.until})}return out}function sendHome(d){const bodies=d.bodies||[];for(let i=0;i<bodies.length;i++){const p=bodies[i];if(!p)continue;const home=p._govHome;p._govDeployed=!1,!p.dead&&home&&p.pos&&(p.pos.x=home.x,p.pos.z=home.z,p.guard=home.guard||null,p.path=null,p.state="idle",p.speed=0,p.target&&p.target.set&&p.target.set(home.x,0,home.z)),p._govHome=null}d.bodies=[]}function refundMil(){const S=st();if(!(S.readinessHeld||S.soldiersOut))return;const h=holds();let mil=h?militaryOf(h):null;if(!mil&&CBZ.polwar&&CBZ.polwar.militaryOf)try{mil=CBZ.polwar.militaryOf(S.seatCountryId||"republic")}catch{mil=null}mil&&(isFinite(mil.readiness)&&S.readinessHeld&&(mil.readiness=clamp(mil.readiness+S.readinessHeld,0,1)),isFinite(mil.soldiers)&&S.soldiersOut&&(mil.soldiers=mil.soldiers+S.soldiersOut)),S.readinessHeld=0,S.soldiersOut=0}function endDeployment(d){if(d.kind==="martial")sendHome(d),refundMil();else if(d.kind==="surge"){const S=st();if(CBZ.cityPoliceForceAdd&&d.bodies)try{CBZ.cityPoliceForceAdd(-(d.bodies|0)),S.forceAdded=Math.max(0,(S.forceAdded||0)-(d.bodies|0))}catch{}}}function standDownAll(quiet){const S=st();let had=!1;for(let i=S.deployments.length-1;i>=0;i--){had=!0;try{endDeployment(S.deployments[i])}catch{}S.deployments.splice(i,1)}return S.curfewUntil&&(had=!0,S.curfewUntil=0,S.curfewNight=null,S.curfewFlagged=0),(S.policeBumped||S.forceAdded)&&(had=!0,restorePolice(S)),refundMil(),had&&!quiet&&news("The orders stand down, the office changes hands."),had}function dailyTick(d){const S=st(),h=holds();if(S.tyranny>0&&(S.tyranny=Math.max(0,S.tyranny-TYRANNY_DECAY)),!h){S.seatId?(S.seatId=null,standDownAll(!1)):(S.deployments.length||S.curfewUntil||S.policeBumped||S.forceAdded)&&standDownAll(!0);return}S.seatId!==h.id&&(S.seatId=h.id,S.sinceDay=d,S.paidDay=-1);const c=seatCountry(h);S.seatCountryId=c?c.id:null;for(let i=S.deployments.length-1;i>=0;i--){const dep=S.deployments[i];if(!(d<dep.until)){try{endDeployment(dep)}catch{}S.deployments.splice(i,1),dep.kind==="martial"&&news("The garrison returns to Fort Brandt.")}}if(S.curfewUntil&&d>=S.curfewUntil&&(S.curfewUntil=0,S.curfewNight=null,S.curfewFlagged=0,news("The curfew over "+h.rec.name+" lapses.")),S.paidDay!==d){S.paidDay=d;const wage=SALARY[h.kind]||SALARY.city,have=h.rec.treasury||0;have>=wage?(h.rec.treasury=have-wage,CBZ.city&&CBZ.city.addCash&&CBZ.city.addCash(wage)):have>1?(h.rec.treasury=0,CBZ.city&&CBZ.city.addCash&&CBZ.city.addCash(Math.round(have)),orders("Treasury","Payroll ran dry \xB7 "+money(have)+" is all there was.",1)):orders("Treasury","There is no money in "+h.rec.name+". You were not paid.",1)}const p=politics();p&&(p.corruption||0)>0&&rng()<(p.corruption||0)/DISCOVERY_DIVISOR&&(p.scandal=clamp((p.scandal||0)+DISCOVERY_SCANDAL,0,100),p.corruption=clamp((p.corruption||0)-12,0,100),shock(h.id,-6),news("Auditors publish a line item nobody can explain. "+h.title+"'s office declines to comment.")),S.tyranny>TYRANNY_DRAG_FLOOR&&shock(h.id,-(S.tyranny-TYRANNY_DRAG_FLOOR)/TYRANNY_DRAG_DIV)}CBZ.onNewDay&&CBZ.onNewDay(function(d){if(CBZ.CONFIG.GOV_OFFICE)try{dailyTick(d)}catch(e){try{console.error("[statecraft] daily tick failed",e)}catch{}}});let curfewT=0,npcT=0;CBZ.onUpdate&&CBZ.onUpdate(33.35,function(dt){if(!CBZ.CONFIG.GOV_OFFICE||g.mode!=="city"||(curfewT-=dt,curfewT>0))return;curfewT=1;const S=st();if(!S.curfewUntil||day()>=S.curfewUntil)return;const h=holds();if(!h)return;const P=CBZ.player;if(!P||P.dead||!P.pos)return;const hr=CBZ.citySunHour?CBZ.citySunHour():12;if(!(hr>=CURFEW_LO||hr<CURFEW_HI)){S.curfewNight=null;return}const d=day();S.curfewNight!==d&&(S.curfewNight=d,S.curfewFlagged=0);const inside=inJurisdiction(h,P.pos.x,P.pos.z),indoor=!!(CBZ.cityNav&&CBZ.cityNav.indoorLotAt&&CBZ.cityNav.indoorLotAt(P.pos.x,P.pos.z));if(inside&&!indoor&&(g.heat=(g.heat||0)+CURFEW_DRIP,CBZ.cityHudDirty&&CBZ.cityHudDirty(),S.curfewWarned!==d&&(S.curfewWarned=d,orders("Dispatch","Curfew. Your own curfew. Get inside.",1))),npcT-=1,npcT>0||S.curfewFlagged>=CURFEW_NPC_MAX||!CBZ.cityNpcOffense)return;npcT=CURFEW_NPC_GAP;const peds=CBZ.cityPeds||[];let best=null,bestD=45*45;for(let i=0;i<peds.length;i++){const q=peds[i];if(!q||q.dead||q.isPlayer||!q.pos||(q.npcWanted|0)>0||q.organization==="military"||q.kind==="cop"||!inJurisdiction(h,q.pos.x,q.pos.z)||CBZ.cityNav&&CBZ.cityNav.indoorLotAt&&CBZ.cityNav.indoorLotAt(q.pos.x,q.pos.z))continue;const dx=q.pos.x-P.pos.x,dz=q.pos.z-P.pos.z,dd=dx*dx+dz*dz;dd<bestD&&(bestD=dd,best=q)}if(best){try{CBZ.cityNpcOffense(best,30,"curfew violation")}catch{}S.curfewFlagged=(S.curfewFlagged||0)+1}});let _sweeperWired=!1;function wireSweeper(){return _sweeperWired?!0:!CBZ.mission||typeof CBZ.mission.onInterrupt!="function"?!1:(_sweeperWired=!0,CBZ.mission.onInterrupt(function(){try{holds()||standDownAll(!0)}catch{}}),!0)}let _zoneWired=!1;function wireZone(){if(_zoneWired)return!0;if(!CBZ.interactions||!CBZ.interactions.registerZone||!CBZ._militaryBase)return!1;_zoneWired=!0;const B=CBZ._militaryBase,tok={x:B.center.x,z:B.center.z,kind:"govcommand"},R=8;try{CBZ.interactions.registerZone({id:"gov-command",kind:"govcommand",radius:R,find:function(px,pz){if(!CBZ.CONFIG.GOV_MILITARY||!holds())return null;const dx=tok.x-px,dz=tok.z-pz;return dx*dx+dz*dz<R*R?tok:null},options:[{id:"gov-order-garrison",slot:"e",label:function(){const h=holds();if(!h)return"Command";const reason=martialReason(h);return reason?"Order the garrison out ("+reason+")":"Ask the garrison for a briefing"},onSelect:function(){const h=holds();if(!h)return;if(!martialReason(h)){orders(generalName(),"Nothing is burning, "+h.title+". The wire holds. Go home.",0);return}const P=CBZ.player,r=deploy("martial",P?{x:P.pos.x,z:P.pos.z}:null);!r.ok&&!r.refused&&orders(generalName(),r.why,1)}}]}),CBZ.interactions.describe&&CBZ.interactions.describe("govcommand",function(){return{label:"Command HQ",note:"Fort Brandt garrison"}})}catch{}return!0}const POWER_MOVES={salary:["rec.treasury","g.cash via CBZ.city.addCash"],pardon:["CBZ.cityWantedReset / CBZ.cityReduceWanted -> g.wanted/g.heat","approvalShock","tyranny"],guard:["CBZ.protection detail off_<id>.memberCount -> real spawned bodies","rec.treasury","militia.js escalation threshold"],surge:["police.js forcePool via cityPoliceForceAdd","rec.treasury","approvalShock"],martial:["CBZ.cityMilitaryPersonnel ped.pos/ped.guard","CBZ.polwar militaryOf().soldiers","CBZ.polwar militaryOf().readiness -> combatPower + civilwar coupEligible","rec.treasury","approvalShock"],refusal:["CBZ.polwar militaryOf().readiness -> civilwar.js coupEligible","approvalShock","tyranny"],tyranny:["legitimacy() gate on every deploy order","daily approvalShock drag"]};function audit(){let n=0;for(const k in DECREES)(DECREES[k].moves||[]).length||n++;for(const k in POWER_MOVES)POWER_MOVES[k].length||n++;return n}CBZ.gov={holds,approval:approvalOf,legitimacy,tyranny,treasury:treasuryOf,decrees,decree,pardon,deploy,deployments,audit,forceUsed:function(amount,why){addTyranny(+amount||0,why)},title:function(){const h=holds();return h?h.title:null},refusals:function(){return st().refusals||0},curfewUntil:function(){return st().curfewUntil||0},standDown:function(){return standDownAll(!1)},reset,serialize,apply,_state:st,_martialReason:martialReason,_garrison:garrison,_refuse:refuse,_dailyTick:dailyTick},CBZ.govAudit=audit,CBZ.govReset=reset;function serialize(){const S=st(),deps=[];for(let i=0;i<S.deployments.length;i++){const d=S.deployments[i];deps.push({kind:d.kind,at:{x:d.at.x,z:d.at.z},day:d.day,until:d.until,bodies:d.kind==="surge"?d.bodies|0:d.n|0})}return{v:1,seatId:S.seatId||null,seatCountryId:S.seatCountryId||null,sinceDay:S.sinceDay||0,tyranny:S.tyranny||0,refusals:S.refusals||0,skimmed:S.skimmed||0,curfewUntil:S.curfewUntil||0,lastPardonDay:S.lastPardonDay,lastSkimDay:S.lastSkimDay,lastAmnestyDay:S.lastAmnestyDay,forceAdded:S.forceAdded||0,origPoliceForce:S.origPoliceForce!=null?S.origPoliceForce:null,policeBumped:!!S.policeBumped,readinessHeld:S.readinessHeld||0,soldiersOut:S.soldiersOut||0,paidDay:S.paidDay,deployments:deps}}function apply(obj){if(reset(),!obj||obj.v!==1)return;const S=st();S.seatId=obj.seatId||null,S.seatCountryId=obj.seatCountryId||null,S.sinceDay=obj.sinceDay||0,S.tyranny=clamp(+obj.tyranny||0,0,TYRANNY_MAX),S.refusals=obj.refusals|0,S.skimmed=+obj.skimmed||0,S.curfewUntil=obj.curfewUntil||0,S.lastPardonDay=isFinite(obj.lastPardonDay)?obj.lastPardonDay:-999,S.lastSkimDay=isFinite(obj.lastSkimDay)?obj.lastSkimDay:-999,S.lastAmnestyDay=isFinite(obj.lastAmnestyDay)?obj.lastAmnestyDay:-999,S.readinessHeld=+obj.readinessHeld||0,S.soldiersOut=obj.soldiersOut|0,S.paidDay=isFinite(obj.paidDay)?obj.paidDay:-1;for(let i=0;i<(obj.deployments||[]).length;i++){const d=obj.deployments[i];S.deployments.push({kind:d.kind,at:{x:d.at.x,z:d.at.z},day:d.day,until:d.until,bodies:d.kind==="surge"?d.bodies|0:[],n:d.bodies|0})}if(S.forceAdded=0,S.origPoliceForce=isFinite(obj.origPoliceForce)?+obj.origPoliceForce:null,S.policeBumped=!!obj.policeBumped,S.policeBumped&&CBZ.CITY){const base=S.origPoliceForce!=null?S.origPoliceForce:CBZ.CITY.policeForce||40;S.origPoliceForce=base,CBZ.CITY.policeForce=Math.round(base*POLICE_MUL)}}function stampGov(){const led=g.cityWorld;led&&typeof led=="object"&&(led.gov=serialize())}let _wrapsDone=!1;function ensureSaveWraps(){if(_wrapsDone)return;_wrapsDone=!0;const c=CBZ.cityWorldCommit;if(typeof c=="function"&&!c._govWrap){const w=function(){return stampGov(),c.apply(this,arguments)};w._govWrap=!0,CBZ.cityWorldCommit=w}const cc=CBZ.cityWorldCollect;if(typeof cc=="function"&&!cc._govWrap){const w2=function(){return stampGov(),cc.apply(this,arguments)};w2._govWrap=!0,CBZ.cityWorldCollect=w2}}let _hydrated=null;function hydrate(){const led=g.cityWorld;!led||led===_hydrated||(_hydrated=led,led.gov&&apply(led.gov))}CBZ.onUpdate&&CBZ.onUpdate(46.14,function(){g&&(ensureSaveWraps(),hydrate(),wireSweeper(),wireZone())})})();
