(function(){"use strict";const CBZ=window.CBZ;if(!CBZ||!window.THREE||!CBZ.scene)return;const THREE=window.THREE,scene=CBZ.scene;if(CBZ.cityNukeFX)return;CBZ.CONFIG=CBZ.CONFIG||{},CBZ.CONFIG.NUKE_FX_V1==null&&(CBZ.CONFIG.NUKE_FX_V1=!0),CBZ.CONFIG.NUKE_FX_SHELL==null&&(CBZ.CONFIG.NUKE_FX_SHELL=!0),CBZ.CONFIG.NUKE_FX_ASH==null&&(CBZ.CONFIG.NUKE_FX_ASH=!1),CBZ.CONFIG.NUKE_FX_FOGPROOF==null&&(CBZ.CONFIG.NUKE_FX_FOGPROOF=!0),CBZ.CONFIG.NUKE_FX_SOLID_CLOUD==null&&(CBZ.CONFIG.NUKE_FX_SOLID_CLOUD=!0),CBZ.CONFIG.NUKE_FX_BIG_FORMATION==null&&(CBZ.CONFIG.NUKE_FX_BIG_FORMATION=!0),CBZ.CONFIG.NUKE_FX_AFTERMATH==null&&(CBZ.CONFIG.NUKE_FX_AFTERMATH=!0),CBZ.CONFIG.NUKE_FX_SOFT_LOBES==null&&(CBZ.CONFIG.NUKE_FX_SOFT_LOBES=!0),CBZ.CONFIG.NUKE_FX_SMOKE_LOBES==null&&(CBZ.CONFIG.NUKE_FX_SMOKE_LOBES=!0);function smokeLobes(){return CBZ.CONFIG.NUKE_FX_SMOKE_LOBES!==!1}CBZ.CONFIG.NUKE_FX_ORGANIC==null&&(CBZ.CONFIG.NUKE_FX_ORGANIC=!0);const organic=()=>!!CBZ.CONFIG.NUKE_FX_ORGANIC;function lobeHash(i){const s=Math.sin((i+1)*127.1)*43758.5453;return s-Math.floor(s)}const SALT_WIND=30481,SALT_WINDK=30482,SALT_BODY=45265;function h01(x,z,salt){return CBZ.hash01?CBZ.hash01(x,z,salt):.5}CBZ.CONFIG.NUKE_FX_SKY==null&&(CBZ.CONFIG.NUKE_FX_SKY=!0),CBZ.CONFIG.NUKE_FX_MOAB==null&&(CBZ.CONFIG.NUKE_FX_MOAB=!0),CBZ.CONFIG.BOMB_WALK_V1==null&&(CBZ.CONFIG.BOMB_WALK_V1=!0),CBZ.CONFIG.NUKE_FX_PULSE==null&&(CBZ.CONFIG.NUKE_FX_PULSE=!0),CBZ.CONFIG.NUKE_FX_VEIL==null&&(CBZ.CONFIG.NUKE_FX_VEIL=!0),CBZ.CONFIG.NUKE_FX_RISE==null&&(CBZ.CONFIG.NUKE_FX_RISE=!0),CBZ.CONFIG.NUKE_FX_ROLL==null&&(CBZ.CONFIG.NUKE_FX_ROLL=!0),CBZ.CONFIG.NUKE_FX_GLASS==null&&(CBZ.CONFIG.NUKE_FX_GLASS=!0),CBZ.CONFIG.NUKE_FX_V2==null&&(CBZ.CONFIG.NUKE_FX_V2=!0);function v2(){return CBZ.CONFIG.NUKE_FX_V2!==!1}CBZ.CONFIG.NUKE_REAL_SCALE==null&&(CBZ.CONFIG.NUKE_REAL_SCALE=!0);function real(){return v2()&&CBZ.CONFIG.NUKE_REAL_SCALE!==!1}CBZ.CONFIG.NUKE_FX_PHASED_CLOUD==null&&(CBZ.CONFIG.NUKE_FX_PHASED_CLOUD=!0);function phasedCloud(){return real()&&CBZ.CONFIG.NUKE_FX_PHASED_CLOUD!==!1}CBZ.CONFIG.NUKE_FX_COHERENT_CLOUD==null&&(CBZ.CONFIG.NUKE_FX_COHERENT_CLOUD=!0);function coherentCloud(){return phasedCloud()&&CBZ.CONFIG.NUKE_FX_COHERENT_CLOUD!==!1}CBZ.CONFIG.NUKE_FX_LEGACY_PUFFS==null&&(CBZ.CONFIG.NUKE_FX_LEGACY_PUFFS=!1);let _rs=5369207;function rng(){return _rs=_rs*1103515245+12345&2147483647,_rs/2147483647}function q01(){return CBZ.qScale?Math.max(0,Math.min(1,CBZ.qScale(0,1))):1}function floorAt(x,z){return CBZ.floorAt?CBZ.floorAt(x,z):0}function camPos(){return CBZ.camera&&CBZ.camera.position?CBZ.camera.position:null}function camFar(){const c=CBZ.camera;return c&&c.far>1?c.far:1e3}const BLOOM_MAX=.35+.9+.16;function camDist(x,y,z){const c=camPos();return c?Math.hypot(x-c.x,y-c.y,z-c.z):0}function clamp(v,a,b){return v<a?a:v>b?b:v}function ease(t){return t<=0?0:t>=1?1:t*t*(3-2*t)}const _fogTint=new THREE.Color;function makeCloudTexture(){const c=document.createElement("canvas");c.width=c.height=256;const ctx=c.getContext("2d");ctx.globalCompositeOperation="lighter";for(let i=0;i<54;i++){const a=rng()*6.2832,rr=Math.sqrt(rng())*78,px=128+Math.cos(a)*rr,py=128+Math.sin(a)*rr*.86,br=20+rng()*52,g=ctx.createRadialGradient(px,py,0,px,py,br),v=150+(rng()*90|0);g.addColorStop(0,"rgba("+v+","+v+","+v+","+(.36+rng()*.4)+")"),g.addColorStop(1,"rgba("+v+","+v+","+v+",0)"),ctx.fillStyle=g,ctx.beginPath(),ctx.arc(px,py,br,0,6.2832),ctx.fill()}ctx.globalCompositeOperation="destination-in";const m=ctx.createRadialGradient(128,128,8,128,128,128);m.addColorStop(0,"rgba(0,0,0,1)"),m.addColorStop(.62,"rgba(0,0,0,0.92)"),m.addColorStop(.88,"rgba(0,0,0,0.28)"),m.addColorStop(1,"rgba(0,0,0,0)"),ctx.fillStyle=m,ctx.fillRect(0,0,256,256);const t=new THREE.CanvasTexture(c);return t.needsUpdate=!0,t}function makeNoiseTexture(){const c=document.createElement("canvas");c.width=c.height=128;const ctx=c.getContext("2d"),img=ctx.createImageData(128,128),d=img.data;function grid(n){const g=new Float32Array(n*n);for(let i=0;i<g.length;i++)g[i]=rng();return g}const gA=grid(8),gB=grid(16);function samp(g,n,u,v){const fx=u*n,fy=v*n,x0=Math.floor(fx)%n,y0=Math.floor(fy)%n,x1=(x0+1)%n,y1=(y0+1)%n,tx=fx-Math.floor(fx),ty=fy-Math.floor(fy),a=g[y0*n+x0],b=g[y0*n+x1],e=g[y1*n+x0],f=g[y1*n+x1],sx=tx*tx*(3-2*tx),sy=ty*ty*(3-2*ty);return a+(b-a)*sx+(e+(f-e)*sx-(a+(b-a)*sx))*sy}for(let y=0;y<128;y++)for(let x=0;x<128;x++){const u=x/128,v=y/128;let n=samp(gA,8,u,v)*.65+samp(gB,16,u,v)*.35;n=clamp(n,0,1);const o=(y*128+x)*4,b=n*255|0;d[o]=b,d[o+1]=b,d[o+2]=b,d[o+3]=255}ctx.putImageData(img,0,0);const t=new THREE.CanvasTexture(c);return t.wrapS=t.wrapT=THREE.RepeatWrapping,t.needsUpdate=!0,t}const IMP_W=256,IMP_H=512;function maskHash(x,y,seed){let h=Math.imul(x|0,374761393)^Math.imul(y|0,668265263)^Math.imul(seed|0,69069);return h=Math.imul(h^h>>>13,1274126177),((h^h>>>16)>>>0)/4294967295}function maskNoise(x,y,seed){const ix=Math.floor(x),iy=Math.floor(y),fx=x-ix,fy=y-iy,sx=fx*fx*(3-2*fx),sy=fy*fy*(3-2*fy),a=maskHash(ix,iy,seed),b=maskHash(ix+1,iy,seed),c=maskHash(ix,iy+1,seed),d=maskHash(ix+1,iy+1,seed);return a+(b-a)*sx+(c+(d-c)*sx-(a+(b-a)*sx))*sy}function maskFbm(x,y){return maskNoise(x,y,91)*.54+maskNoise(x*2.07+13.1,y*2.07-7.3,173)*.3+maskNoise(x*4.19-5.7,y*4.19+11.9,251)*.16}function maskSmooth(a,b,x){const u=clamp((x-a)/Math.max(1e-5,b-a),0,1);return u*u*(3-2*u)}function makeMushroomTexture(stage){stage=stage==null?2:clamp(stage|0,0,2);const c=document.createElement("canvas");c.width=IMP_W,c.height=IMP_H;const ctx=c.getContext("2d"),D=nukeDims(126),capScale=[.15,.5,1][stage],capHScale=[.12,.55,1][stage],capY=[900,3600,D.capY][stage],capRx=D.capW*.5*capScale,capRy=D.capH*.5*capHScale,stemRx=D.stemW*.5*[.33,.68,1][stage],baseRx=D.base*.5*[.28,.62,1][stage],baseH=D.top*[.025,.045,.055][stage],shapeW=capRx*2.6,shapeTop=capY+capRy*1.12,img=ctx.createImageData(IMP_W,IMP_H),data=img.data,stemBottom=baseH*.28;for(let iy=0;iy<IMP_H;iy++){const wy=(1-(iy+.5)/IMP_H)*shapeTop;for(let ix=0;ix<IMP_W;ix++){const wx=((ix+.5)/IMP_W-.5)*shapeW,noise=maskFbm(ix/31,iy/31),fine=maskFbm(ix/12+4.7,iy/12-2.1),capDy=wy-capY,capYn=capDy>=0?capDy/Math.max(1,capRy):capDy/Math.max(1,capRy*.86),capXn=Math.abs(wx)/Math.max(1,capRx),capField=1-Math.pow(capXn,3)-Math.pow(Math.abs(capYn),2.6),collarWarp=(maskNoise(ix/41,stage*3.7+.8,417)-.5)*capRy*.16+Math.sin(wx/Math.max(1,capRx)*4.2+stage)*capRy*.035,collarY=capY-capRy*.52+collarWarp,collarField=1-Math.pow(Math.abs(wx)/Math.max(1,capRx*1.02),4)-Math.pow(Math.abs(wy-collarY)/Math.max(1,capRy*.3),2.2),stemTop=capY+capRy*.3;let stemF=clamp((wy-stemBottom)/Math.max(1,stemTop-stemBottom),0,1);const width=Math.min(stemRx*stemProfile(stemF),capRx*.74),centre=Math.sin(stemF*5.1+stage*.35)*width*.1+(maskNoise(stemF*6,1.7,331)-.5)*width*.16,lateralField=1-Math.abs(wx-centre)/Math.max(1,width),axialField=Math.min((wy-stemBottom)/Math.max(1,Math.max(baseH*.55,capRy*.1)),(stemTop-wy)/Math.max(1,Math.max(capRy*.85,baseH*.35))),stemField=Math.min(lateralField,axialField),stemJoin=maskSmooth(-.06,.34,axialField),stemAxial=clamp(lateralField,0,1)*stemJoin,baseField=1-Math.pow(Math.abs(wx)/Math.max(1,baseRx),2.6)-Math.pow(Math.abs(wy-baseH*.38)/Math.max(1,baseH*.62),2.2),structure=Math.max(capField,collarField,stemField,baseField),edgeK=1-Math.min(1,Math.abs(structure)),field=structure+(noise-.5)*(.24+.44*edgeK);let alpha=maskSmooth(-.16,.18,field);if(alpha*=.82+fine*.18,alpha<=.002)continue;const capMask=maskSmooth(-.14,.3,Math.max(capField,collarField)),stemMask=maskSmooth(-.08,.3,stemField),baseMask=maskSmooth(-.1,.28,baseField),capBand=1-Math.min(1,Math.abs(capDy+capRy*.04)/Math.max(1,capRy*.58)),capCore=(1-Math.min(1,capXn/.86))*capBand,stemHeat=Math.pow(stemAxial,1.65)*(.58+noise*.5),hot=clamp(Math.max(capMask*capCore*(1-stage*.1),stemMask*stemHeat*(.38+stemF*.62))*(.84+fine*.2),0,1),capCool=capMask*clamp(.08+.62*Math.max(0,capYn)+.32*capXn,0,1),stemCool=stemMask*clamp(.2+(1-stemAxial)*.52+(1-stemF)*.18,0,1),cool=clamp(Math.max(baseMask*.94,capCool,stemCool),0,1),o=(iy*IMP_W+ix)*4;data[o]=Math.round(hot*255),data[o+1]=Math.round(cool*255),data[o+2]=0,data[o+3]=Math.round(clamp(alpha,0,1)*255)}}ctx.putImageData(img,0,0);for(let i=0;i<898;i++)rng();const t=new THREE.CanvasTexture(c);return t.wrapS=t.wrapT=THREE.ClampToEdgeWrapping,t.generateMipmaps=!1,t.minFilter=THREE.LinearFilter,t.magFilter=THREE.LinearFilter,t.needsUpdate=!0,t}function stemProfile(f){return f=clamp(f,0,1),(1+.9*Math.pow(1-f,2.4))*(1+.25*Math.pow(f,3))}const RAMP=[[0,"#fffdf2"],[.06,"#ffeda6"],[.14,"#ffc25a"],[.26,"#ff8a2e"],[.4,"#d1512a"],[.55,"#8a5340"],[.7,"#6b6157"],[.85,"#575049"],[1,"#3d3934"]];function makeLutTexture(){const c=document.createElement("canvas");c.width=64,c.height=1;const ctx=c.getContext("2d"),g=ctx.createLinearGradient(0,0,64,0);for(const s of RAMP)g.addColorStop(s[0],s[1]);ctx.fillStyle=g,ctx.fillRect(0,0,64,1);const t=new THREE.CanvasTexture(c);return t.generateMipmaps=!1,t.minFilter=THREE.LinearFilter,t.magFilter=THREE.LinearFilter,t.wrapS=t.wrapT=THREE.ClampToEdgeWrapping,t.needsUpdate=!0,t}const BLUE_WHITE=new THREE.Color(14084351),RAMP_C=RAMP.map(function(s){return new THREE.Color(s[1])});function rampColor(out,t){t=clamp(t,0,1);for(let i=1;i<RAMP.length;i++)if(t<=RAMP[i][0]){const p=(t-RAMP[i-1][0])/(RAMP[i][0]-RAMP[i-1][0]||1);return out.copy(RAMP_C[i-1]).lerp(RAMP_C[i],p),out}return out.copy(RAMP_C[RAMP_C.length-1])}const TEX={cloud:null,noise:null,lut:null,mushEarly:null,mushForm:null,mush:null,blastFlame:null,blastSmoke:null};function FOG_U(extra){return Object.assign({fogColor:{value:new THREE.Color(11977928)},fogNear:{value:95},fogFar:{value:360},fogDensity:{value:25e-5}},extra)}const TAIL_FOG=["  #include <tonemapping_fragment>","  #include <encodings_fragment>","  #include <fog_fragment>"].join(`
`),TAIL_FOG_ADD=["  #ifdef USE_FOG","    #ifdef FOG_EXP2","      gl_FragColor.a *= 1.0 - 0.85 * clamp(1.0 - exp(-fogDensity * fogDensity * fogDepth * fogDepth), 0.0, 1.0);","    #else","      gl_FragColor.a *= 1.0 - 0.85 * smoothstep(fogNear, fogFar, fogDepth);","    #endif","  #endif","  #include <tonemapping_fragment>","  #include <encodings_fragment>"].join(`
`),SHELL_VS=["#include <fog_pars_vertex>","uniform float uBoil; uniform float uTime;","varying vec3 vN; varying vec3 vV; varying vec3 vW;","void main() {","  float b = uBoil * (","      sin(position.x * 7.3 + uTime * 1.7) * sin(position.y * 6.1 - uTime * 1.3) * 0.055","    + sin(position.y * 11.7 + uTime * 2.3) * sin(position.z * 9.9 + uTime * 1.1) * 0.032","    + sin((position.x + position.z) * 17.3 - uTime * 3.1) * 0.018);","  vec3 sp = position + normal * b;","  vec4 mvPosition = modelViewMatrix * vec4(sp, 1.0);","  vN = normalize(normalMatrix * normal);","  vV = normalize(-mvPosition.xyz);","  vW = (modelMatrix * vec4(sp, 1.0)).xyz;","  gl_Position = projectionMatrix * mvPosition;","  #include <fog_vertex>","}"].join(`
`);function shellFs(additive){return["#include <fog_pars_fragment>","uniform vec3 uRimColor; uniform vec3 uCoreColor;","uniform float uOpacity; uniform float uRimPow; uniform float uCore;","uniform float uMottle; uniform float uTime;","varying vec3 vN; varying vec3 vV; varying vec3 vW;","void main() {","  float f = 1.0 - abs(dot(normalize(vN), normalize(vV)));","  f = pow(clamp(f, 0.0, 1.0), uRimPow);","  vec3 c = mix(uCoreColor, uRimColor, f);","  float mo = 0.5 + 0.5 * sin(vW.x * 0.09 + uTime * 1.9) * sin(vW.y * 0.075 - uTime * 1.4);","  mo = mix(mo, 0.5 + 0.5 * sin(vW.z * 0.24 - uTime * 2.6) * sin(vW.x * 0.21 + uTime * 1.6), 0.42);","  c *= 1.0 - uMottle * 0.45 * mo;","  float a = (uCore + (1.0 - uCore) * f) * uOpacity;","  if (a <= 0.003) discard;","  gl_FragColor = vec4(c, a);",additive?TAIL_FOG_ADD:TAIL_FOG,"}"].join(`
`)}function makeShellMat(additive){const m=new THREE.ShaderMaterial({uniforms:FOG_U({uRimColor:{value:new THREE.Color(16774096)},uCoreColor:{value:new THREE.Color(16756820)},uOpacity:{value:0},uRimPow:{value:1.7},uCore:{value:.35},uBoil:{value:0},uTime:{value:0},uMottle:{value:0}}),vertexShader:SHELL_VS,fragmentShader:shellFs(additive),transparent:!0,depthWrite:!1,depthTest:!0,fog:!0,side:THREE.DoubleSide,blending:additive?THREE.AdditiveBlending:THREE.NormalBlending});return m._shared=!0,m}const BILL_VS=["#include <fog_pars_vertex>","varying vec2 vUv;","void main() {","  vUv = uv;","  vec4 mvPosition = modelViewMatrix * vec4(position, 1.0);","  gl_Position = projectionMatrix * mvPosition;","  #include <fog_vertex>","}"].join(`
`),BILL_FS=["#include <fog_pars_fragment>","uniform sampler2D uMask; uniform sampler2D uNoise; uniform sampler2D uLut;","uniform vec2 uScroll; uniform vec2 uScroll2;","uniform float uLife; uniform float uOpacity; uniform float uErode; uniform float uGlow;","uniform float uCool;","varying vec2 vUv;","void main() {","  vec4 m = texture2D(uMask, vUv);","  float n1 = texture2D(uNoise, vUv * 2.1 + uScroll).r;","  float n2 = texture2D(uNoise, vUv * 0.9 + uScroll2).r;","  float d = m.a * (0.42 + 0.78 * n1) * (0.55 + 0.7 * n2);","  float a = smoothstep(uErode, uErode + 0.30, d) * uOpacity;","  if (a <= 0.004) discard;","  float life = clamp(uLife + uCool * m.g * 0.55, 0.02, 0.98);","  vec3 c = texture2D(uLut, vec2(life, 0.5)).rgb;","  c *= 0.72 + uGlow * m.r * (0.6 + 0.7 * n1);","  gl_FragColor = vec4(c, a);",TAIL_FOG,"}"].join(`
`),IMP_FS=["#include <fog_pars_fragment>","uniform sampler2D uMask; uniform sampler2D uMaskB; uniform sampler2D uMaskC;","uniform sampler2D uNoise; uniform sampler2D uLut;","uniform vec2 uScroll; uniform vec2 uScroll2;","uniform float uLife; uniform float uOpacity; uniform float uErode; uniform float uGlow;","uniform float uCool; uniform float uPhase;","varying vec2 vUv;","void main() {","  float p = clamp(uPhase, 0.0, 1.0);","  vec4 a0 = texture2D(uMask, vUv);","  vec4 a1 = texture2D(uMaskB, vUv);","  vec4 a2 = texture2D(uMaskC, vUv);","  vec4 m = p < 0.5","    ? mix(a0, a1, smoothstep(0.0, 0.5, p))","    : mix(a1, a2, smoothstep(0.5, 1.0, p));","  float n1 = texture2D(uNoise, vUv * 2.1 + uScroll).r;","  float n2 = texture2D(uNoise, vUv * 0.9 + uScroll2).r;","  float d = m.a * (0.42 + 0.78 * n1) * (0.55 + 0.7 * n2);","  float alpha = smoothstep(uErode, uErode + 0.30, d) * uOpacity;","  if (alpha <= 0.004) discard;","  float life = clamp(uLife + uCool * m.g * 0.55, 0.02, 0.98);","  vec3 c = texture2D(uLut, vec2(life, 0.5)).rgb;","  float roil = clamp(n1 * 0.62 + n2 * 0.38, 0.0, 1.0);","  c *= 0.58 + 0.78 * roil;","  c *= 1.0 - 0.28 * m.g;","  c *= 0.72 + uGlow * m.r * (0.6 + 0.7 * n1);","  gl_FragColor = vec4(c, alpha);",TAIL_FOG,"}"].join(`
`),POOL={shell:null,dome:null,bills:[],wdome:null,imp:null,capVol:null,stemVol:null,surgeVol:null,hotVol:null,crownVol:null,glowVol:null},MAX_BILLS=5,VOL_MAX=smokeLobes()?{cap:88,stem:organic()?64:48,surge:organic()?88:76,hot:10,crown:60,glow:8}:{cap:18,stem:10,surge:20,hot:10,crown:14,glow:8},CROWN_N=smokeLobes()?34:8,VOL_SEED={cap:[],stem:[],surge:[],hot:[],crown:[],glow:[]};let VOL_BODY=0;function seedVolumes(seed){if(VOL_SEED.cap.length&&seed==null)return;let _rsSaved=0;seed!=null&&(_rsSaved=_rs,_rs=(seed|0)&2147483647,_rs||(_rs=5369207),VOL_SEED.cap.length=0,VOL_SEED.stem.length=0,VOL_SEED.surge.length=0,VOL_SEED.hot.length=0,VOL_SEED.crown.length=0,VOL_SEED.glow.length=0);for(let i=0;i<VOL_MAX.cap;i++){const a=i?i*2.399963+rng()*.24:0,rr=i?Math.sqrt(rng())*.86:0;VOL_SEED.cap.push({a,r:rr,y:i?(rng()-.42)*.48:.08,s:i?.16+rng()*.12:.34,spin:(rng()-.5)*.28,s2:(.34-.2*Math.pow(rr,1.4))*(.82+rng()*.36),y2:i?rng()*1.7-.85:.1})}for(let i=0;i<VOL_MAX.stem;i++)VOL_SEED.stem.push({f:(i+.45)/VOL_MAX.stem,a:i*2.399963+rng()*.35,r:.08+rng()*.28,s:.78+rng()*.42,r2:.22+Math.sqrt(rng())*.72,s2:.34+rng()*.26,tw:(rng()-.5)*.8});for(let i=0;i<VOL_MAX.surge;i++)VOL_SEED.surge.push({a:i*2.399963+rng()*.42,r:Math.sqrt((i+.6)/VOL_MAX.surge)*(.82+rng()*.18),s:.7+rng()*.55});for(let i=0;i<VOL_MAX.hot;i++){const a=i*2.399963+rng()*.3;VOL_SEED.hot.push({a,r:.18+Math.sqrt(rng())*.72,y:(rng()-.35)*.9,s:.14+rng()*.12})}for(let i=0;i<VOL_MAX.crown;i++)if(i<CROWN_N){const r=Math.sqrt((i+.35)/CROWN_N)*.92;VOL_SEED.crown.push({crown:!0,a:i*2.399963+rng()*.3,r,y:Math.sqrt(Math.max(0,1-r*r)),s:.15+rng()*.13,spin:(rng()-.5)*.22})}else{const k=(i-CROWN_N+.5)/(VOL_MAX.crown-CROWN_N);VOL_SEED.crown.push({crown:!1,a:k*6.2832+rng()*.5,r:.7+rng()*.28,y:-(.3+rng()*.26),s:.17+rng()*.12,spin:(rng()-.5)*.16})}for(let i=0;i<VOL_MAX.glow;i++)VOL_SEED.glow.push({a:i*2.399963+rng()*.4,r:Math.sqrt(rng())*.62,y:(rng()-.45)*.55,s:.22+rng()*.16});seed!=null&&(_rs=_rsSaved)}function park(mesh,order){return mesh.visible=!1,mesh.frustumCulled=!1,mesh.renderOrder=order,mesh.matrixAutoUpdate=!0,mesh.userData.nukefx=!0,scene.add(mesh),mesh}const SMOKE_U={uSmokeSun:{value:new THREE.Vector3(.35,.86,.37)},uSmokeSunCol:{value:new THREE.Color(16774368)},uSmokeAmb:{value:new THREE.Color(6122109)},uSmokeMask:{value:null},uSmokeSpan:{value:new THREE.Vector2(0,1)},uSmokeNoise:{value:null},uSmokeCore:{value:new THREE.Vector3(0,0,0)},uSmokeCapR:{value:1},uSmokeCam:{value:new THREE.Vector3(0,0,0)},uSmokeHaze:{value:new THREE.Color(11977928)},uSmokeGlow:{value:new THREE.Vector2(1,0)},uSmokeTime:{value:0}};function buildPool(){TEX.cloud=makeCloudTexture(),TEX.noise=makeNoiseTexture(),TEX.lut=makeLutTexture();const blastAssets=CBZ.cityBlastPuffAssets?CBZ.cityBlastPuffAssets():null;TEX.blastFlame=blastAssets?blastAssets.flame:TEX.cloud,TEX.blastSmoke=blastAssets?blastAssets.smoke:TEX.cloud,TEX.blastSmoke&&TEX.blastSmoke.clone?(TEX.smokeMask=TEX.blastSmoke.clone(),TEX.smokeMask.wrapS=TEX.smokeMask.wrapT=THREE.RepeatWrapping,TEX.smokeMask.needsUpdate=!0):TEX.smokeMask=TEX.blastSmoke,SMOKE_U.uSmokeMask.value=TEX.smokeMask,SMOKE_U.uSmokeNoise.value=TEX.noise,TEX.mushEarly=makeMushroomTexture(0),TEX.mushForm=makeMushroomTexture(1),TEX.mush=makeMushroomTexture(2);const sphereGeo=new THREE.IcosahedronGeometry(1,2);sphereGeo._shared=!0;const billowGeo=new THREE.IcosahedronGeometry(1,2),bp=billowGeo.attributes.position,rk=smokeLobes()?.34:1;for(let bi=0;bi<bp.count;bi++){const bx=bp.getX(bi),by=bp.getY(bi),bz=bp.getZ(bi),rough=1+rk*(.1*Math.sin(bx*7.1+by*3.7)*Math.sin(bz*8.3-by*4.1)+.055*Math.sin((bx+bz)*13.7+by*9.2));bp.setXYZ(bi,bx*rough,by*rough,bz*rough)}bp.needsUpdate=!0,billowGeo.computeVertexNormals(),billowGeo._shared=!0;const quadGeo=new THREE.PlaneGeometry(1,1);quadGeo._shared=!0;const shellMat=makeShellMat(!0),fireGeo=organic()?new THREE.IcosahedronGeometry(1,3):sphereGeo;fireGeo._shared=!0,POOL.shell=park(new THREE.Mesh(fireGeo,shellMat),8);const domeMat=makeShellMat(!1);domeMat.uniforms.uRimColor.value.set(16777215),domeMat.uniforms.uCoreColor.value.set(14674162),domeMat.uniforms.uRimPow.value=2.6,domeMat.uniforms.uCore.value=.06,POOL.dome=park(new THREE.Mesh(sphereGeo,domeMat),9),seedVolumes();function volumeMat(color,emissive,opacity,rimFloor,role){const smoke=smokeLobes(),m=new THREE.MeshLambertMaterial({color,emissive,emissiveIntensity:1,transparent:!0,opacity,depthWrite:!smoke,depthTest:!0,fog:!CBZ.CONFIG.NUKE_FX_FOGPROOF,side:THREE.FrontSide,flatShading:!1});if(smoke){const org=organic(),roleId=role??0;return m.customProgramCacheKey=function(){return org?"cbzSmokeLobes2":"cbzSmokeLobes1"},m.onBeforeCompile=function(shader){shader.uniforms.uSmokeSun=SMOKE_U.uSmokeSun,shader.uniforms.uSmokeSunCol=SMOKE_U.uSmokeSunCol,shader.uniforms.uSmokeAmb=SMOKE_U.uSmokeAmb,shader.uniforms.uSmokeMask=SMOKE_U.uSmokeMask,shader.uniforms.uSmokeSpan=SMOKE_U.uSmokeSpan,shader.uniforms.uSmokeNoise=SMOKE_U.uSmokeNoise,shader.uniforms.uSmokeCore=SMOKE_U.uSmokeCore,shader.uniforms.uSmokeCapR=SMOKE_U.uSmokeCapR,shader.uniforms.uSmokeCam=SMOKE_U.uSmokeCam,shader.uniforms.uSmokeHaze=SMOKE_U.uSmokeHaze,shader.uniforms.uSmokeGlow=SMOKE_U.uSmokeGlow,shader.uniforms.uSmokeTime=SMOKE_U.uSmokeTime,shader.uniforms.uSmokeRole={value:roleId},shader.vertexShader=shader.vertexShader.replace("#include <common>",`#include <common>
varying vec3 vSmN;
varying vec3 vSmV;
varying vec3 vSmW;
varying vec3 vSmO;
varying vec3 vSmSeed;`).replace("#include <defaultnormal_vertex>",`#include <defaultnormal_vertex>
vSmN = transformedNormal;`).replace("#include <project_vertex>",`#include <project_vertex>
vSmV = -mvPosition.xyz;
vSmO = transformed;
vSmSeed = vec3(0.0);
vSmW = (modelMatrix * (
#ifdef USE_INSTANCING
 instanceMatrix *
#endif
 vec4(transformed, 1.0))).xyz;
#ifdef USE_INSTANCING
 vSmSeed = vec3(instanceMatrix[3][0], instanceMatrix[3][1], instanceMatrix[3][2]);
#endif
`),shader.fragmentShader=shader.fragmentShader.replace("#include <common>",`#include <common>
varying vec3 vSmN;
varying vec3 vSmV;
varying vec3 vSmW;
varying vec3 vSmO;
varying vec3 vSmSeed;
uniform vec3 uSmokeSun;
uniform vec3 uSmokeSunCol;
uniform vec3 uSmokeAmb;
uniform sampler2D uSmokeMask;
uniform vec2 uSmokeSpan;
`+(org?`uniform sampler2D uSmokeNoise;
uniform vec3 uSmokeCore;
uniform float uSmokeCapR;
uniform vec3 uSmokeCam;
uniform vec3 uSmokeHaze;
uniform vec2 uSmokeGlow;
uniform float uSmokeTime;
uniform float uSmokeRole;
`:"")+`float smNoise;
float smMask;
float smRim;
float smFine;`).replace("#include <tonemapping_fragment>",`vec3 smN = normalize(vSmN);
vec3 smV = normalize(vSmV);
smRim = abs(dot(smN, smV));
float smA1 = sin(vSmW.x * 0.0041 + vSmW.y * 0.0062) * sin(vSmW.z * 0.0051 - vSmW.y * 0.0039);
float smA2 = sin(vSmW.x * 0.0118 - vSmW.y * 0.0089) * sin(vSmW.z * 0.0104 + vSmW.x * 0.0077);
float smA3 = sin(vSmW.x * 0.0472 + vSmW.z * 0.0388) * sin(vSmW.y * 0.0431 - vSmW.x * 0.0295);
`+(org?`vec2 smQ1 = vec2(vSmW.x + vSmW.z, vSmW.y - vSmW.z) * 0.00045 + uSmokeTime * vec2(0.006, -0.004);
vec2 smQ2 = vec2(vSmW.x - vSmW.y, vSmW.z + vSmW.y * 0.6) * 0.00238 + uSmokeTime * vec2(-0.011, 0.008);
float smT1 = (texture2D(uSmokeNoise, smQ1).r - 0.5) * 2.0;
smFine = texture2D(uSmokeNoise, smQ2).r;
smNoise = clamp(0.5 + 0.44 * smT1 + 0.28 * (smFine - 0.5) * 2.0 + 0.12 * smA1 + 0.10 * smA3, 0.0, 1.0);
`:`smNoise = 0.5 + 0.27 * smA1 + 0.15 * smA2 + 0.14 * smA3;
smFine = 0.5;
`)+`vec2 smOff = vec2(vSmSeed.x + vSmSeed.y, vSmSeed.z - vSmSeed.y) * 0.0037;
float smM1 = texture2D(uSmokeMask, vSmO.xy * 0.62 + smOff).a;
float smM2 = texture2D(uSmokeMask, vSmO.zy * 0.62 + smOff.yx).a;
smMask = 0.5 * (smM1 + smM2);
vec3 smL = normalize((viewMatrix * vec4(uSmokeSun, 0.0)).xyz);
float smWrap = dot(smN, smL) * 0.5 + 0.5;
float smFwd = pow(max(0.0, dot(-smV, smL)), 3.0);
float smH = clamp((vSmW.y - uSmokeSpan.x) / max(1.0, uSmokeSpan.y - uSmokeSpan.x), 0.0, 1.0);
`+(org?`float smDeep = max(step(uSmokeRole, 0.5), step(2.5, uSmokeRole));
float smStemR = step(0.5, uSmokeRole) * step(uSmokeRole, 1.5);
float smLow = mix(mix(0.54, 0.62, smStemR), 0.45, smDeep);
smH = smLow + (1.0 - smLow) * smoothstep(0.0, 0.85, smH);
`:`smH = 0.45 + 0.55 * smoothstep(0.0, 0.85, smH);
`)+`float smShade = 0.30 * smWrap * smWrap + 0.70 * smH;
vec3 smLit = min(vec3(1.15), uSmokeAmb * (0.62 + 0.38 * smH) + uSmokeSunCol * (smShade * 0.90 + 0.10 + smFwd * 0.50));
`+(org?`vec3 smC = vSmW - uSmokeCore;
float smCapR = max(1.0, uSmokeCapR);
float smRad = length(smC.xz) / smCapR;
float smCapV = smC.y / (smCapR * 0.9);
float smCapIn = 1.0 - smoothstep(0.55, 1.15, smRad);
float smShadow = mix(mix(0.28, 0.45, smStemR), 1.0, smDeep);
smLit *= mix(1.0, mix(0.65, 1.12, smoothstep(-0.75, 0.30, smCapV)), smCapIn * smShadow);
float smJraw = texture2D(uSmokeNoise, vSmSeed.xz * 0.00111 + vSmSeed.y * 0.00032).r;
float smJit = clamp(0.5 + (smJraw - 0.5) * 2.2, 0.0, 1.0);
vec3 smJitK = (0.84 + 0.32 * smJit) * mix(vec3(1.0), vec3(1.06, 0.99, 0.92), (smJit - 0.5) * 2.0);
float smGz = length(vSmW.xz - uSmokeCore.xz);
float smBase = (1.0 - smoothstep(uSmokeGlow.x * 0.35, uSmokeGlow.x, smGz)) *
  (1.0 - smoothstep(uSmokeSpan.x + 40.0, uSmokeSpan.x + 260.0, vSmW.y));
float smEmb = mix(1.0, smBase, clamp(uSmokeGlow.y, 0.0, 1.0));
`:"")+"gl_FragColor.rgb = diffuse * smLit"+(org?" * smJitK":"")+" * (0.70 + 0.60 * smNoise) + emissive"+(org?" * smEmb":"")+`;
`+(org?`float smAir = 1.0 - exp(-length(vSmW - uSmokeCam) * 0.000035);
gl_FragColor.rgb = mix(gl_FragColor.rgb, uSmokeHaze, smAir * 0.55);
`:"")+"#include <tonemapping_fragment>").replace("#include <dithering_fragment>",`float smBody = smoothstep(0.0, 0.42, smRim);
float smDen = smBody * (0.42 + 1.16 * smMask) * (0.70 + 0.60 * smNoise);
`+(org?`smDen *= 0.80 + 0.40 * smFine;
`:"")+(org?`smDen *= smoothstep(0.06, 0.40, smDen);
`:`smDen *= smoothstep(0.05, 0.34, smDen);
`)+`gl_FragColor.a *= clamp(smDen, 0.0, 1.0);
#include <dithering_fragment>`)},m._shared=!0,m}if(CBZ.CONFIG.NUKE_FX_SOFT_LOBES){const floor=rimFloor??.22;m.customProgramCacheKey=function(){return"cbzSoftLobes"+floor.toFixed(2)},m.onBeforeCompile=function(shader){shader.vertexShader=shader.vertexShader.replace("#include <common>",`#include <common>
varying vec3 vSoftN;
varying vec3 vSoftV;
varying vec3 vSoftW;`).replace("#include <defaultnormal_vertex>",`#include <defaultnormal_vertex>
vSoftN = transformedNormal;`).replace("#include <project_vertex>",`#include <project_vertex>
vSoftV = -mvPosition.xyz;
vSoftW = (modelMatrix * (
#ifdef USE_INSTANCING
 instanceMatrix *
#endif
 vec4(transformed, 1.0))).xyz;`),shader.fragmentShader=shader.fragmentShader.replace("#include <common>",`#include <common>
varying vec3 vSoftN;
varying vec3 vSoftV;
varying vec3 vSoftW;`).replace("#include <dithering_fragment>",`float softRim = abs(dot(normalize(vSoftN), normalize(vSoftV)));
float softN1 = sin(vSoftW.x * 0.0041 + vSoftW.y * 0.0062) * sin(vSoftW.z * 0.0051 - vSoftW.y * 0.0039);
float softN2 = sin(vSoftW.x * 0.0118 - vSoftW.y * 0.0089) * sin(vSoftW.z * 0.0104 + vSoftW.x * 0.0077);
float softNoise = 0.5 + 0.32 * softN1 + 0.18 * softN2;
gl_FragColor.rgb *= 0.78 + 0.44 * softNoise;
gl_FragColor.a *= (${floor.toFixed(2)} + ${(1-floor).toFixed(2)} * smoothstep(0.05, 0.85, softRim)) * (0.70 + 0.44 * softNoise);
#include <dithering_fragment>`)}}return m._shared=!0,m}function volumeMesh(name,count,material,order){const mesh=new THREE.InstancedMesh(billowGeo,material,count);return mesh.name="nuke-"+name,mesh.instanceMatrix.setUsage(THREE.DynamicDrawUsage),mesh.count=0,park(mesh,order)}POOL.surgeVol=volumeMesh("ground-cloud",VOL_MAX.surge,volumeMat(7627092,1772807,.82,.45,2),4),POOL.stemVol=volumeMesh("stem",VOL_MAX.stem,volumeMat(4930097,2363658,.9,.6,1),5.1),POOL.capVol=volumeMesh("cap",VOL_MAX.cap,volumeMat(5980208,3150344,.88,null,0),5.2);const hotMat=new THREE.MeshBasicMaterial({color:16747040,transparent:!0,opacity:0,depthWrite:!1,depthTest:!0,fog:!CBZ.CONFIG.NUKE_FX_FOGPROOF,side:THREE.FrontSide,blending:THREE.AdditiveBlending});hotMat._shared=!0,POOL.hotVol=volumeMesh("hot-billows",VOL_MAX.hot,hotMat,smokeLobes()?5.05:7),POOL.crownVol=volumeMesh("cap-crown",VOL_MAX.crown,volumeMat(3879209,1378307,.92,null,3),5.3);const glowMat=new THREE.MeshBasicMaterial({color:16765562,transparent:!0,opacity:0,depthWrite:!1,depthTest:!0,fog:!CBZ.CONFIG.NUKE_FX_FOGPROOF,side:THREE.FrontSide,blending:THREE.AdditiveBlending});glowMat._shared=!0,POOL.glowVol=volumeMesh("cap-glow",VOL_MAX.glow,glowMat,smokeLobes()?5.15:5.25);const hemiGeo=new THREE.SphereGeometry(1,30,15,0,Math.PI*2,0,Math.PI*.5);hemiGeo._shared=!0;const wdMat=new THREE.MeshBasicMaterial({color:16777215,transparent:!0,opacity:0,depthWrite:!1,depthTest:!0,fog:!1,side:THREE.DoubleSide,blending:THREE.NormalBlending});wdMat._shared=!0,POOL.wdome=park(new THREE.Mesh(hemiGeo,wdMat),11);for(let i=0;i<MAX_BILLS;i++){const mat=new THREE.ShaderMaterial({uniforms:FOG_U({uMask:{value:TEX.cloud},uNoise:{value:TEX.noise},uLut:{value:TEX.lut},uScroll:{value:new THREE.Vector2(0,0)},uScroll2:{value:new THREE.Vector2(0,0)},uLife:{value:.3},uOpacity:{value:0},uErode:{value:.18},uGlow:{value:.5},uCool:{value:0}}),vertexShader:BILL_VS,fragmentShader:BILL_FS,transparent:!0,depthWrite:!1,depthTest:!0,fog:!0,side:THREE.DoubleSide,blending:THREE.NormalBlending});mat._shared=!0,POOL.bills.push(park(new THREE.Mesh(quadGeo,mat),5))}const impMat=new THREE.ShaderMaterial({uniforms:FOG_U({uMask:{value:TEX.mushEarly},uMaskB:{value:TEX.mushForm},uMaskC:{value:TEX.mush},uNoise:{value:TEX.noise},uLut:{value:TEX.lut},uScroll:{value:new THREE.Vector2(0,0)},uScroll2:{value:new THREE.Vector2(0,0)},uLife:{value:.12},uOpacity:{value:0},uErode:{value:.1},uGlow:{value:1.55},uCool:{value:1},uPhase:{value:0}}),vertexShader:BILL_VS,fragmentShader:IMP_FS,transparent:!0,depthWrite:!1,depthTest:!0,fog:!1,side:THREE.DoubleSide,blending:THREE.NormalBlending});impMat._shared=!0,POOL.imp=park(new THREE.Mesh(quadGeo,impMat),4.5)}const FLASH_DOUBLE=[[0,1],[.028,.78],[.062,.09],[.105,.12],[.185,1],[.4,.82],[.68,.4],[1,0]],FLASH_SINGLE=[[0,1],[.22,.45],[1,0]];function keyAt(keys,u){u=clamp(u,0,1);for(let i=1;i<keys.length;i++)if(u<=keys[i][0]){const a=keys[i-1],b=keys[i];return a[1]+(b[1]-a[1])*((u-a[0])/(b[0]-a[0]||1))}return 0}function flashRadiance(t,P){if(!CBZ.CONFIG.NUKE_FX_PULSE||!P.dbl)return 1;const w=P.white*.72;return t>=w||w<=0?1:1-(1-t/w)*(1-keyAt(FLASH_DOUBLE,t/P.white))*.9}let flashEl=null,flash=null;function flashDiv(){return typeof document>"u"||!document.body?null:(flashEl&&flashEl.parentNode||(flashEl=document.getElementById("nukeFlash"),flashEl||(flashEl=document.createElement("div"),flashEl.id="nukeFlash",flashEl.style.cssText="position:fixed;inset:0;background:#fff;opacity:0;pointer-events:none;z-index:80",document.body.appendChild(flashEl)),flashEl.style.transition="none"),flashEl)}function whiteout(fadeSec,peak,dbl){const el=flashDiv();if(!el)return;const dur=Math.max(.12,fadeSec==null?2.8:+fadeSec||.12),pk=peak==null?1:clamp(+peak||0,0,1);if(!(flash&&flash.peak>pk&&flash.t<flash.dur*.5)){flash={t:0,dur,peak:pk,keys:dbl?FLASH_DOUBLE:FLASH_SINGLE};try{el.style.opacity=String(pk)}catch{}}}function stepFlash(dt){const f=flash;f.t+=dt;const el=flashDiv(),u=clamp(f.t/f.dur,0,1),v=keyAt(f.keys,u);if(el)try{el.style.opacity=v<=.002?"0":(v*f.peak).toFixed(3)}catch{}u>=1&&(flash=null)}function flashClear(){flash=null;const el=flashEl;if(el)try{el.style.transition="none",el.style.opacity="0"}catch{}}CBZ.cityNukeWhiteout=whiteout;function fireR(row,opts){const pw=Math.max(.1,+row.power||1),rr=Math.max(1,+row.radius||14),sc=opts&&opts.scale>0?+opts.scale:1;return Math.max(8,rr*pw*sc)}function yieldKt(R){return Math.pow(Math.max(1,R)/50,3)}function cubeRt(W){return Math.pow(Math.max(.001,W),1/3)}const CLOUD_TOP_M=1e4,CAP_DIA_20KT=5500,CAP_THICK_20KT=4300,STEM_OF_CAP=1/3;function solidOp(peak,smokePeak){return smokeLobes()?Math.min(1,smokePeak??peak*.68):CBZ.CONFIG.NUKE_FX_SOLID_CLOUD===!1?peak:Math.min(1,peak*1.45)}function nukeDims(R){const W=yieldKt(R),k=Math.pow(W/20,1/3),capW=CAP_DIA_20KT*k,capH=CAP_THICK_20KT*k,top=CLOUD_TOP_M;return{W,fireball:R,capW,capH,capY:top-capH*.5,top,stemW:capW*STEM_OF_CAP,base:RING_1KT.p2*cubeRt(W)}}function formationDims(R){if(!CBZ.CONFIG.NUKE_FX_BIG_FORMATION){const capW0=R*3.6,capH0=capW0*.78,capY0=R*5.2;return{capW:capW0,capH:capH0,capY:capY0,top:capY0+capH0*.5*CAP_FLAT,stemW:capW0*STEM_OF_CAP,base:R*4}}const capW=R*11,capH=capW*.78,capY=R*20;return{capW,capH,capY,top:capY+capH*.5*CAP_FLAT,stemW:capW*STEM_OF_CAP,base:R*4}}const RING_1KT={p20:200,p10:300,p5:440,p2:800,p1:1300},USSBS=[[.5,.86],[1,.83],[1.5,.51],[2,.216],[2.5,.049],[3,.024],[4,.003],[5,0]],HIROSHIMA_KT=15;function nukeRings(R){const W=yieldKt(R),k=cubeRt(W),hk=Math.pow(W/HIROSHIMA_KT,1/3);return{W:+W.toFixed(2),fireball:R,psi20:RING_1KT.p20*k,psi10:RING_1KT.p10*k,psi5:RING_1KT.p5*k,psi2:RING_1KT.p2*k,psi1:RING_1KT.p1*k,fatal:USSBS.map(function(b){return[b[0]*1e3*hk,b[1]]}),rad500:1100*Math.pow(W/20,.2)}}function nukeLethalAt(r,R){const T=nukeRings(R);if(r<=T.fireball)return 1;const f=T.fatal;let prev=T.fireball,prevV=1;for(let i=0;i<f.length;i++){if(r<=f[i][0]){const u=(r-prev)/Math.max(1,f[i][0]-prev);return Math.max(0,prevV+(f[i][1]-prevV)*u)}prev=f[i][0],prevV=f[i][1]}return 0}CBZ.nukeYield=function(R){return yieldKt(R??126)},CBZ.nukeRings=function(R){return nukeRings(R??126)},CBZ.nukeLethalAt=function(r,R){return nukeLethalAt(r,R??126)},CBZ.nukeDims=function(R){return nukeDims(R??126)};const STYLE={nuke:{rFrac:1,riseK:7.2,capK:2.75,stemK:.28,surgeK:3.4,thermK:1.25,riseT:13,dur:34,white:2.9,whitePeak:1,dbl:!0,bills:5,dome:!0,volume:!0,ash:!0,secondary:6,shatter:4,thermal:9,shake:9,frontLife:7.5,glow:.85},moab:{rFrac:.35,riseK:4,capK:1.85,stemK:.3,surgeK:2.6,thermK:0,riseT:5.6,dur:13,white:.8,whitePeak:.82,dbl:!1,bills:3,dome:!1,volume:!0,ash:!1,secondary:3,shatter:1,thermal:0,shake:4.5,frontLife:3.4,glow:.7}},GLASS_K=[.42,.85,1.35,2.1],GLASS_K_REAL=[.339,.615,1,1.25];function glassLadder(kind){if(kind==="nuke"&&real()){if(CBZ.impact&&CBZ.impact.row)try{const row=CBZ.impact.row("nuke");if(row&&row.wave&&row.wave.glassK&&row.wave.glassK.length)return row.wave.glassK}catch{}return GLASS_K_REAL}return GLASS_K}const RISE_K=3.4,RISE_E=Math.exp(-RISE_K),PHASED_RISE_T=26,REAL_RISE_KEYS=[[0,0],[.08,.06],[.28,.3],[.58,.68],[1,1]],REAL_BLOOM_KEYS=[[0,.055],[.06,.16],[.18,.38],[.4,.82],[.7,1.22],[1,BLOOM_MAX]];function keyedEase(u,keys){u=clamp(u,0,1);for(let i=1;i<keys.length;i++)if(u<=keys[i][0]){const a=keys[i-1],b=keys[i],p=(u-a[0])/Math.max(1e-4,b[0]-a[0]);return a[1]+(b[1]-a[1])*ease(p)}return keys[keys.length-1][1]}function riseWindow(L){return L.style===STYLE.nuke&&phasedCloud()?PHASED_RISE_T:L.style.riseT}function riseAt(t,L){const u=clamp((t-.9)/riseWindow(L),0,1);if(!CBZ.CONFIG.NUKE_FX_RISE)return ease(u);if(L.style===STYLE.nuke&&phasedCloud())return keyedEase(u,REAL_RISE_KEYS);const d=(1-Math.exp(-RISE_K*u))/(1-RISE_E),w=clamp(u/.1,0,1);return d*w+ease(u)*(1-w)}function capYAt(rise,L){return L.by+L.R*.6+(L.riseH-L.R*.6)*rise}function bloomAt(t,L){const P=L.style;if(P===STYLE.nuke&&phasedCloud()){const u=clamp((t-.55)/PHASED_RISE_T,0,1);return keyedEase(u,REAL_BLOOM_KEYS)}const spread=clamp((t-P.riseT)/Math.max(1,P.dur-P.riseT),0,1);return .35+.9*ease((t-1.2)/(P.riseT*.85))+.16*spread}function cloudPhaseAt(t,L){return L.style===STYLE.nuke&&phasedCloud()?clamp(.5*ease((t-1.2)/6.8)+.5*ease((t-7.5)/16.5),0,1):1}const WDOME_T=1.05,WDOME_OUT=.42,WDOME_P=.4,WDOME_K=3.6,FIRE_R_K=1.75;function wdomeRadius(t,L){return L.R*(organic()?WDOME_K:1)*Math.pow(clamp(t/WDOME_T,0,1),WDOME_P)}const CAP_FLAT=.62;function capFlatAt(t,L){return v2()?1-(1-CAP_FLAT)*ease((t-1.4)/Math.max(1,riseWindow(L)*.9)):1}const ROLES=["cap","stem","surge","cap2","collar"];let live=null;function beginSequence(x,y,z,styleName,row,opts){if(!POOL.shell||!POOL.capVol||!POOL.crownVol||!POOL.glowVol)return null;const P=STYLE[styleName]||STYLE.nuke,q=q01(),gy=floorAt(x,z),legacyPuffs=styleName!=="nuke"||CBZ.CONFIG.NUKE_FX_LEGACY_PUFFS===!0,radius=fireR(row,opts),R=Math.max(5,radius*P.rFrac),wave=row.wave||null,sc=opts.scale>0?+opts.scale:1,reachQ=styleName==="nuke"?1:CBZ.qScale?CBZ.qScale(.45,1):1,maxR=(wave&&wave.maxR?wave.maxR:radius*4)*reachQ*sc,spd=wave&&wave.speed?wave.speed:150,burstY=Math.max(gy,Math.min(gy+R*3,y==null?gy:+y||gy)),dist=camDist(x,burstY+R,z),oneCloud=styleName==="nuke"&&coherentCloud(),nBills=oneCloud?0:Math.max(1,Math.min(P.bills,Math.round(CBZ.qScale?CBZ.qScale(1,P.bills):P.bills))),bills=[];for(let i=0;i<nBills;i++){const mesh=POOL.bills[i];if(!mesh)break;bills.push({mesh,role:ROLES[i],seed:rng(),roll:(rng()-.5)*.7,sx:(rng()-.5)*.05,sy:.02+rng()*.05}),mesh.visible=!1,mesh.material.uniforms.uOpacity.value=0,mesh.material.uniforms.uGlow.value=P.glow}if(live={kind:row.id||styleName,style:P,styleName,detonationId:opts._carBlastId||0,x,y:gy,by:burstY,z,R,maxR,spd,eff:radius,burnR:styleName==="nuke"&&real()?nukeRings(radius).psi2:P.thermK>0?maxR*P.thermK:0,riseH:R*P.riseK,capW:R*P.capK,stemW:R*P.stemK,surgeW:R*P.surgeK,capH:R*P.capK*.66,capThick:0,dims:null,drawDims:null,impW:0,impH:0,surgeDraw:0,t:0,r:Math.max(1,row.radius||radius*.1),dur:P.dur,q,frontLife:Math.min(P.frontLife,styleName==="nuke"&&CBZ.impact&&CBZ.impact.shockArrival?CBZ.impact.shockArrival(maxR,radius)+1.6:maxR/Math.max(1,spd)+1.6),bills,dustAcc:0,pending:[],ash:null,ashT:0,boomAt:-1,frontAt:-1,frontHit:!1,fogK:0,mix:0,mode:CBZ.game&&CBZ.game.mode||null,quiet:!!opts.quiet,noDamage:!!opts.noDamage,byPlayer:!!opts.byPlayer,legacyPuffs,genericPuffEvents:0,coherentCloud:oneCloud,windA:organic()?h01(x,z,SALT_WIND)*Math.PI*2:0,windK:organic()?.55+.9*h01(z,x,SALT_WINDK):0},organic()&&styleName==="nuke"){const body=(Math.floor(h01(x,z,SALT_BODY)*16777215)^5369207)&2147483647;VOL_BODY!==body&&(VOL_BODY=body,seedVolumes(body)),live.bodySeed=body}if(styleName==="nuke"&&real()){const D=nukeDims(R),F=formationDims(R);live.dims=D,live.drawDims=F,live.capW=F.capW/BLOOM_MAX,live.capH=F.capH,live.capThick=F.capH/F.capW,live.riseH=F.capY,live.stemW=F.stemW*.5,live.surgeW=F.base,live.surgeDraw=F.base,live.impW=F.capW,live.impH=F.top,CBZ.CONFIG.NUKE_FX_AFTERMATH&&(live.matureFrom=P.dur,live.dur=420)}const wantShell=CBZ.CONFIG.NUKE_FX_SHELL&&q>.28;if(live.shell=wantShell?POOL.shell:null,live.dome=wantShell&&P.dome&&q>.45?POOL.dome:null,live.shell&&(live.shell.position.set(x,burstY+R*.55,z),live.shell.scale.setScalar(.01),live.shell.material.uniforms.uOpacity.value=0,live.shell.visible=!0),live.dome&&(live.dome.position.set(x,burstY+R*.3,z),live.dome.scale.setScalar(.01),live.dome.material.uniforms.uOpacity.value=0,live.dome.visible=!1),live.volume=!!P.volume,live.volume){const nuke=styleName==="nuke",count=function(lo,hi){return Math.max(lo,Math.min(hi,Math.round(CBZ.qScale?CBZ.qScale(lo,hi):hi)))},fullCold=nuke&&phasedCloud();if(live.volN={cap:fullCold?VOL_MAX.cap:count(nuke?12:8,nuke?VOL_MAX.cap:Math.min(18,VOL_MAX.cap)),stem:fullCold?VOL_MAX.stem:count(nuke?8:5,nuke?VOL_MAX.stem:Math.min(10,VOL_MAX.stem)),surge:fullCold?VOL_MAX.surge:count(nuke?10:7,nuke?VOL_MAX.surge:Math.min(15,VOL_MAX.surge)),hot:count(nuke?8:6,nuke?VOL_MAX.hot:11),crown:0,glow:0},nuke&&v2()){const cn=fullCold?VOL_MAX.crown:count(10,VOL_MAX.crown);live.volN.crown=cn,live.crownN=clamp(Math.round(cn*(CROWN_N/VOL_MAX.crown)),3,cn-3),live.volN.glow=count(6,VOL_MAX.glow)}POOL.capVol.count=live.volN.cap,POOL.stemVol.count=live.volN.stem,POOL.surgeVol.count=live.volN.surge,POOL.hotVol.count=live.volN.hot,POOL.crownVol.count=live.volN.crown,POOL.glowVol.count=live.volN.glow;const vv=[POOL.surgeVol,POOL.stemVol,POOL.capVol,POOL.hotVol,POOL.crownVol,POOL.glowVol];for(let i=0;i<vv.length;i++)vv[i].position.set(x,gy,z),vv[i].visible=!0;POOL.capVol.material.opacity=0,POOL.stemVol.material.opacity=0,POOL.surgeVol.material.opacity=0,POOL.hotVol.material.opacity=0,POOL.crownVol.material.opacity=0,POOL.glowVol.material.opacity=0,live.volN.crown||(POOL.crownVol.visible=!1),live.volN.glow||(POOL.glowVol.visible=!1)}if(live.wdome=v2()&&P.dbl?POOL.wdome:null,live.wdome){const lift=Math.max(0,burstY-gy);live.wdomeY=gy+Math.min(lift,R*.55),live.wdome.position.set(x,live.wdomeY,z),live.wdome.scale.setScalar(.01),live.wdome.material.opacity=0,live.wdome.visible=!0}if(legacyPuffs){const nSat=Math.round(CBZ.qScale?CBZ.qScale(0,P.secondary):P.secondary);for(let i=0;i<nSat;i++){const a=i*2.399963+rng()*.55,rr=R*(.16+Math.sqrt((i+.35)/Math.max(1,nSat))*.68);live.pending.push({t:.22+i*.09,x:x+Math.cos(a)*rr,z:z+Math.sin(a)*rr,power:2.4,radius:13,sat:!0})}const nSat2=Math.round(nSat*.7);for(let i=0;i<nSat2;i++){const a=i*2.399963+.7+rng()*.45,rr=R*(.55+Math.sqrt(rng())*.85);live.pending.push({t:.95+i*.12,x:x+Math.cos(a)*rr,z:z+Math.sin(a)*rr,power:1.9,radius:11,sat:!0})}const nTherm=Math.round(CBZ.qScale?CBZ.qScale(0,P.thermal):P.thermal);for(let i=0;i<nTherm;i++){const a=i*2.399963+1.9+rng()*.7,inner=Math.min(maxR*.7,Math.max(R*1.25,1)),outer=Math.min(Math.max(inner,live.burnR>0?live.burnR:maxR),real()?camFar():1/0),rr=Math.sqrt(inner*inner+rng()*(outer*outer-inner*inner));live.pending.push({t:.9+i*.14,x:x+Math.cos(a)*rr,z:z+Math.sin(a)*rr,power:1.1,radius:16,sat:!0,smoke:!0})}live.genericPuffEvents=nSat+nSat2+nTherm}if(!(styleName==="nuke"&&!!opts._carBlastId&&CBZ.CONFIG.IMPACT_SHOCKWAVE!==!1&&!opts.noDamage)){const nShatter=Math.max(1,Math.round(CBZ.qScale?CBZ.qScale(1,P.shatter):P.shatter));if(CBZ.CONFIG.NUKE_FX_GLASS){const GK=glassLadder(styleName),step=GK.length/nShatter;for(let i=0;i<nShatter;i++){const k=GK[Math.min(GK.length-1,Math.max(0,Math.round((i+.5)*step-.5)))],rr=maxR*k,arrive=styleName==="nuke"&&CBZ.impact&&CBZ.impact.shockArrival?CBZ.impact.shockArrival(rr,radius):rr/Math.max(1,spd);live.pending.push({t:Math.max(.08,arrive),shatter:rr})}}else for(let i=0;i<nShatter;i++)live.pending.push({t:.3+i*.55,shatter:radius*(.9+i*.75)})}if(!live.quiet&&(whiteout(P.white,P.whitePeak,P.dbl),styleName!=="nuke")){const att=Math.max(.1,Math.min(1,1.25-dist/420));if(CBZ.shake)try{CBZ.shake(P.shake*att)}catch{}}return live}function firePending(p){if(p.sat&&CBZ.cityExplosion){try{CBZ.cityExplosion(p.x,p.z,{power:p.power,radius:p.radius,noDamage:!0,ordnance:live?live.kind:"nuke",_impact:!0})}catch{}if(p.smoke&&CBZ.cityCrashSmoke)try{CBZ.cityCrashSmoke(p.x,floorAt(p.x,p.z)+1.2,p.z,{count:4,scale:2})}catch{}return}if(p.shatter!=null&&CBZ.cityShatter&&live){try{CBZ.cityShatter(live.x,live.z,p.shatter)}catch{}return}}function frontRadius(dt){if(CBZ.impact&&CBZ.impact.waveState)try{const ws=CBZ.impact.waveState();for(let i=0;i<ws.length;i++)if(ws[i].kind===live.kind&&!(live.detonationId&&ws[i].detonationId&&ws[i].detonationId!==live.detonationId))return live.r=ws[i].r,live.r}catch{}return live.styleName==="nuke"&&CBZ.impact&&CBZ.impact.shockDistance?live.r=Math.min(live.maxR,CBZ.impact.shockDistance(live.t,live.R)):live.r=Math.min(live.maxR,live.r+live.spd*dt),live.r}function impostorMix(L){if(!L.dims||!POOL.imp)return 0;const c=camPos();if(!c)return 1;const cy=capYAt(riseAt(L.t,L),L),capR=L.capW*bloomAt(L.t,L)*.5,dFar=Math.hypot(L.x-c.x,cy-c.y,L.z-c.z)+capR,f=camFar();return clamp((dFar-f*.55)/(f*.4),0,1)}const _impBase=new THREE.Vector3,_impTop=new THREE.Vector3,_impUp=new THREE.Vector3,_impRight=new THREE.Vector3,_impNormal=new THREE.Vector3,_impBasis=new THREE.Matrix4;function impostorPointAtDepth(out,x,y,z,D,cam){out.set(x,y,z).applyMatrix4(cam.matrixWorldInverse);const depth=-out.z;return!(depth>.05)||!isFinite(depth)?0:(out.set(out.x*D/depth,out.y*D/depth,-D).applyMatrix4(cam.matrixWorld),depth)}function stepImpostor(t,L,mix){const imp=POOL.imp;if(!imp)return;if(mix<=.004||!L.dims){imp.visible=!1,imp.material.uniforms.uOpacity.value=0;return}const cam=CBZ.camera;if(!cam||!cam.position){imp.visible=!1;return}const rise=riseAt(t,L),grow=.3+.7*rise,capNow=L.capW*bloomAt(t,L),w=L.coherentCloud?capNow*1.3:L.impW*(phasedCloud()?1:grow),h=L.coherentCloud?Math.max(L.R*.55,capYAt(rise,L)-L.y+capNow*(L.capThick||.782)*.5*1.12):L.impH*(phasedCloud()?1:grow),D=camFar()*.86;cam.updateMatrixWorld&&cam.updateMatrixWorld(!0);const baseDepth=impostorPointAtDepth(_impBase,L.x,L.y,L.z,D,cam),topDepth=impostorPointAtDepth(_impTop,L.x,L.y+h,L.z,D,cam);if(!baseDepth||!topDepth){imp.visible=!1,imp.material.uniforms.uOpacity.value=0;return}imp.position.copy(_impBase).add(_impTop).multiplyScalar(.5);const hh=_impUp.subVectors(_impTop,_impBase).length();if(!(hh>.01)){imp.visible=!1,imp.material.uniforms.uOpacity.value=0;return}_impUp.multiplyScalar(1/hh),_impNormal.set(0,0,1).transformDirection(cam.matrixWorld),_impRight.crossVectors(_impUp,_impNormal),_impRight.lengthSq()<1e-6?_impRight.set(1,0,0).transformDirection(cam.matrixWorld):_impRight.normalize(),_impUp.crossVectors(_impNormal,_impRight).normalize(),_impBasis.makeBasis(_impRight,_impUp,_impNormal),imp.quaternion.setFromRotationMatrix(_impBasis),imp.scale.set(w*D/Math.max(.05,(baseDepth+topDepth)*.5),hh,1);const u=imp.material.uniforms;u.uLife.value=clamp(.04+t/40,0,.55),u.uPhase.value=cloudPhaseAt(t,L),u.uScroll.value.set(.013*t,-.021*t),u.uScroll2.value.set(-.008*t,.011*t);const fadeIn=ease((t-.55)/1.15),fadeOut=1-ease((t-(L.dur-9))/9);u.uOpacity.value=Math.max(0,mix*fadeIn*Math.max(0,fadeOut)),imp.visible=u.uOpacity.value>.004}function faceCameraYaw(mesh){const cam=CBZ.camera;!cam||!cam.position||mesh.rotation.set(0,Math.atan2(cam.position.x-mesh.position.x,cam.position.z-mesh.position.z),0)}const _volDummy=new THREE.Object3D;let _volWrite=!0;const VOL_HOT=new THREE.Color(16742936),VOL_ASH_V=[new THREE.Color(3354412),new THREE.Color(3814703)],VOL_STEM_HOT_V=[new THREE.Color(6175531),new THREE.Color(11026460)],VOL_STEM_ASH_V=[new THREE.Color(2696997),new THREE.Color(12102552)],VOL_DUST_HOT_V=[new THREE.Color(8414294),new THREE.Color(9060130)],VOL_DUST_ASH_V=[new THREE.Color(5920337),new THREE.Color(3679516),new THREE.Color(9404270)],VOL_EMBER=new THREE.Color(8006155),VOL_EMBER_OFF=new THREE.Color(525828),VOL_CROWN_HOT=new THREE.Color(4862757),VOL_CROWN_ASH_V=[new THREE.Color(2762531),new THREE.Color(3091238)],VOL_CROWN_EMBER=new THREE.Color(2886915),VOL_SORT_MAX=Math.max(VOL_MAX.cap,VOL_MAX.stem,VOL_MAX.surge,VOL_MAX.hot,VOL_MAX.crown,VOL_MAX.glow),_volBuf={n:0,x:new Float64Array(VOL_SORT_MAX),y:new Float64Array(VOL_SORT_MAX),z:new Float64Array(VOL_SORT_MAX),sx:new Float64Array(VOL_SORT_MAX),sy:new Float64Array(VOL_SORT_MAX),sz:new Float64Array(VOL_SORT_MAX),r:new Float64Array(VOL_SORT_MAX),key:new Float64Array(VOL_SORT_MAX),h:new Float64Array(VOL_SORT_MAX),idx:new Int32Array(VOL_SORT_MAX)};function writeVolume(mesh,slot,x,y,z,sx,sy,sz,ry,h){_volDummy.position.set(x,y,z);const r=ry||0;if(organic()){const hh=h||0;_volDummy.rotation.set(hh*6.2832,r+hh*2.7,hh*3.9)}else _volDummy.rotation.set(r*.37,r,r*.19);_volDummy.scale.set(Math.max(.01,sx),Math.max(.01,sy),Math.max(.01,sz)),_volDummy.updateMatrix(),mesh.setMatrixAt(slot,_volDummy.matrix)}function putVolume(mesh,i,x,y,z,sx,sy,sz,ry,h){if(!_volWrite)return;if(!smokeLobes()){writeVolume(mesh,i,x,y,z,sx,sy,sz,ry,h);return}const n=_volBuf.n;if(n>=VOL_SORT_MAX){writeVolume(mesh,i,x,y,z,sx,sy,sz,ry,h);return}_volBuf.x[n]=x,_volBuf.y[n]=y,_volBuf.z[n]=z,_volBuf.sx[n]=sx,_volBuf.sy[n]=sy,_volBuf.sz[n]=sz,_volBuf.r[n]=ry||0,_volBuf.h[n]=h||0,_volBuf.n=n+1}function flushVolume(mesh,sorted){if(!_volWrite||(mesh.instanceMatrix.needsUpdate=!0,!smokeLobes()))return;const n=_volBuf.n;if(_volBuf.n=0,!n)return;const B=_volBuf;for(let i=0;i<n;i++)B.idx[i]=i;if(sorted!==!1){const cam=camPos();if(cam){const cx=cam.x-mesh.position.x,cy=cam.y-mesh.position.y,cz=cam.z-mesh.position.z;for(let i=0;i<n;i++){const dx=B.x[i]-cx,dy=B.y[i]-cy,dz=B.z[i]-cz;B.key[i]=dx*dx+dy*dy+dz*dz}for(let i=1;i<n;i++){const v=B.idx[i],k=B.key[v];let j=i-1;for(;j>=0&&B.key[B.idx[j]]<k;)B.idx[j+1]=B.idx[j],j--;B.idx[j+1]=v}}}for(let i=0;i<n;i++){const j=B.idx[i];writeVolume(mesh,i,B.x[j],B.y[j],B.z[j],B.sx[j],B.sy[j],B.sz[j],B.r[j],B.h[j])}}const _smokeDir=new THREE.Vector3,_smokeCol=new THREE.Color;function smokeLight(){if(!smokeLobes())return;const sun=CBZ.sun,hemi=CBZ.hemi;if(sun&&sun.position&&(_smokeDir.copy(sun.position),CBZ.sunTarget&&CBZ.sunTarget.position&&_smokeDir.sub(CBZ.sunTarget.position),_smokeDir.lengthSq()>1e-6&&SMOKE_U.uSmokeSun.value.copy(_smokeDir).normalize(),sun.color)){const si=organic()?sun.intensity==null?1:sun.intensity:sun.intensity||1;SMOKE_U.uSmokeSunCol.value.copy(sun.color).multiplyScalar(clamp(si,0,2.4)*.34)}if(live){const base=live.y||0,capY=capYAt(riseAt(live.t,live),live),top=base+Math.max(60,capY-base);if(SMOKE_U.uSmokeSpan.value.set(base,top),organic()){SMOKE_U.uSmokeCore.value.set(live.x,capY,live.z),SMOKE_U.uSmokeCapR.value=Math.max(1,live.capW*bloomAt(live.t,live)*.5),SMOKE_U.uSmokeTime.value=live.t,SMOKE_U.uSmokeGlow.value.set(Math.max(60,live.surgeDraw||live.surgeW||live.R*4),clamp((live.t-7)/7,0,1));const c=camPos();c&&SMOKE_U.uSmokeCam.value.copy(c),scene.fog&&scene.fog.color&&SMOKE_U.uSmokeHaze.value.copy(scene.fog.color)}}if(hemi&&hemi.color){_smokeCol.copy(hemi.color),hemi.groundColor&&_smokeCol.lerp(hemi.groundColor,.34);const hi=hemi.intensity==null?1:hemi.intensity,dayHi=CBZ.lightKeys&&CBZ.lightKeys.day&&CBZ.lightKeys.day.hi||.72,amb=organic()?clamp(hi,0,3)*.32*clamp(hi/Math.max(.05,dayHi),.3,1):clamp(hemi.intensity||1,0,3)*.32;SMOKE_U.uSmokeAmb.value.copy(_smokeCol).multiplyScalar(amb)}}function cloudColor(mat,hot,ash,u,ember,uEmber){if(mat.color.copy(hot).lerp(ash,clamp(u,0,1)),mat.emissive){const ue=uEmber??u;mat.emissive.copy(ember||VOL_EMBER).lerp(VOL_EMBER_OFF,clamp(ue*1.25,0,1))}}function stepVolumes(t,L,mix){if(!L.volume||!L.volN)return;let volHz=12;organic()&&(volHz=camDist(L.x,L.y+(L.riseH||0)*.35,L.z)<4e3?24:12),_volWrite=L.volNext==null||t+1e-6>=L.volNext,_volWrite&&(L.volNext=t+1/volHz),_volBuf.n=0,smokeLight();const mixC=clamp(mix||0,0,1),near=1-mixC,rise=riseAt(t,L),capY=capYAt(rise,L)-L.y,bloom=bloomAt(t,L),cloudCool=clamp((t-.7)/11,0,1);let endFade=Math.max(0,1-ease((t-(L.dur-8))/8));L.matureFrom&&(endFade*=1-.5*ease((t-200)/Math.max(60,L.dur-208)));const roll=CBZ.CONFIG.NUKE_FX_ROLL?t*(.11+.22*Math.exp(-Math.max(0,t-1)/8)):t*.08,wind=organic(),windA=L.windA||0,windX=Math.cos(windA),windZ=Math.sin(windA),windK=L.windK||1,cap=POOL.capVol,capIn=ease((t-.55)/1.15),capRadius=L.capW*bloom*.5,flat=capFlatAt(t,L),photo=real(),halfH=capRadius*(L.capThick||.782),capDrift=wind?Math.min(capRadius*.12,Math.max(0,t-.55)*windK):0;for(let i=0;i<L.volN.cap;i++){const s=VOL_SEED.cap[i],a=s.a+roll*(.35+s.r*.45)+s.spin*t,rr=capRadius*s.r,lens=Math.sqrt(Math.max(.04,1-s.r*s.r)),lobe=capRadius*(photo?s.s2:s.s)*(.45+.55*capIn),overturn=Math.sin(a*1.7+t*.55)*capRadius*.035,yOff=photo?(halfH*s.y2*lens+overturn)*flat:(capRadius*s.y+overturn)*flat,dk=capDrift*(.5+.5*clamp(s.y2*.5+.5,0,1));putVolume(cap,i,Math.cos(a)*rr+windX*dk,capY+yOff,Math.sin(a)*rr+windZ*dk,lobe*(1.08+s.r*.22),lobe*(.72+(1-s.r)*.18)*flat,lobe,a*.35,lobeHash(i))}cap.material.opacity=solidOp(.86,.26)*capIn*endFade*near,cloudColor(cap.material,VOL_HOT,VOL_ASH_V[wind?1:0],cloudCool),cap.visible=cap.material.opacity>.004,flushVolume(cap);const glow=POOL.glowVol;if(L.volN.glow){const glowIn=ease((t-.6)/1),glowOut=Math.max(0,1-ease((t-2.6)/7.4));for(let i=0;i<L.volN.glow;i++){const s=VOL_SEED.glow[i],a=s.a+roll*.5,rr=capRadius*s.r,lobe=capRadius*s.s*.7;putVolume(glow,i,Math.cos(a)*rr+windX*capDrift,capY+capRadius*s.y*.55*flat,Math.sin(a)*rr+windZ*capDrift,lobe*1.15,lobe*flat,lobe,a,lobeHash(i+601))}glow.material.color.setHex(t<1.6?16773832:t<4.5?16761946:14248478),glow.material.opacity=.54*glowIn*glowOut*endFade*near,glow.visible=glow.material.opacity>.004,flushVolume(glow,!1)}const crown=POOL.crownVol;if(L.volN.crown){const collarIn=ease((t-1.5)/1.8),crownIn=ease((t-2.6)/3.4),nC=Math.max(1,Math.min(L.volN.crown-1,L.crownN||CROWN_N)),nK=L.volN.crown-nC,COLLAR_N=VOL_MAX.crown-CROWN_N;for(let i=0;i<L.volN.crown;i++){const isCrown=i<nC,si=isCrown?Math.min(CROWN_N-1,Math.floor(i*CROWN_N/nC)):CROWN_N+Math.min(COLLAR_N-1,Math.floor((i-nC)*COLLAR_N/Math.max(1,nK))),s=VOL_SEED.crown[si],grow2=isCrown?crownIn:collarIn,a=s.a+roll*(isCrown?.42:.24)+s.spin*t,rr=capRadius*s.r*(isCrown?1:1.04),lobe=capRadius*s.s*(.5+.5*grow2),dk=capDrift*(isCrown?1:.72);putVolume(crown,i,Math.cos(a)*rr+windX*dk,capY+capRadius*s.y*(isCrown?.46:.62)*flat,Math.sin(a)*rr+windZ*dk,lobe*(isCrown?1.05:1.42),lobe*(isCrown?.94:.56)*flat,lobe*(isCrown?1.05:1.42),a*.5,lobeHash(i+307))}crown.material.opacity=solidOp(.88,.3)*Math.max(collarIn,crownIn)*endFade*near,cloudColor(crown.material,VOL_CROWN_HOT,VOL_CROWN_ASH_V[wind?1:0],cloudCool,VOL_CROWN_EMBER),crown.visible=crown.material.opacity>.004,flushVolume(crown)}const stem=POOL.stemVol,stemIn=ease((t-.65)/1.3),STEM_TURNS=.85,capBase=capY-(photo?capRadius*(L.capThick||.782):capRadius*.4),hTrue=Math.max(L.R*.55,capBase),h=photo?L.coherentCloud?hTrue:Math.min(hTrue,camFar()*.82):Math.max(L.R*.55,capY),stemR=photo?L.stemW*(.55+rise*.45):L.stemW*(.72+rise*.88),stemY=Math.max(L.R*.1,h/Math.max(5,L.volN.stem*.72));for(let i=0;i<L.volN.stem;i++){const s=VOL_SEED.stem[i];if(!photo){const a0=s.a+roll*(.28+s.f*.35),neck=.72+Math.abs(s.f-.55)*.55;putVolume(stem,i,Math.cos(a0)*stemR*s.r,Math.max(stemY*.45,h*s.f),Math.sin(a0)*stemR*s.r,stemR*s.s*neck,stemY*s.s,stemR*s.s*neck,a0,lobeHash(i+101));continue}const fS=(i+.5+(s.f-.5)*.9)/Math.max(1,L.volN.stem),fTrue=fS*(h/Math.max(1,hTrue)),prof=stemProfile(fTrue),a=s.a+s.tw+roll*(.28+fS*.35)+fTrue*STEM_TURNS*6.2832,rr=stemR*prof*s.r2,taper=1-ease((fS-.82)/.18),lobe=stemR*prof*s.s2*Math.max(.02,taper),seg=h/Math.max(4,L.volN.stem),lean=wind?h*.035*fS:0,waveAmp=wind?Math.min(1,Math.max(0,t-1.5)/20)*Math.sin(fTrue*3.1+windA)*stemR*.35:0;putVolume(stem,i,Math.cos(a)*rr+windX*lean-windZ*waveAmp,Math.max(stemY*.35,h*fS),Math.sin(a)*rr+windZ*lean+windX*waveAmp,lobe*1.06,Math.max(lobe*.92,seg*1.35*Math.max(.02,taper)),lobe*1.06,a,lobeHash(i+101))}stem.material.opacity=solidOp(.84,wind?.64:.74)*stemIn*endFade*(photo?1-mixC*.55:near),cloudColor(stem.material,VOL_STEM_HOT_V[v2()?1:0],VOL_STEM_ASH_V[wind?1:0],cloudCool,VOL_EMBER,wind?clamp((t-.7)/80,0,1):null),stem.visible=stem.material.opacity>.004,flushVolume(stem);const surge=POOL.surgeVol,surgeIn=ease((t-.75)/2),surgeFade=Math.max(0,1-ease((t-Math.min(15,L.dur-5))/7)),surgeMax=photo?L.surgeDraw:Math.min(L.maxR*.72,L.R*3.6),surgeR=surgeMax*(.12+.88*ease((t-.55)/6.2)),deep=v2();for(let i=0;i<L.volN.surge;i++){const s=VOL_SEED.surge[i],a=s.a+Math.sin(t*.16+i)*.08;let rk=1;if(wind){const hf=lobeHash(i+911),c=Math.cos(a-windA);rk=1+(hf-.5)*.14+.03*Math.sin(t*.21+i*1.7)+(c>0?.08*c:.05*c)}const rr=surgeR*s.r*rk,lobe=Math.max(L.R*.075,surgeMax*(.055+s.s*.028)),tall=deep?.72+(i&1?.62:.02):1,spin=deep?-t*(.55/(.45+s.r*1.6)):0;putVolume(surge,i,Math.cos(a)*rr,lobe*(.24+.1*Math.sin(i*1.7))*tall,Math.sin(a)*rr,lobe*1.25,lobe*.38*tall,lobe,a+spin,lobeHash(i+211))}surge.material.opacity=solidOp(deep?.76:.82,wind?.39:.44)*surgeIn*surgeFade,cloudColor(surge.material,VOL_DUST_HOT_V[deep?1:0],VOL_DUST_ASH_V[wind?2:deep?1:0],cloudCool*.85,VOL_EMBER,wind?clamp((t-.7)/75,0,1):null),surge.visible=surge.material.opacity>.004,flushVolume(surge);const hot=POOL.hotVol,hotIn=ease(t/.16),hotFade=Math.max(0,1-ease((t-2.8)/3.2)),grow=1-Math.exp(-t*5.5),fireR0=L.R*grow*(1+rise*.3)*(wind?FIRE_R_K:1),fireY=L.by-L.y+L.R*.55+(L.riseH*.92-L.R*.55)*rise,groundMix=wind?ease((t-4)/4):0,fireLow=Math.max(L.R*.3,(L.surgeDraw||L.R*4)*.34);for(let i=0;i<L.volN.hot;i++){const s=VOL_SEED.hot[i],a=s.a+t*.18,rr=fireR0*s.r*.68,lobe=Math.max(.01,fireR0*s.s),gr=fireLow*(.15+s.r*.85),gy=L.R*.22+lobe*.35,gl=Math.max(.01,lobe*1.25),px=Math.cos(a)*(rr+(gr-rr)*groundMix),pz=Math.sin(a)*(rr+(gr-rr)*groundMix),py=(fireY+s.y*fireR0*.42)*(1-groundMix)+gy*groundMix,ls=lobe+(gl-lobe)*groundMix;putVolume(hot,i,px,py,pz,ls*1.08,ls*(.88-.3*groundMix),ls,a,lobeHash(i+419))}const pulse=flashRadiance(t,L.style);hot.material.color.setHex(t<.45?16774351:t<1.8?16752686:wind&&t>9?12071948:14238482);const emberTail=wind?.3*Math.max(0,1-ease((t-10)/48)):0;hot.material.opacity=.82*hotIn*Math.max(hotFade,emberTail)*(.18+.82*pulse),hot.visible=hot.material.opacity>.004,flushVolume(hot,!1),_volWrite=!0}function stepBill(b,t,L){const m=b.mesh,u=m.material.uniforms,rise=riseAt(t,L),capY=capYAt(rise,L),bloom=bloomAt(t,L),fadeIn=ease((t-b.t0)/.7),fadeOut=1-ease((t-(L.dur-9))/9);let op=fadeIn*Math.max(0,fadeOut);if(t<b.t0){m.visible=!1;return}const shear=CBZ.CONFIG.NUKE_FX_ROLL&&b.role==="cap"?.055:0;u.uScroll.value.set(b.seed+b.sx*t,b.seed-(b.sy+shear)*t),u.uScroll2.value.set(b.seed*.7-b.sx*.42*t,b.seed*1.3+(b.sy+shear*1.6)*.37*t);const flat=capFlatAt(t,L);switch(b.role){case"cap":m.position.set(L.x,capY,L.z),m.scale.set(L.capW*bloom,L.capW*bloom*.66*flat,1),u.uLife.value=clamp(t/9,0,1),u.uErode.value=.14,op*=.95;break;case"cap2":m.position.set(L.x+L.capW*.16,capY+L.capW*.05*flat,L.z-L.capW*.1),m.scale.set(L.capW*bloom*.72,L.capW*bloom*.5*flat,1),u.uLife.value=clamp(t/8+.05,0,1),u.uErode.value=.22,op*=.7;break;case"stem":{const h=Math.max(2,capY-L.y);m.position.set(L.x,L.y+h*.5,L.z),m.scale.set(L.stemW*(1+rise*.7),h,1),u.uLife.value=clamp(.3+t/26,0,1),u.uErode.value=.3,op*=.78;break}case"collar":m.position.set(L.x,capY-L.capW*.3*bloom*flat,L.z),m.scale.set(L.capW*bloom*.62,L.capW*bloom*.26*flat,1),u.uLife.value=clamp(.22+t/18,0,1),u.uErode.value=.26,op*=.62;break;case"surge":{const g=ease((t-1.6)/6),w=L.surgeW*(.35+1.5*g),h=w*.19;m.position.set(L.x,L.y+h*.42,L.z),m.scale.set(w,h,1),u.uLife.value=clamp(.42+t/24,0,1),u.uErode.value=.34,op*=.6*(1-ease((t-9)/9));break}default:m.visible=!1,u.uOpacity.value=0;return}if(L.volume&&(op*=.46),L.mix>0){const keep=b.role==="stem"||b.role==="surge"?.55:1;op*=Math.max(0,1-L.mix*keep)}u.uOpacity.value=Math.max(0,op),m.visible=u.uOpacity.value>.004,m.visible&&faceCameraYaw(m)}function stepSequence(dt){const L=live,P=L.style;L.t+=dt;const t=L.t;if(L.mode&&CBZ.game&&CBZ.game.mode!==L.mode){endSequence();return}for(let i=L.pending.length-1;i>=0;i--)if(t>=L.pending[i].t){const p=L.pending.splice(i,1)[0];try{firePending(p)}catch{}}const rad0=flashRadiance(t,P);if(CBZ.CONFIG.NUKE_FX_SKY&&scene.fog&&scene.fog.color){let k,hex;t<.55?(hex=16774370,k=(.92*(1-t/.55)+.3)*(.34+.66*rad0)):t<3.5?(hex=16749632,k=.62*(1-(t-.55)/2.95)+.22):(hex=9274488,k=.55*Math.max(0,1-(t-3.5)/(L.dur-3.5))),organic()&&(k*=.16+.84*Math.exp(-Math.max(0,t-1.5)/2.6)),_fogTint.setHex(hex),L.fogK=k,scene.fog.color.lerp(_fogTint,clamp(k,0,.95))}const r=frontRadius(dt);if(L.legacyPuffs&&t<L.frontLife&&r<L.maxR){L.dustAcc+=dt;const nd=Math.round(CBZ.qScale?CBZ.qScale(0,3):3);if(L.dustAcc>.3&&nd>0&&CBZ.cityDustKick){L.dustAcc=0;for(let i=0;i<nd;i++){const a=rng()*6.2832,px=L.x+Math.cos(a)*r,pz=L.z+Math.sin(a)*r;try{CBZ.cityDustKick(px,floorAt(px,pz)+.6,pz,1.5+L.q)}catch{}}}}if(L.wdome){const rad=wdomeRadius(t,L),lift=clamp((t-WDOME_T)/WDOME_OUT,0,1);L.wdome.position.set(L.x,L.wdomeY+rad*.3*lift,L.z);const sr=Math.max(.01,rad);L.wdome.scale.set(sr,sr*(.92+.16*lift),sr);const op=(1-ease(lift))*(.1+.9*rad0);L.wdome.material.opacity=Math.max(0,op),(lift>=1||L.wdome.material.opacity<=.004)&&(L.wdome.visible=!1,L.wdome=null)}const rise=riseAt(t,L);if(L.shell){const grow=1-Math.exp(-t*5.5),rad=L.R*(organic()?FIRE_R_K:1)*(grow*(1+rise*.35)),y=L.by+L.R*.55+(L.riseH*.92-L.R*.55)*rise;L.shell.position.set(L.x,y,L.z),L.shell.scale.setScalar(Math.max(.01,rad));const u=L.shell.material.uniforms;organic()&&(u.uTime.value=t*2.2,u.uBoil.value=ease(t/.8)*(.3+.7*Math.exp(-Math.max(0,t-.8)/1.6)),u.uMottle.value=.5*ease(t/.6));const age=clamp(t/7,0,1);rampColor(u.uRimColor.value,age*.55),rampColor(u.uCoreColor.value,Math.max(0,age*.9-.04));const blue=1-ease(t/.35);blue>.001&&(u.uRimColor.value.lerp(BLUE_WHITE,blue*.8),u.uCoreColor.value.lerp(BLUE_WHITE,blue));const gain=1+2.4*rad0*(1-ease((t-.1)/1.7));u.uCoreColor.value.multiplyScalar(gain),u.uRimColor.value.multiplyScalar(1+(gain-1)*.45),u.uOpacity.value=Math.max(0,Math.min(1,t/.12)*(1-ease((t-2.15)/1.75))*(.14+.86*rad0)),u.uOpacity.value<=.004&&t>3.5&&(L.shell.visible=!1,L.shell=null)}if(L.dome){const veil=CBZ.CONFIG.NUKE_FX_VEIL&&P.dbl,t0=veil?.06:.28;if(t>=t0){const dr=Math.min(r,L.R*(veil?1.35:1)+(t-t0)*L.spd*.85);L.dome.visible=!0,L.dome.position.set(L.x,L.by+dr*.16,L.z),L.dome.scale.set(dr,dr*.72,dr);let op;if(!veil)op=.34*(1-ease((t-.5)/1.1));else{const opaque=ease(t/.2)*(1-rad0),wilson=.3*(1-ease((t-.62)/1.25));op=Math.max(.88*opaque,wilson)}L.dome.material.uniforms.uOpacity.value=Math.max(0,op),t>1.9&&(L.dome.visible=!1,L.dome=null)}}const mix=L.coherentCloud?0:real()?impostorMix(L):0;if(L.mix=mix,stepImpostor(t,L,mix),L.matureFrom&&t>L.matureFrom&&L.drawDims&&L.dims){const F=L.drawDims,D=L.dims,k=ease(clamp((t-L.matureFrom)/170,0,1));L.capW=(F.capW+(D.capW-F.capW)*k)/BLOOM_MAX,L.capH=F.capH+(D.capH-F.capH)*k,L.capThick=L.capH/Math.max(1,L.capW*BLOOM_MAX),L.riseH=F.capY+(D.capY-F.capY)*k,L.stemW=(F.stemW+(D.stemW-F.stemW)*k)*.5}stepVolumes(t,L,mix);for(let i=0;i<L.bills.length;i++){const b=L.bills[i];b.t0==null&&(b.t0=b.role==="stem"?.8:b.role==="cap"?.9:b.role==="surge"?1.4:b.role==="cap2"?1.9:2.2),stepBill(b,t,L)}if(P.ash&&CBZ.CONFIG.NUKE_FX_ASH&&!L.ash&&t>8&&CBZ.fx&&CBZ.fx.particleCloud&&L.q>.3)try{L.ash=CBZ.fx.particleCloud({count:260,radius:62,top:46,bottom:-2,mode:"fall",vMin:1.6,vMax:4.2,drift:1.1,driftZ:.5,color:10129538,size:.24,opacity:.42}),L.ash.setActive(.9)}catch{L.ash=null}if(L.ash){const c=camPos(),fade=t>L.dur-8?Math.max(0,1-(t-(L.dur-8))/8):1;L.ash.setActive(.9*fade);try{L.ash.update(dt,c?c.x:L.x,c?c.y:L.y+20,c?c.z:L.z)}catch{}}t>=L.dur&&endSequence()}function endSequence(){const L=live;live=null;const mm=[POOL.shell,POOL.dome,POOL.wdome,POOL.imp,POOL.capVol,POOL.stemVol,POOL.surgeVol,POOL.hotVol,POOL.crownVol,POOL.glowVol];for(let i=0;i<mm.length;i++){const m=mm[i];m&&(m.visible=!1,m.isInstancedMesh&&(m.count=0),m.material&&m.material.uniforms&&m.material.uniforms.uOpacity&&(m.material.uniforms.uOpacity.value=0),m.material&&m.material.opacity!=null&&(m.material.opacity=0))}for(let i=0;i<POOL.bills.length;i++)POOL.bills[i].visible=!1,POOL.bills[i].material.uniforms.uOpacity.value=0;if(L&&L.ash)try{L.ash.dispose()}catch{}}CBZ.cityNukeFxAbort=endSequence;function nearField(x,y,z,row,opts){if((row.id||"nuke")==="nuke"&&CBZ.CONFIG.NUKE_FX_V1!==!1&&coherentCloud()&&POOL.shell&&POOL.capVol)return;const fn=CBZ.cityAirstrikeExplosion||CBZ.cityExplosion;if(fn)try{fn(x,z,{power:Math.max(.5,(+row.power||4)*(opts.scale>0?+opts.scale:1)),radius:Math.max(1,+row.radius||14),y,byPlayer:!!opts.byPlayer,noDamage:!!opts.noDamage,ordnance:row.id||"nuke",_impact:!0,noVisual:(row.id||"nuke")==="nuke"&&coherentCloud()})}catch{}}function compose(styleName){return function(x,y,z,row,opts){if(opts=opts||{},row=row||{},!CBZ.CONFIG.NUKE_FX_V1){nearField(x,y,z,row,opts);return}if(nearField(x,y,z,row,opts),live){opts.quiet||whiteout(STYLE[styleName].white*.4,.6,!1);return}try{beginSequence(x,y,z,styleName,row,opts)}catch{try{endSequence()}catch{}}}}const composeNuke=compose("nuke"),composeMoab=compose("moab");CBZ.cityNukeFX=function(x,y,z,opts){opts=opts||{};const kind=opts.kind==="moab"?"moab":"nuke";let row=null;if(CBZ.impact&&CBZ.impact.row)try{row=CBZ.impact.row(kind)}catch{}return row=Object.assign({id:kind,power:kind==="moab"?4.6:9,radius:kind==="moab"?26:14,wave:kind==="moab"?{speed:140,maxR:320}:{model:"nuclear",speed:343,maxR:3276}},row||{},opts.row||{}),opts.power!=null&&(row.power=opts.power),opts.radius!=null&&(row.radius=opts.radius),opts.wave!==void 0&&(row.wave=opts.wave),(kind==="moab"?composeMoab:composeNuke)(x,y??floorAt(x,z)+1.2,z,row,opts),live};const walks=[],WALK_MAX=2,WALK_POINTS=24;CBZ.cityBombWalk=function(points,opts){if(opts=opts||{},!points||!points.length)return null;const kind=opts.kind||"bomb",interval=clamp(opts.interval==null?.24:+opts.interval,.06,3),willDetonate=opts.detonate===!0,budget=willDetonate?WALK_POINTS:Math.max(2,Math.round(WALK_POINTS*(CBZ.qScale?CBZ.qScale(.55,1):1))),stride=Math.max(1,Math.ceil(points.length/budget)),pts=[];for(let i=0;i<points.length;i+=stride){const p=points[i];p&&pts.push({x:+p.x||0,y:p.y==null?null:+p.y,z:+p.z||0})}if(!pts.length)return null;const walk={pts,i:0,t:-Math.max(0,+opts.delay||0),interval,kind,detonate:willDetonate,by:opts.by||null,byPlayer:!!opts.byPlayer,scale:opts.scale||1,dirx:opts.dirx||0,dirz:opts.dirz||0,onEach:opts.onEach||null,prev:null,dead:!1};if(CBZ.CONFIG.BOMB_WALK_V1===!1){for(let i=0;i<pts.length;i++)dropOne(walk,pts[i]);return{cancel:function(){},points:pts.length}}for(;walks.length>=WALK_MAX;)walks.shift();return walks.push(walk),{points:pts.length,cancel:function(){walk.dead=!0},done:function(){return walk.dead||walk.i>=walk.pts.length}}},CBZ.cityBombWalkActive=function(){return walks.length};function dropOne(walk,p){const y=p.y==null?floorAt(p.x,p.z)+1.2:p.y;if(walk.detonate){if(CBZ.detonate)try{CBZ.detonate(p.x,y,p.z,walk.kind,{by:walk.by,byPlayer:walk.byPlayer,scale:walk.scale,dirx:walk.dirx,dirz:walk.dirz})}catch{}else if(CBZ.cityAirstrikeExplosion)try{CBZ.cityAirstrikeExplosion(p.x,p.z,{power:2.4,radius:13,byPlayer:walk.byPlayer})}catch{}}if(CBZ.cityDustKick){try{CBZ.cityDustKick(p.x,y,p.z,walk.detonate?1.4:2)}catch{}const prev=walk.prev;if(prev){const nMid=Math.round(CBZ.qScale?CBZ.qScale(0,2):2);for(let i=1;i<=nMid;i++){const f=i/(nMid+1),mx=prev.x+(p.x-prev.x)*f,mz=prev.z+(p.z-prev.z)*f;try{CBZ.cityDustKick(mx,floorAt(mx,mz)+.7,mz,1.6)}catch{}}}}if(walk.prev=p,walk.onEach)try{walk.onEach(p.x,y,p.z)}catch{}}function stepWalks(dt){for(let w=walks.length-1;w>=0;w--){const walk=walks[w];if(walk.dead){walks.splice(w,1);continue}walk.t+=dt;let fired=0;for(;walk.i<walk.pts.length&&walk.t>=walk.i*walk.interval&&fired<3;)dropOne(walk,walk.pts[walk.i]),walk.i++,fired++;walk.i>=walk.pts.length&&walks.splice(w,1)}}let wired=!1;function wire(){if(!(wired||!CBZ.impact||!CBZ.impact.fx)){wired=!0;try{if(CBZ.impact.fx("nuke",composeNuke),CBZ.impact.fx("moab",composeMoab),CBZ.CONFIG.NUKE_FX_MOAB&&CBZ.impact.row&&CBZ.impact.define){const row=CBZ.impact.row("moab");if(row&&row.fx==="heavy"){const spec=Object.assign({},row);spec.fx="moab",CBZ.impact.define("moab",spec)}}}catch{}}}let resetWrapped=!1;function wrapReset(){if(resetWrapped)return;const orig=CBZ.cityBlastFxReset;if(typeof orig!="function"||(resetWrapped=!0,orig._nukeFxWrapped))return;const wrapped=function(){try{endSequence(),flashClear(),walks.length=0}catch{}return orig.apply(this,arguments)};for(const k in orig)k.endsWith("Wrapped")&&(wrapped[k]=orig[k]);wrapped._nukeFxWrapped=!0,CBZ.cityBlastFxReset=wrapped}CBZ.onAlways&&CBZ.onAlways(9.62,function(dt){if(wire(),wrapReset(),!live&&!walks.length&&!flash)return;const d=dt>.25?.25:dt;if(flash)try{stepFlash(d)}catch{flash=null}if(live)try{stepSequence(d)}catch{endSequence()}if(walks.length)try{stepWalks(d)}catch{walks.length=0}});const LIGHT={SUN_K:2.4,HEMI_K:4.2,SUN_MIX:.88,HEMI_MIX:.94,NEAR:300,FAR:2400,FLOOR:.22},_lightC=new THREE.Color,FIRE_WHITE=new THREE.Color(16774884),FIRE_ORANGE=new THREE.Color(16743466),FIRE_EMBER=new THREE.Color(12863770);function fireLum(t,L){const P=L.style,pulse=flashRadiance(t,P),flashK=Math.max(0,1-t/Math.max(.01,P.white*.72)),tail=1-ease((t-(P.dur-10))/10),burn=Math.exp(-Math.max(0,t-.6)/5.2)*Math.max(0,tail);return clamp(Math.max(flashK*pulse,burn*.72),0,1)}function lightAtten(L){const d=camDist(L.x,L.by+L.R,L.z);if(d<=LIGHT.NEAR)return 1;const u=clamp((d-LIGHT.NEAR)/(LIGHT.FAR-LIGHT.NEAR),0,1);return 1-(1-LIGHT.FLOOR)*u}CBZ.onAlways&&CBZ.onAlways(94.6,function(){if(!live||!v2()||!CBZ.CONFIG.NUKE_FX_SKY)return;const L=live,k=fireLum(L.t,L)*lightAtten(L);if(k<=.004)return;L.t<.45?_lightC.copy(FIRE_WHITE).lerp(FIRE_ORANGE,L.t/.45):_lightC.copy(FIRE_ORANGE).lerp(FIRE_EMBER,clamp((L.t-.45)/7,0,1));const sun=CBZ.sun,hemi=CBZ.hemi,bnc=CBZ.bounce;sun&&(sun.intensity*=1+LIGHT.SUN_K*k,sun.color&&sun.color.lerp(_lightC,LIGHT.SUN_MIX*k)),hemi&&(hemi.intensity*=1+LIGHT.HEMI_K*k,hemi.color&&hemi.color.lerp(_lightC,LIGHT.HEMI_MIX*k),hemi.groundColor&&hemi.groundColor.lerp(_lightC,.78*k)),bnc&&bnc.color&&(bnc.color.lerp(_lightC,.85*k),bnc.intensity*=1+1.8*k)});const _auditM=new THREE.Matrix4,_auditP=new THREE.Vector3,_auditQ=new THREE.Quaternion,_auditS=new THREE.Vector3;CBZ.nukeSmokeAudit=function(){const layers={cap:POOL.capVol,crown:POOL.crownVol,stem:POOL.stemVol,surge:POOL.surgeVol},out={smokeLobes:smokeLobes(),live:!!live,t:live?+live.t.toFixed(2):null,mask:!!SMOKE_U.uSmokeMask.value,layers:{}};for(const key in layers){const mesh=layers[key];if(!mesh)continue;const n=Math.max(0,mesh.count|0),a=+(mesh.material.opacity||0),rec={lobes:n,perLobeAlpha:+a.toFixed(3),depthWrite:!!mesh.material.depthWrite,renderOrder:mesh.renderOrder,visible:!!mesh.visible};if(n>0){const px=[],py=[],pz=[],pr=[];let cx=0,cy=0,cz=0;for(let i=0;i<n;i++)mesh.getMatrixAt(i,_auditM),_auditM.decompose(_auditP,_auditQ,_auditS),px.push(_auditP.x),py.push(_auditP.y),pz.push(_auditP.z),pr.push((_auditS.x+_auditS.y+_auditS.z)/3),cx+=_auditP.x,cy+=_auditP.y,cz+=_auditP.z;cx/=n,cy/=n,cz/=n;let sum=0,min=1e9,max=0;const RAYS=16;for(let r=0;r<RAYS;r++){const ang=r*Math.PI/RAYS,dx=Math.cos(ang),dz=Math.sin(ang);let hits=0;for(let i=0;i<n;i++){const ox=px[i]-cx,oy=py[i]-cy,oz=pz[i]-cz,proj=ox*dx+oz*dz,ex=ox-proj*dx,ez=oz-proj*dz;ex*ex+oy*oy+ez*ez<pr[i]*pr[i]&&hits++}sum+=hits,hits<min&&(min=hits),hits>max&&(max=hits)}const mean=sum/RAYS,cov=function(h){return+(1-Math.pow(1-a,h)).toFixed(3)};rec.hits={min:min===1e9?0:min,mean:+mean.toFixed(2),max},rec.coverage={one:cov(1),mean:cov(mean),max:cov(max)}}out.layers[key]=rec}return out},CBZ.nukeFxDebug=function(){return{wired,live:live?{kind:live.kind,t:+live.t.toFixed(2),r:+live.r.toFixed(1),maxR:+live.maxR.toFixed(1),eff:+live.eff.toFixed(1),gy:+live.y.toFixed(1),burstY:+live.by.toFixed(1),R:+live.R.toFixed(1),capW:+live.capW.toFixed(1),riseH:+live.riseH.toFixed(1),frontLife:+live.frontLife.toFixed(2),fogK:+live.fogK.toFixed(3),burnR:+(live.burnR||0).toFixed(1),rise:+riseAt(live.t,live).toFixed(3),capY:+capYAt(riseAt(live.t,live),live).toFixed(1),radiance:+flashRadiance(live.t,live.style).toFixed(3),bills:live.bills.length,pending:live.pending.length,ash:!!live.ash,shell:!!live.shell,dome:!!live.dome,volume:!!live.volume,volumeCounts:live.volN||null,groundRings:0,wdome:!!live.wdome,wdomeR:+wdomeRadius(live.t,live).toFixed(1),capFlat:+capFlatAt(live.t,live).toFixed(3),lum:+fireLum(live.t,live).toFixed(3),lumAtLens:+(fireLum(live.t,live)*lightAtten(live)).toFixed(3),crownN:live.crownN||0,mix:+(live.mix||0).toFixed(3),impostor:!!(POOL.imp&&POOL.imp.visible),realScale:!!live.dims,yieldKt:live.dims?+live.dims.W.toFixed(2):null,capWNow:+(live.capW*bloomAt(live.t,live)).toFixed(0),capYNow:+capYAt(riseAt(live.t,live),live).toFixed(0),stemWNow:+(live.stemW*2).toFixed(0),surgeDraw:+(live.surgeDraw||0).toFixed(0),cloudPhase:+cloudPhaseAt(live.t,live).toFixed(3),genericPuffEvents:live.genericPuffEvents||0,windDeg:+((live.windA||0)*57.2958).toFixed(1),windK:+(live.windK||0).toFixed(2),bodySeed:live.bodySeed||0,legacyPuffs:!!live.legacyPuffs,coherentCloud:!!live.coherentCloud}:null,flash:flash?{t:+flash.t.toFixed(2),dur:flash.dur,peak:flash.peak,keys:flash.keys.length}:null,walks:walks.map(function(w){return{kind:w.kind,i:w.i,n:w.pts.length}}),pool:{bills:POOL.bills.length,built:!!POOL.shell},q:+q01().toFixed(2),flags:{v1:!!CBZ.CONFIG.NUKE_FX_V1,shell:!!CBZ.CONFIG.NUKE_FX_SHELL,ash:!!CBZ.CONFIG.NUKE_FX_ASH,moab:!!CBZ.CONFIG.NUKE_FX_MOAB,sky:!!CBZ.CONFIG.NUKE_FX_SKY,walk:!!CBZ.CONFIG.BOMB_WALK_V1,pulse:!!CBZ.CONFIG.NUKE_FX_PULSE,veil:!!CBZ.CONFIG.NUKE_FX_VEIL,rise:!!CBZ.CONFIG.NUKE_FX_RISE,roll:!!CBZ.CONFIG.NUKE_FX_ROLL,glass:!!CBZ.CONFIG.NUKE_FX_GLASS,v2:v2(),smokeLobes:smokeLobes(),organic:organic(),phasedCloud:phasedCloud(),coherentCloud:coherentCloud(),legacyPuffs:CBZ.CONFIG.NUKE_FX_LEGACY_PUFFS===!0}}},CBZ.nukeFxSize=function(kind,opts){opts=opts||{},kind=kind==="moab"?"moab":"nuke";const P=STYLE[kind];let row=null;if(CBZ.impact&&CBZ.impact.row)try{row=CBZ.impact.row(kind)}catch{}row=row||{power:kind==="moab"?4.6:9,radius:kind==="moab"?26:14,wave:kind==="moab"?{speed:140,maxR:320}:{model:"nuclear",speed:343,maxR:3276}};const eff=fireR(row,opts),R=Math.max(5,eff*P.rFrac),sc=opts.scale>0?+opts.scale:1,reachQ=kind==="nuke"?1:CBZ.qScale?CBZ.qScale(.45,1):1,reach=(row.wave?row.wave.maxR:eff*4)*reachQ*sc,v2n=kind==="nuke"&&v2(),oneCloud=kind==="nuke"&&coherentCloud(),volumeDraws=P.volume?v2n?6:4:0,RD=kind==="nuke"&&real()?nukeDims(R):null,FD=RD?formationDims(R):null;return{kind,nearField:+eff.toFixed(1),fireball:+R.toFixed(1),R:+R.toFixed(1),capW:+(RD?RD.capW:R*P.capK).toFixed(1),capY:+(RD?RD.capY:R*P.riseK).toFixed(1),capH:+(RD?RD.capH:R*P.capK*.66).toFixed(1),stemW:+(RD?RD.stemW:R*P.stemK).toFixed(1),cloudTop:+(RD?RD.top:R*P.riseK*1.16).toFixed(1),yieldKt:RD?+RD.W.toFixed(2):null,realScale:!!RD,drawDims:FD?{capW:+FD.capW.toFixed(1),capH:+FD.capH.toFixed(1),capY:+FD.capY.toFixed(1),top:+FD.top.toFixed(1),stemW:+FD.stemW.toFixed(1),base:+FD.base.toFixed(1)}:null,impostorDraws:RD&&!oneCloud?1:0,reach:+reach.toFixed(1),burnR:+(RD?nukeRings(R).psi2:P.thermK>0?reach*P.thermK:0).toFixed(1),bills:oneCloud?0:Math.max(1,Math.min(P.bills,Math.round(CBZ.qScale?CBZ.qScale(1,P.bills):P.bills))),shell:!!(CBZ.CONFIG.NUKE_FX_SHELL&&q01()>.28),volumeDraws,whiteDome:v2n&&!!P.dbl,groundRings:0,addLayers:(CBZ.CONFIG.NUKE_FX_SHELL&&q01()>.28?1:0)+volumeDraws+(v2n&&P.dbl?1:0)}},CBZ.nukeFxAudit=function(kind,opts){kind=kind==="moab"?"moab":"nuke";const P=STYLE[kind],S=CBZ.nukeFxSize(kind,opts),spd=kind==="moab"?140:343;let dipI=0;for(;dipI+1<FLASH_DOUBLE.length&&FLASH_DOUBLE[dipI+1][1]<=FLASH_DOUBLE[dipI][1];)dipI++;const dipT=FLASH_DOUBLE[dipI][0],dipV=FLASH_DOUBLE[dipI][1];let pk2T=dipT,pk2V=dipV;for(let i=dipI+1;i<FLASH_DOUBLE.length;i++)FLASH_DOUBLE[i][1]>pk2V&&(pk2V=FLASH_DOUBLE[i][1],pk2T=FLASH_DOUBLE[i][0]);const bloomMax=BLOOM_MAX,RD=kind==="nuke"&&real()?nukeDims(S.fireball):null,FD=RD?formationDims(S.fireball):null,capWide=RD?RD.capW:S.capW*bloomMax,flatK=kind==="nuke"&&v2()?CAP_FLAT:1,capTall=RD?RD.capH:capWide*.66*flatK,cloudTop=RD?RD.top:S.capY+capWide*.66*.5*flatK,capMidY=RD?RD.capY:S.capY,stemWide=RD?RD.stemW:S.R*P.stemK*1.7,collarWide=capWide*.98,glassK=glassLadder(kind),T=kind==="nuke"&&real()?nukeRings(S.fireball):null,zones=T?{fireball:S.fireball,severe:+T.psi20.toFixed(1),flatten:+T.psi5.toFixed(1),burn:+T.psi2.toFixed(1),glass:+T.psi1.toFixed(1),rad500:+T.rad500.toFixed(1),reach:S.reach}:{flatten:S.reach,burn:S.burnR,glass:+(S.reach*glassK[glassK.length-1]).toFixed(1),fireball:S.fireball},beats={whiteout:0,firstMax:0,minimum:P.dbl?+(dipT*P.white).toFixed(3):-1,secondMax:P.dbl?+(pk2T*P.white).toFixed(3):-1,veilIn:P.dbl&&CBZ.CONFIG.NUKE_FX_VEIL?.06:.28,veilOut:1.9,volumeIn:.55,stemIn:.65,capIn:.55,surgeIn:.75,cap2In:1.9,collarIn:2.2,thermalIgnitionIn:kind==="nuke"?0:.9,shellOut:3.9,whiteDomeIn:S.whiteDome?0:-1,whiteDomeFull:S.whiteDome?WDOME_T:-1,whiteDomeOut:S.whiteDome?+(WDOME_T+WDOME_OUT).toFixed(2):-1,capGlowIn:S.whiteDome?.6:-1,capGlowOut:S.whiteDome?10:-1,collar3dIn:S.whiteDome?1.5:-1,crownIn:S.whiteDome?2.6:-1,capFlattenAt:S.whiteDome?+(1.4+riseWindow({style:P})*.9).toFixed(2):-1,riseStart:.9,riseEnd:+(.9+riseWindow({style:P})).toFixed(2),glassAt:glassK.map(function(k){const radius=S.reach*k,arrival=kind==="nuke"&&CBZ.impact&&CBZ.impact.shockArrival?CBZ.impact.shockArrival(radius,S.fireball):radius/spd;return+Math.max(kind==="nuke"?.08:.3,arrival).toFixed(2)}),ashIn:P.ash?8:-1,end:P.dur},proportions={cloudTop:+cloudTop.toFixed(1),capWidth:+capWide.toFixed(1),capHeight:+capTall.toFixed(1),capAltitude:+capMidY.toFixed(1),stemWidth:+stemWide.toFixed(1),collarWidth:+collarWide.toFixed(1),capWideOverTall:+(capWide/Math.max(1,capTall)).toFixed(2),topOverCap:+(cloudTop/capWide).toFixed(2),altOverCap:+(capMidY/capWide).toFixed(2),capOverStem:+(capWide/stemWide).toFixed(2),overhang:+(collarWide/stemWide).toFixed(2),capFlatten:flatK,burnOverFlatten:+(zones.burn/Math.max(1,zones.flatten)).toFixed(3)},domeCurve=S.whiteDome?[.05,.25,.5,1].map(function(u){return+Math.pow(u,WDOME_P).toFixed(3)}):null,handoffT=WDOME_T+WDOME_OUT,curveL={style:P,R:S.R,by:0,y:0,riseH:FD?FD.capY:S.capY,capW:FD?FD.capW/BLOOM_MAX:S.capW,dims:RD},handoffRise=riseAt(handoffT,curveL),formation=kind==="nuke"?{handoffT:+handoffT.toFixed(2),handoffRise:+handoffRise.toFixed(3),handoffCapW:+(curveL.capW*bloomAt(handoffT,curveL)).toFixed(0),handoffCapY:+capYAt(handoffRise,curveL).toFixed(0),handoffPhase:+cloudPhaseAt(handoffT,curveL).toFixed(3),riseWindow:+riseWindow(curveL).toFixed(1),genericPuffEvents:CBZ.CONFIG.NUKE_FX_LEGACY_PUFFS===!0?Math.round(CBZ.qScale?CBZ.qScale(0,P.secondary):P.secondary)+Math.round(Math.round(CBZ.qScale?CBZ.qScale(0,P.secondary):P.secondary)*.7)+Math.round(CBZ.qScale?CBZ.qScale(0,P.thermal):P.thermal):0}:null;return{kind,zones,rings:zones,beats,proportions,formation,pulse:{min:dipV,secondMax:pk2V,keys:FLASH_DOUBLE.length},yieldKt:T?T.W:null,dims:RD,drawDims:FD,casualty:T?[126,504,756,1109,1533,2016,2554,3276].map(function(r){return{r,killed:+nukeLethalAt(r,S.fireball).toFixed(3)}}):null,impostor:kind==="nuke"&&real()?{at:+(camFar()*.86).toFixed(1),far:camFar(),band:[+(camFar()*.55).toFixed(0),+(camFar()*.95).toFixed(0)],w:S.impostorDraws&&FD?+FD.capW.toFixed(0):0,h:S.impostorDraws&&FD?+FD.top.toFixed(0):0,draws:S.impostorDraws}:null,dome:S.whiteDome?{r:+S.fireball.toFixed(1),t:WDOME_T,out:WDOME_OUT,p:WDOME_P,curve:domeCurve}:null,light:v2()&&CBZ.CONFIG.NUKE_FX_SKY?{sunK:LIGHT.SUN_K,hemiK:LIGHT.HEMI_K,near:LIGHT.NEAR,far:LIGHT.FAR,lum:[0,.5,2,6,15].map(function(tt){return+fireLum(tt,{style:P}).toFixed(3)})}:null,layers:{bills:S.bills,shell:S.shell,dome:!!(S.shell&&P.dome&&q01()>.45),volumeDraws:S.volumeDraws,coherentCloud:kind==="nuke"&&coherentCloud(),whiteDome:!!S.whiteDome,groundRings:0,genericPuffEvents:formation?formation.genericPuffEvents:0},ok:{zonesOrdered:P.thermK===0?zones.flatten<zones.glass:zones.flatten<zones.burn&&zones.burn<zones.glass,ringsOrdered:P.thermK===0?zones.flatten<zones.glass:zones.flatten<zones.burn&&zones.burn<zones.glass,noGroundRings:S.groundRings===0,noGenericPuffStorm:kind!=="nuke"||!formation||formation.genericPuffEvents===0,domeHandsToYoungCloud:kind!=="nuke"||!phasedCloud()||formation.handoffCapW<1e3&&formation.handoffCapY<500&&formation.handoffPhase<.1,fullNuclearFireball:kind!=="nuke"||S.fireball===S.nearField&&S.R===S.fireball,volumetricCloud:!P.volume||(kind==="nuke"&&coherentCloud()?S.impostorDraws===0&&S.volumeDraws===6&&S.bills===0:S.volumeDraws>=4),coherentPostFlash:kind!=="nuke"||!coherentCloud()||S.impostorDraws===0&&S.volumeDraws===6&&S.bills===0&&CBZ.CONFIG.NUKE_FX_ASH===!1,domeIsSedov:!S.whiteDome||domeCurve[0]>.25&&domeCurve[2]>.7&&domeCurve[3]===1,domeReachesFireball:!S.whiteDome||S.fireball===S.R,capOverhangs:!S.whiteDome||proportions.overhang>2.5,capFlattens:!S.whiteDome||flatK<.85&&flatK>.3,crownIsLate:!S.whiteDome||beats.crownIn>beats.collar3dIn&&beats.collar3dIn>beats.capIn,glowCoolsFirst:!S.whiteDome||beats.capGlowOut<P.dur,lightPeaksAtFlash:!(v2()&&CBZ.CONFIG.NUKE_FX_SKY)||fireLum(0,{style:P})>=.98&&fireLum(P.dur,{style:P})<=.02,thermalOutranges:P.thermK===0||zones.burn>zones.flatten,dipPresent:!P.dbl||dipV<.2,secondBrighter:!P.dbl||pk2V>=FLASH_DOUBLE[0][1],tallEnough:proportions.topOverCap>=(P.thermK>0?1.8:1.5),thinStem:kind==="nuke"&&real()?proportions.capOverStem>=2.5&&proportions.capOverStem<=4.5:proportions.capOverStem>=(P.thermK>0?6:4.5),capWiderThanTall:!(kind==="nuke"&&real())||proportions.capWideOverTall>1.15,capOverhangsStem:!(kind==="nuke"&&real())||proportions.overhang>2.5,riseDecelerates:!CBZ.CONFIG.NUKE_FX_RISE||(kind==="nuke"&&phasedCloud()?riseAt(.9+riseWindow({style:P})*.08,{style:P})<=.08&&riseAt(.9+riseWindow({style:P})*.58,{style:P})>=.65&&riseAt(.9+riseWindow({style:P}),{style:P})===1:riseAt(.9+P.riseT*.25,{style:P})>.5)}}};try{buildPool()}catch{}wire()})();
