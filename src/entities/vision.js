(function(){"use strict";const CBZ=window.CBZ;function visionWedge(radius,half,segs,color){const verts=[];for(let i=0;i<segs;i++){const a0=-half+i/segs*2*half,a1=-half+(i+1)/segs*2*half;verts.push(0,0,0,Math.sin(a0)*radius,0,Math.cos(a0)*radius,Math.sin(a1)*radius,0,Math.cos(a1)*radius)}const geo=new THREE.BufferGeometry;geo.setAttribute("position",new THREE.Float32BufferAttribute(verts,3));const m=new THREE.Mesh(geo,new THREE.MeshBasicMaterial({color,transparent:!0,opacity:.16,side:THREE.DoubleSide,depthWrite:!1}));return m.position.y=.07,m.renderOrder=2,m}CBZ.visionWedge=visionWedge})();
